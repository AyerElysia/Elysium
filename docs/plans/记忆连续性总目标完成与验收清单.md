# 记忆连续性总目标完成与验收清单

2026-09-08，北京时间。状态：进行中，不能以阶段有限出口代替全部目标完成。本文为工程任务清单，不是主体记忆。

用户再次指定[减法重构总计划](记忆连续性减法重构计划.md)，要求此前讨论的全部目标实施并验证，不等待重复提醒。本清单恢复[原需求 G01–G12](../architecture/记忆连续性理念与约束.md)的覆盖关系，避免只围绕 G06/G07 重复报告。此前“工程改动和回归已完成”的口头表述过宽：只有各报告列出的变更和样本通过，未覆盖目标不因此消失。

## 1. 完成判据与执行边界

逐项区分工程合同、真实主体执行、行为效果及长期外推。通过必须有可追溯输入、实际调用/产物/回执与独立检查；拒绝、未知、失败和未覆盖不涂改成成功。不保证“任意经历永不遗忘”或“长期稳定”这类无穷外推；仍须完成多样保留场景和相称的故障覆盖，不能用这一限制跳过具体工作。

此前工程改动和本清单保存在 WSL 源目录 `/root/Elysia/Elysium`；项目已另迁至 Spark 的 `/home/ayerelysia/Elysia/Elysium`，二者不是同一部署。2026-09-09 03:09—03:11 只读复核确认 Spark 正式目录存在、staging 已不存在，但无 Elysium 主进程/18000/8087，只有 New API 容器运行；旧 attach 命令和系统级/用户级 elysium.service 均未安装。此前 WSL 验收不得计作 Spark 验收。后续先核对最新源码、唯一正式 writer、两边新增历史与迁移状态，不直接同步运行数据或并行拉起两份服务。详见 [Spark 迁移位置与运行入口复核](../report/Spark迁移位置与运行入口复核_2026-09-09.md)。保留共享修改，不暂存/提交/推送、不创建 worktree。用户已明确授权本任务必要的精确目标安全重启；仍先核对进程 owner、新鲜保全和恢复方式，不建立 Elysium 自动拉起机制。主体执笔、选择、拒绝及权限边界保留；不新建常驻 reviewer、第二人格或旁路长期库。

持续跟进属于当前 Codex 工程任务，不是给 Elysium 加入新的认知活动或自动启动项。任务在确有进展时继续；不靠向主体刷消息、强制压缩、重复同题或提高轮数制造回忆证据。

09-09 04:39 双边只读差异核对：WSL 仍有 main.py 进程，Life Event tail=268066；Spark 无对应主进程，tail=267883。源端在 267883 后确有 183 行而 Spark 为 0；冻结后 21 行的位置/已存 payload hash 序列一致，但全历史/全域未在本轮核完。两边聊天及全局 runtime revision 不同、memory/tools.py 指纹不同；因此最新连续性和近期修复尚未由 Spark 承接。后续部署先无损保全并核对，不直接覆盖/同步运行库或并行拉起。

## 2. 总目标与未完成项

| 目标 | 已有证据边界 | 仍须完成的验收 |
|---|---|---|
| G01 行为影响 | 已发生来源校准和主体修订；不是长期效果 | 不同表达的新情境中，旧反馈是否改变实际检查/选择；保留无改变结果 |
| G02 整体文件空间 | S2 已列受管入口和两个后端的有限合同；原件 22 组签名补验通过。附件引用修复相邻回归 82 passed，结构同步另 3 passed；跨域原件→附件→历史→重开回取及临时 Life Event 账本重放（11 passed）通过。09-09 再次核实正式附件列、完成新鲜停机备份、受控重启及 50 份文档记忆恢复；New API 恢复后原测试流新邀请产生真实模型回合、工具调用和 delivery confirmed | 正式媒体接收→Life Event→主体回取的完整链仍须核验；本次实际对话未携带媒体，未产生媒体原件读取证据。原测试注入入口仅支持文字、既有凭据不含媒体权限，不能绕过。未受管脚本和实际编解码不能由签名样本替代 |
| G03 近期回忆 | S1 两次主体检查点、重启、未提前给答案样本 | 多样普通细节保留场景，逐题核对实际上下文暴露、遗漏/错归与可回取性 |
| G04 学习与习惯 | 同窗口纠错不等于习惯 | 真实方法实践、外部可核反馈、主体选择调整、不同材料再次实践；跨连续性条件另列 |
| G05 主体压缩 | S1 合同和真实检查点已通过，后续检查点安装已复核 | 新活动中继续核对实际作者、归档/释放与恢复，不代写、不强迫压缩 |
| G06 回忆入口 | 主体自写索引、精确引用与一跳回读已有证据；09-09 WSL 同流受邀接续发生续页核对和错误锚点纠正，实际稳定关系提示为 NULL | 形成阶段外、仅有自然线索的独立回取仍待验；非空提示真实采用与收益未证实，不能将字段存在当使用证据 |
| G07 成片关联 | 隔离两跳合同；真实主体本轮沿自然线索核对一条真实关系并自主停止 | 仍缺至少两条不同关联跳转到多个来源、形成阶段外的保留情境及同条件收益/误联想/成本；不得强造边 |
| G08 当前与历史 | S2/S3 版本、改名、删除、追加关系修订合同；managed-file 生命周期与 subject-document storage 定向回归 36 passed（`--no-cov`，2 个既有 websockets 弃用警告） | 结合 G02 检查遗漏入口；明确精确关系只锚定旧版本，不自动漂到 current；仍缺真实主体对当前/历史版本的自然选择证据 |
| G09 时效整理 | 一次改期/恢复可靠性；版本变化的正确区分 | 新证据与旧证据对照的真实选择；重读/重存不算重新验证 |
| G10 技能效果 | 机会安装/调用不等于技能掌握 | 优先复用“交接证据核验”这一真实实践，先发现可证伪失败，再验证方法调整是否改善 |
| G11 言行一致 | 一次安排、改期、恢复和关闭通过；不外推承诺 | 无外部风险场景中区分实际完成、未知、受阻、取消/重商，不能用再次保证替代行动 |
| G12 统一与减法 | 原始事件、主体文件、checkpoint、机会复用 | 每次增量证明现有入口不足及退出方式；汇总成本、恢复代价和失败，不新增专用系统堆叠 |

## 3. 当前批次：从真实局部读取误报进入反馈实践

09:14 的只读审计发现：原流 267355 请求精确交接版本 `ver_b4303781...`，`offset=41, limit=14`；267356 返回 `showing=41-51`、`remaining_lines_before=40`、`source_file_bytes=5551`，本次编号片段 2133 字节，`truncated=false`。此前第 25 行的临时推断不正确，实际是第 41 行。旧版本完整读回不能代替此新版本前 40 行已读；最终投递中的“已完整读回”没有本版本全文覆盖证据。

修复前源码只在末尾还有未读行时设置 `source_selection_truncated`；从中间读到末尾时没有此标记，而 `truncated` 仅是请求行窗口的传输分页。现已用临时数据复现并补齐已有范围标记与说明：任一侧漏选均显式为 True，完整选择显式为 False；原有 `limit=0` 选择全文、不受 offset 影响的语义保持不变。六个相邻定向文件合计 110 passed，未改历史正文、分页身份或主体判断。20:50 已受控重启加载当前源码；实际工具回执和主体行为仍须核实，不把加载当作行为改善。

向主体反馈的发送命令在启动前被自动权限审查拒绝：载荷包含内部文件版本与读取范围，需要用户对具体发送作确认。只读审计已证明引用来自同一授权流且已在该窗口出现；沿相同路径提交这些证据后仍被拒绝，未执行 POST、未产生新注入，不再重试或改走旁路。反馈稿仅作外部工程建议，不直接改写主体材料；批准后才由她决定补读、修订或解释。详见[修复证据、保留实践预注册与待决项](../report/文件读回范围误报与反馈待验_2026-09-08.md)。

实际反馈后，用未提前暴露答案的不同材料检查她是否能执行覆盖/版本核验，而不只说“以后注意”。结果可证明本次实践，不自动证明长期习惯。没有自然隔离条件时继续其它有用工作，不创建新流冒充新意识、不重问原题凑独立性。

## 4. 独立条件的已核查事实

`ConsciousnessRegistry.get_for_stream` 对未绑定场景流回退 `chat_global`。现有管理 API 提供读取、suspend/resume/drain，不提供受限测试器可用的“清空上下文/新建隔离主体”入口。测试凭据确实仅绑定原流，但这只是授权事实，不是已经穷尽隔离方案的证明。

优先沿真实有用实践等待主体自然形成新的连续性，再核对实际输入是否还暴露旧形成材料；必要时研究现有场景生命周期，但不能偷渡后台角色、读取其它实例私有上下文或制造第二正式实例。自然条件尚未成立期间，G06/G07 独立验收保持未通过，其他目标继续实施。

## 5. 执行记录

- 09:10：限定原测试流复核位置 267328–267364；10 个成功模型活动、12 次工具选择，11 成功/1 未知工具失败，最终发送与 delivered hash 一致。全部成功请求仍含形成材料标记。
- 09:14：确认新版本只读 41–51 行，保留失败，准备最小回归。
- 09:15：原实例健康；启动日志已证明完整 Memory 恢复，5207 条受管投影核对完成。未因本次检查重启或发送新邀请。
- 本批新增读取范围测试最初 9 failed / 1 passed；这些失败包含同一遗漏标记的多种表现，不能称作九种独立缺陷。修复后相邻回归最初 108 passed / 2 failed，两个失败来自新增测试对 limit=0 的错误假设；查明既有函数后更正测试与说明，没有改变生产选择行为。
- 最终六文件轻量定向回归 110 passed / 1 个既有 websockets 弃用警告，17.21 秒；新增测试全量 Ruff、修改工具 F/E9 与 git diff --check 均通过。一次非登录环境找不到 uv，未执行测试；随后沿既有登录环境完成上述有效回归。
- 此前具体反馈未发送，不重试被拒载荷。09-09 00:12 按用户明确授权完成再次受控重启；00:21:40 记忆恢复完成。最初普通邀请因同一 WSL 的 `new-api.service` 未运行而连接失败；启动该既有 unit 后于 00:43:52 自动重试，产生真实模型回合、`action-life_send_text` 调用和 delivery confirmed。主体反馈迁移、不同材料再实践与形成阶段外验证仍不因这一单次投递而通过。
- 前次命令因错误测试路径收集 0 项并触发覆盖率失败，不是一次全仓通过；修正为准确两个文件后 131 passed。这是相邻定向回归，不替代其它目标验收。

后续每一项把实现、测试、真实行为和未通过项同步写回此清单及对应报告，不将全部完成作为纯口头承诺。

09-09 03:37—03:41：本地复现并修复稳定关系提示第二次异步读取后未复核 authority 的缺口。新增三项先 2 failed/1 passed，修复后关系修订/语义关系/文件覆盖三文件 72 passed、1 个既有警告；未重启或部署到 Spark。原始事件 267947 复核确认提示为 NULL，已更正将字段存在说成真实使用的表述。见 [二次读取边界补验](../report/稳定关系提示二次读取边界补验_2026-09-09.md)。

09-09 05:11—05:16：双边只读 proactive 分域诊断确认 Spark 保留 WSL 路径 identity 的既有绑定，唯一旧迁移证书只证明当时 1 行，不能证明当前 302 行；WSL 为 318 行。现存条件解释先前 generation root mismatch，旧 root 不随日常历史增长而更新是正常行为，不能强改 root/marker/chain 来“修复”。新增已绑定 endpoint 漂移的临时数据样本 2 passed，迁移测试全文件 13 passed / 1 个既有警告；更正首次绑定修复的适用边界，生产代码和正式数据未改。详见 [Spark 根因续验](../report/Spark迁移位置与运行入口复核_2026-09-09.md)。

09-09 05:39：补齐主体文档域比较，WSL/Spark 八张指定表按主键全部列的类型敏感行摘要、schema 摘要和计数均一致，包含 5349 版本、5350 head event 和 5337 path event；不是只比较数量。只读事务分别 0.251/2.652 秒，无正文输出、正式写入或服务操作。外部原件/投影、Life Event 全历史、其它数据域与后续变化仍未由此覆盖；不将八表一致升级为 Spark 完整接续。详见 [八表核对证据](../report/Spark迁移位置与运行入口复核_2026-09-09.md)。

09-09 06:07：刷新双边只读前沿：Spark 仍 tail=267883、freeze 后 21 行且无 main.py；WSL 运行中的源端已 tail=268078、freeze 后 216 行，Spark 尚缺 195 行，聊天/全局 runtime revision 为 WSL 1928/28323、Spark 1888/28156。共同冻结窗口位置/hash 仍一致，新增差异来自源端继续写入；因此迁移接续阻断仍在扩大，不启动/同步/覆盖，先保全并核对新增历史与唯一 writer。

09-09 06:38：有界 Life Event 尾部重读确认 WSL tail=268105、Spark tail=267883；源端连续新增区间 267884–268105 共 222 行，Spark 为 0，较 06:07 再增 27 行。只读摘要同时保留位置、occurrence/source ID、sequence、时间和 payload hash 的序列摘要；未输出正文，未做跨全库去重或其它数据域核验。缺口持续增长，仍不启动/同步/覆盖。

09-09 07:08：补齐 Learning 两表及机会运行时十表的全部列摘要核对，schema/行数/行摘要两端一致：Learning 事件 24616 行、投影 3 行；机会登记 3 次、发生/回执各 2 行等。只读事务分别 0.465/2.382 秒，无正式写入或服务操作。不把表一致当作技能成熟、长期习惯或 Spark 真实消费证据，也不消除跨路径绑定/源端新增历史阻断。见 [十二表证据](../report/Spark迁移位置与运行入口复核_2026-09-09.md)。

09-09 07:39—07:44：Core 分域核对发现真正的共同编号记录分叉：WSL 消息 90978 行、Spark 90915 行，WSL 尾部多 63 行之外，id 91239–91251 的 13 行 message_id/time 均不同、其中 12 行正文摘要不同；两组 message_id 经对方整张 Core 消息表哈希查找均 0/13 命中。两端均须保全，不得只补源端尾部或整库覆盖。图片元数据两表完全一致，API 媒体两表均空；附件引用实际原件尚未验。无正式写入或服务操作；源端诊断 helper Ruff/语法检查及 git diff --check 通过。见 [Core 分域续验](../report/Spark迁移位置与运行入口复核_2026-09-09.md)。

09-09 08:09：进一步只读交叉匹配 26 个分叉 Core `message_id` 摘要与各自 Life Event payload 字符串标量：WSL 的 13 个 ID 全在其 Life Event 位置 267863–267875 出现；Spark Life Event 也保留这批 WSL ID，而 Spark Core 自己的 13 个 ID 在该次扫描为 0/13。这只说明该 ID 映射尚未建立，不证明所有历史归因都不存在；两组数据均须保全并按实际协议继续复核，不能单向覆盖。08:39 只查正命中的 13 个事件，双边均确认标准 `metadata.chat.message_id` / `provider_identity.message_id` 各 13 命中；已纠正此前过强的“均无稳定归因”表述。无解析错误、正文输出或正式写入。详见 [Core/Life Event 交叉归因](../report/Spark迁移位置与运行入口复核_2026-09-09.md)。

## 6. 不受反馈发送阻断的 G02 媒体原件补验

10:39 已完成独立的临时数据补验：新增 `test/api/v1/test_media_original_continuity.py` 28 项，与既有媒体 API 12 项合跑 **40 passed / 1 warning，7.35 秒**。22 组图片、音频、视频和普通二进制签名样本覆盖原字节/hash/引用、保存重开、重复保存、识别结果与原件分离、越权读取拒绝及同长度损坏拒绝；另有 4 个解析器类型边界和 2 个事务事件失败后重试用例。仅新增测试与工程记录，生产媒体代码未改，未重启、未操作正式媒体、未发送待批准反馈。

这些是签名和存储合同，不证明媒体可播放、识别质量、断电恢复或主体使用收益。尤其媒体 `media_id` 和主体 `document_id@version_id` 仍是不同域，不能将原件存储通过写作 G02 全空间已统一。下一项沿现有媒体原件→附件引用→生命事件/主体回取入口检查是否保留固定来源与可读引用，不新建平行记忆。详见[媒体原件合同补验](../report/记忆文件空间媒体原件合同补验_2026-09-08.md)。

09-09 09:09 UTC 跟进批次（北京时间 17:09 触发）：媒体原件与 Life Event 分段存储回放复核合计 39 passed / 2 个既有警告。此前“最短链路已确认”表述过宽；当时没有从主体可见工具取得附件，不能推出模型侧可达。静态检查本身未改生产代码或正式数据，后续修复见下条。

09-09 18:44（北京时间）：实际核查确认 `nucleus_read_event` 只返回正文，已落账的附件描述符未由该工具提供；新增可选 `view=attachments`，以既有 UTF-8 分页返回严格验证、排除内部 storage_key 的引用 JSON，区分未记录／明确空／损坏，绑定事件和描述符前沿，不读取媒体或增加授权。四类临时媒体→授权解析→消息→双事件适配器→重建测试服务→工具引用→原件库重开／授权回取闭环通过，主体并未实际参与。新增 21 项，最终五文件 81 passed / 1 既有警告（12.58 秒）；不累计前次重叠结果。源码与文档只在 WSL 源副本，未部署到 Spark、未发送任何验收消息；真实 G02 接收／主体回取、全部脚本入口与媒体收益仍待验。详见[媒体原件合同补验第 7 节](../report/记忆文件空间媒体原件合同补验_2026-09-08.md)。
