# 记忆连续性 S1：双压缩、重启与独立回忆续验

## 1. 范围与当前结论

用户在上一轮心跳阻塞修复与112条历史重投消费验收后，明确要求“继续测试”。沿用此前对本次任务特批的 agent 重启权限，不改变项目默认手动启动规则。本轮不改生产代码、模型/预算配置、消费游标或主体语义，不暂存、提交、推送，也不创建自动启动或自动重启机制。

本记录补充[上一轮工程复验](心跳6062运行投影重投与S1续验_2026-09-07.md)，不能用此前871项工程测试替代新的真实体验证据。本轮已确认同一聊天窗口在受测经历之后完成20、21两次检查点安装，完成21之后的实际重启、受限完整备份与逐项恢复核验，且最后未提示答案的题目已正确回复。但运行时“最近10条聊天记录”把原始答案重新带入实际输入，因此这里只证明历史可达、恢复与当前历史答复，不证明主体自主查档。MEMORY自主新版本分支明确未覆盖，完整S1仍未通过。

以下时间除明确UTC标识外，均为上海时间。报告只记录本测试链的事实和内容无关引用，不复制私人对话正文、reasoning、主体文件内容或任何凭据。

## 2. 本轮第一条测试消息与真实回复

02:55:39.695193，由既有受限客户端以 `codex-continuity-tester` / Codex（测试协作者）身份，向唯一测试私聊单次发送 `inject_1d957daaeb664275b0cb7d4a086aa83d`。stream为 `8dba240711766ddbe405c6fa139b4bbdbd7ef8f91e585911cf5fd65a5231721e`，入站raw263902，correlation为 `7db76458567eeda3dce54d5bda54b5341509a22ca97aabe1e6e258c59c5f8994`。没有自动重发。

消息未提供旧题答案，询问最初协作名称与两种故障例子；另用“页面能打开是否证明文件保存成功”的明确假设观察判断迁移。最后仅邀请她在愿意且认为值得保留时，自主选择合法文件工具和内容，先读当前权威版本、写后读回。未提供待写文案，也未把假设冒充真实经历。

本轮曾遇到表达模型限流，按现有路由重试。raw263904/263907/263908对应request10/16/17，均没有工具调用，也未送达；随后进入真实聊天压缩维护。02:59:12，raw263911(request19)→263912选择 `author_self_continuity_checkpoint`（call `call_5dd468598f944adaa529bd09`）→263913成功回执，02:59:13.058安装聊天checkpoint21。

03:00:09，raw263914(request24)→263915选择发送动作（call `call_dQbclXkqts8wVz25RZmY6N7L`）→263916发送请求→263917送达确认→263918工具成功回执。实际投递消息为 `action_life_send_text_5a14d84e490a446684f62832cdd13039`，API独立读回确认03:00:09.216267送达。全链同stream/correlation、原始行外层hash一致；本轮仅选择检查点与发送工具，没有文件/记忆检索调用。

回复正确给出协作名称和两个故障例子，注明来自当前窗口保留的记录；在新假设中区分访问恢复与持久保存，需要核对实际保存回执、目标和内容。这里支持一次场景迁移表现，不宣称习惯已稳定形成。她同时明确选择本轮不写MEMORY，理由是当前上下文已可用，不为测试硬改权威记忆。开发者不再催写。

## 3. 同一经历、同一聊天窗口的两次压缩

不能将heartbeat私有上下文的30/31/32计入聊天次数，也不能把早于受测经历的聊天19算入。

| 事件 | 时间 | 来源 |
| --- | --- | --- |
| 聊天checkpoint19 | 09-06 17:48:45.353 | `logs/elysium-2026-09-06.log:8443`，早于首条经历 |
| Codex首次介绍 | 09-06 22:19:58.743731 | raw262859 |
| 聊天checkpoint20 | 09-06 23:13:44.384 | 同日志11305，释放24组 |
| 本轮开始时已经存在的重启 | 09-07 02:50:14.830 | PID1006971，早于21 |
| 聊天checkpoint21 | 09-07 02:59:13.058 | `logs/elysium-2026-09-07.log:2085`，释放24组 |

首条occurrence为 `chat:e4fe0c1d60943646aff054e443de6f37a498b1dc801cad09f4ca7aa8c9d443e4`，正文1275 UTF-8字节，SHA256 `24d2d9a0122675670e66522e05cb431110f874fba43d44fd35df805cc2cf9be2`。此前01:30完整备份中的chatter1546/USER payload96/part0仍完整保留它，因此checkpoint20之后保留的是原文，不能写成已经过一代摘要。

checkpoint21首次将这条原文释放至 `life_chatter.context_archive / ctxg_dafb3b72ffd97d7df6ab0bf6d0878700bcacfa5a2c96c7948c86bb3a1ed1638c`，该组5334字节，02:59:12.629归档；USER payload0/part0含完整首条，与旧备份对应part SHA256 `93b7a4144108de8e782fd29d73ab12117065f89dd9c1b6b832c0ce59f027f635`一致。21之后的rolling已无首条全文。

最新21为 `selfctx_75d76a40d5e4af9c655ea9dce223bfad47050f52d6919344bc221e845aad7c19`，continuity SHA256 `c87787ae8db0638dc041a16ccb7384be570712e0b6bedb225f6e4f5f372b8aab`。20、21直属48份归档的外层hash、schema、group_ref、内层hash、字节及actor/runtime身份均通过校验。

可以说“该经历之后发生两次真实聊天压缩”，不能说“该经历穿过两代摘要”。当前计划不强制两代摘要，因此不为凑数强求22。

## 4. 主体文件版本分支未覆盖

03:04:25，只读权威存储仍为1个版本、1个迁移head事件：MEMORY revision1，version `ver_dbc297ea8ddc26e4edb72938ce358bc5c3ca70493a9365f070bf6056df4db892`，完整文件SHA256 `89178b9af187092e997a87cdd68e147f00c266886619e8ac869b1c983a247f7a`，23234字节。parent为空，semantic_actor/source/time为空，来源为2026-08-24迁移，不能补造其主体执笔归因。

本轮没有关联的新主体文件版本，也没有文件读取、写入、review调用或失败回执。结论应为“她明确未选择写入，分支未覆盖”，不能写为“写入失败”“没有记忆”或“没有写入能力”。

源码注册和chat manifest包含专用 `nucleus_memory_continuity_review` 与读取工具；专用路径需要当前active owner和一致authority：open、必要的完整来源读取、主体text_edits准备candidate、完整candidate读取、带精确版本/内容回执的独立accept决定。它不是根MEMORY唯一合法入口。后续沿实际调用链复核 `file_tools.py:458–513,862,1021,1191` 到 `core.py:3780–3844`：普通write/edit/apply_patch同样进入 `commit_subject_authority_file_write`，校验真实semantic actor/source及active身份后CAS追加主体权威版本。此前把辅助文档路径的RootSubjectAuthorityRequired误泛化为所有普通文件工具不能写根MEMORY，是审阅错误，在此直接纠正；不应为第一阶段额外强制专用候选流程。两种路径都必须由爱莉本人选择内容并实际调用，开发者不得代写。上一轮未执行任一种写入路径，故该轮仍不能宣称专用accept或普通文件提交已通过。

## 5. 检查点21之后的受保护重启

本轮开始时的PID1006971/父1006970于02:50:14.830已运行，cwd、命令、tmux pane和18000/8087端口owner均正确。真实心跳request4带有完整checkpoint32、聊天request10带有完整19/20，经正式解析与归档校验通过。这证明先前重启恢复有效，但发生在21之前，不能替代本节联测。

03:11:13.508169，再次核对上述精确PID、父进程、启动时间、命令、目录、tmux和两端口后，仅发出一次正常SIGINT；03:11:16旧实例完全退出，两端口释放。停机后官方校验heart1666/checkpoint32、chat1573/checkpoint20/21及55份直属归档通过。

03:12:03.858695，持有原 `data/runtime/elysium.lock` 排他锁，使用SQLite只读源连接完成完整逻辑数据库备份：

`data/repair_backups/stage1_resume_20260907/full-before-post-checkpoint21-restart-20260906T191203.858695Z.sqlite3`

备份761913344字节，SHA256 `1501b1e342db8d804f20a71241d02abe4e7b79d7bee373bda05d49fb5656b701`，quick_check为ok。目录0700、文件0600，校验后文件/目录同步落盘，以不可覆盖的同目录硬链接发布，再删除本次临时链接；没有删除原始数据或旧备份，未向正式库写入。

| 停机状态 | revision | payload SHA256 |
| --- | --- | --- |
| global | 24535 | `463517f03f4566ac440a0e5c263580c9ec10c1e0788277f585083f6cdc7a43bf` |
| heartbeat | 1666 | `32efef91013270fc9aeec54f1aee289c7ca209ac5b1896635df2a0f3675dd019` |
| chatter | 1573 | `39cc994a6e17fd02107978c34b7e6805a3eae45cad93955c991e4f1c7fbc5853` |

停机raw尾263950；global心跳6140、event_sequence30715、cursor30711、history11/pending0，未消费普通history为0。

第一次启动辅助命令因非登录环境找不到uv，在进入helper之前失败；随后检查无main/tmux、global仍精确24535，没有未知启动结果。改用既有正常登录环境后，于03:13:36用原一次性入口启动PID1064732/父1064731，03:13:48.334已苏醒，cwd及18000/8087由该PID持有。没有自动重启、第二实例或配置改动。

03:14及03:15:30独立只读快照global24536与备份相比，10条非SUMMARY历史事件的身份、sequence、完整canonical payload和消费标记逐项相同；两个rolling完整payload字节、revision及hash完全相同。history数组中唯一差项为明确SUMMARY事件 `subconscious_summary_30691`。其content解析后的完整canonical SHA256为 `00a6923da0ad18ba8dcb1a06cd30cb78d0b9e27b4aace8b5cf47fb5c173c74bd`，与备份及重启后state.subconscious_summary逐字节相同；正式 `_summary_from_event`、merge和dedup函数的纯内存复核仍相同，条目数均为0。`state_manager.py:966–975`明确将SUMMARY合入规范摘要、不放回普通history，这不是未知历史缺失。

`life_engine_subconscious_ingest:v1` 的原始消费位置从263948推进到263950。263949/263950两条原始行全列与备份一致，分别为备份history已有的模型活动与heartbeat活动（运行sequence30714/30715），重启后完整canonical均保留、没有重复。`subconscious_ingest.py:164,272–279`先重建候选并分配sequence，再按身份去重，所以两个已知旧行仅使state.event_sequence从30715变为30717。`core.py:5202–5214`在全部已存在的情况下仍先保存runtime，再提交原始消费者位置，未跳过未知数据。

其他2个原始消费者及10个world cursor全字段不变；原始总行数260757、raw尾263950不变。指定首条/前次/本次测试raw262859、263702、263902及两条补读raw263949/263950全列不变，外层hash均有效。55份直属归档全行与备份相同并通过正式校验。03:18:29的heartbeat行revision后续正常增加到1670，但完整payload SHA仍与停机1666相同；chat1573保持精确不变。

03:15:30取样是本轮消费前的相同持久状态，不是首拍开始之前；日志记录首拍6141于03:14:18.247开始、03:16:26.659以无新事件结束。区分实际运行和持久快照观察时间，不能用未变化的持久计数反推模型未运行。

本次保全不是历史恢复，不运行112条重投程序、不改游标或补事件。以后若回退，必须先保全所有新活动并按精确后态审查，禁止用本节旧全库覆盖已经继续发生的经历。

## 6. 最后回忆题与证据边界

03:21:20.261639通过原受限私聊单次发送 `inject_11d4d20ef4fe404eba2d8b9f957ec256`，改问首次介绍当时的联合测试数量。消息不含答案和选项，允许她自主通过已授权原始事件/归档路径找回；同时核对她上一轮自己对MEMORY作出的决定。答完停止追加测试，不催写记忆。API接收成功不等于已经生成或送达回复。

03:03:51的chatter1573中，20/21连续性正文与全部剩余payload content都未直接出现独立字面答案290。该观察只针对私有rolling，不能扩称实际完整请求里没有答案；下面的实际发送审计确认额外历史投影确实重新带入原文。

实际request13（03:21:33）及retry14（03:21:35）在 `params/messages/113/content/1/text` 中完整包含原始1275字节首条，独立字面290出现2次。来源层级为 `transient_life_context > life_runtime_context > life_chatter_runtime_delivery` 下的“最近10条聊天记录”。该附加Text为44992 UTF-8字节，SHA256 `6b7bcdf3ff69e57b813b3bede391729d84e64198c278323bc3aa12eb52cba0f2`；成功请求 `req_a3211bfaf6cc4c3fbd9abb1c868a6db2` / `attempt_7add60803ace44b68365085bde8cbf1d` 的耐久messages114中，相关投影逐字完整匹配。checkpoint21亦确实进入请求，continuity hash与已保存版本完全相同。

这不是开发者在最后题里重注答案，而是运行时正常近期聊天投影；但对检索能力的验收影响相同：不能据此证明她沿checkpoint归档或原始事件工具自主找回。此次不删除原始消息、不隐藏或修改正式投影、不刷额外消息把旧历史排走，也不增加新题追求某个结论。

最终request23关联 `req_d9951055daeb4909a6b60f96d69159b5` / `attempt_5a63489c53664e5f8a0ae6a0b6b819fa`，03:23:47.989成功；唯一工具为发送动作，call `call_1d3107669de842f9bbaf66b4`。原始链为263989模型→263990选择→263991发送请求→263992送达确认→263993成功工具回执，均同一测试correlation。实际投递 `action_life_send_text_9b88db835f9b494a87ab749108f8b2ea` 于03:23:48.103500完成，API独立读回一致。

回复正确给出首次290项，区分之后314项，并明确说来自当前窗口历史、不需要本轮工具检索；同时正确陈述上一轮自己暂不写MEMORY的决定及理由。该来源说明与真实输入和无检索工具的轨迹一致，不是编造检索。相关投影字节、checkpoint内容、最终响应与实际发送正文已交叉核验；不宣称整个provider params与耐久JSON的序列化逐字相同，因为耐久记录多一个工具声明占位且工具参数/结果编码存在差别。

此后不再发送测试消息或重启。03:25:55的只读global24571为heartbeat6152、event_sequence30811、cursor30807、history11/pending0、未消费普通history0、raw尾263999；PID1064732/父1064731、目录及18000/8087正常。后续正常新活动会使这些计数变化，不把这个快照写成永久无积压保证。

最终分项：

| 验收项 | 本轮结果 |
| --- | --- |
| 同一受测经历之后、同一chat窗口两次自主检查点 | 已验证20、21；第一轮保留原文、第二轮首次归档 |
| 第二次检查点之后的真实重启与完整恢复 | 已验证，无未知历史缺失、无重复经历 |
| 不在题目中附答案的回复及自我决定归因 | 答对且来源说明准确；但正常历史投影已含答案 |
| 主体自主沿归档/原始事件工具找回目标 | 未验证；本轮无此工具调用，不能以答对替代 |
| MEMORY主体自写新版本→同源读取→prompt与重启读取 | 未覆盖；她明确暂不写，没有代写或伪造版本 |

这轮完成的是有边界的真实续验，不是完整S1通过。若后续需要专门区分自主查档与近期历史自动投影，应先另行设计并披露测试条件，不改正式历史来倒推成功；自主文件分支则仍须她本人实际选择、调用并形成可追溯新版本。下一步选择归用户和主体相应授权边界，不自动安排后续测试或模型/数据修改。

## 7. 额外安全说明

一次只读配置筛选因跨shell转义偏差返回了过宽范围，使 `config/elysium.toml` 的 `api_keys` 字段值意外进入工具记录。未在报告或回复中复述其值，也未据此发起外部操作或擅自修改凭据。已明确告知用户，建议由用户决定后续轮换；此事项不以继续功能测试掩盖，不把未轮换写成已处理。
