# 潜意识近期上下文运行审计（2026-08-19）

## 1. 审计范围

本次只读审计核对以下提交进入主线后的真实运行情况：

- `7c380d02`：提供有界的潜意识近期上下文投影；
- `e017559b`：跨意识投影不再携带工具原始参数；
- `fd6cc81c`：Minecraft 消费同一投影；
- `ef8d95f9`：Voice Live 与 Memory Witness 消费同一投影。

审计只读取 Git、进程列表、文件日志和训练轨迹；没有启动、停止或重启
Elysium，没有修改正式数据；本报告不包含用户聊天正文。

## 2. 运行时间线

- 2026-08-19 02:06:04：包含上述提交的主线合并提交 `8447c206` 完成；
- 2026-08-19 02:50:33：Life Engine 成功启动；
- 2026-08-19 02:50:37：Voice Live 插件成功加载；
- 2026-08-19 02:50:38：Voice Live 路由成功挂载；
- 2026-08-19 02:50:40：Voice 主体上下文预热完成；
- 2026-08-19 03:23:55 起：Elysium 开始关闭；
- 2026-08-19 03:24:09：日志记录收到 `SIGINT`，随后执行关闭流程；
- 审计时：未发现 Elysium 主进程，只有独立视觉嵌入等辅助服务仍在运行。

因此，02:50 至 03:24 的运行窗口确实加载了本次实现；当前状态不能被报告为
“在线健康”。

## 3. Chat 真实链路证据

在 `data/training_data_lake/raw/2026-08-18.jsonl` 中，以
`【潜意识近期上下文】` 作为结构标识做 content-free 统计：

- 共发现 177 个模型 attempt，属于 169 个逻辑 `life_chatter` 请求；
- 168 个逻辑请求至少有一个成功 attempt；
- 1 个逻辑请求全部失败，错误为上游 `RemoteProtocolError` 后连续
  `LLMAPIError`，不是投影生成、预算、哈希或隐私校验失败；
- 在 02:50 完整重启之后发现 3 个 Chat attempt，3 个全部成功；
- 三次实际投影分别包含 5、2、3 个完整因果组；
- 三次投影 UTF-8 大小分别为 4,552、3,209、4,053 bytes，均低于
  16 KiB 硬预算；
- 三次投影均包含已发生的工具调用与工具结果；
- 三次投影中 `raw_args_present=false`，没有工具原始参数；
- 三次投影中 `private_message_rows=0`，没有复制私有聊天消息行；
- 日志中未发现 `RecentSubconscious`、预算超限、投影哈希无效或
  `subconscious_reference` 校验错误。

结论：Chat 的“潜意识近期上下文 → 当前意识模型请求”生产与消费链已经在真实
运行中生效，预算和隐私边界符合设计。

## 4. 尚未形成真实闭环的消费者

02:50 至 03:24 的轨迹中只有以下模型请求类型：

- `life_chatter`；
- `life_engine_heartbeat`；
- `life_epistemic_opportunity`；
- `router`。

Voice Live 已完成插件加载、路由挂载和主体上下文预热，Minecraft 具身组件也已
初始化，但该窗口没有真实 Voice call、Minecraft intent 或 Memory Witness
authoring 请求。因此只能确认这些消费者“代码已加载且没有初始化错误”，不能把它们
报告为真实调用已通过。

## 5. 同期平台故障

2026-08-19 03:09 至 03:23，日志持续出现无法连接远程 MySQL
`frp-one.com` 的 `OperationalError`。受影响的可见任务包括 Learning maintenance
和 Memory index batch。之后 Elysium 进入关闭流程，当前没有主进程。

审计时未使用数据库凭据，只对日志记录的远程地址执行一次 TCP 连通性探测；
`frp-one.com:65429` 仍然超时，因此不能把该异常写成“已经自行恢复”。

该故障没有表现为潜意识投影错误，但会阻止继续进行在线跨实例验收。不能用已有
Chat 成功记录掩盖数据库与进程当前不可用的事实。

## 6. 最终判断与后续验收门

- **Chat：真实链路健康。**
