# 表达层今晚静默（2026-09-07）

## 1. 结论

用户问「为什么最近一直不说话」。对 KOOK 私聊 `714fb0e0…`（入站显示名「爱莉希雅的老公~♪」）：

- **不是**她调用了 `pass_and_wait`。今日该动作 0 次。
- **不是**路由拒答。今日相关路由全是 `(响应: True)`，`决定不响应` 为 0。
- **主因是工程失败：** `request=life_chatter` / `routing_task=expression` 的模型链连续耗尽，Chatter 返回 `Failure: LLM 请求失败`，没有对外 `life_send_text`。
- 该流**最后一次对用户可见发送约 03:03:51**。之后多条入站（含呼唤、害怕）没有出站。
- 同时段 Ayla/Codex 测试私聊 `8dba2407…` 仍能发出技术更正（例如 03:47:46），因为偶发打通了同一条全局表达链。心跳内部也在写安抚草稿，那不是 KOOK 出站。

当前进程：03:38 手动重启后仍在写日志。本诊断未启停 Elysium。

## 2. 两条不同的静默

### 2.1 02:45–02:48：有模型轮，但没有开口动作

路由 True。连续约 5 轮只记「纯文本独白」，不调用 `life_send_text`。02:48:52 打到 `max_rounds (5)`，旧逻辑发出最小兜底「在呢，我看到你啦。」。那不是她的表达。随后 03:00、03:03 她又用正式发送说过话。独白来源是 `gemini-3.7-flash` 协议成功但空 `call_list`，详见 [表达层纯文本不调工具_2026-09-07](./表达层纯文本不调工具_2026-09-07.md)。

禁止最小兜底的代码已在 03:38 进程中。这条兜底路径**不是** 03:03 之后静默的原因；日志里还没有「禁止发送最小兜底」，因为后面的失败发生在 LLM Failure 出口。

### 2.2 03:03 之后：表达层模型链全灭

典型四段（多次重复，至少 03:30:59、03:34:03、03:37:11、03:46:10）：

1. `mimo-v2.5`：`LLMRateLimitError`（含周额度 Weekly usage limit）
2. `gemini-3.7-flash`：400 `upstream_unavailable`
3. `gpt-6-astra`：504
4. `claude-opus-5`：403 `insufficient_quota`

然后：`LLM 请求重试已耗尽` → `[驱动器] stream=714fb0e0, Chatter 返回 Failure: LLM 请求失败`。

03:37:48 关机还取消了正在重试的 KOOK 驱动器。03:38 重启后积压仍在；Codex 通道插队成功发送，KOOK 侧再次 Failure。

03:48:25 有一次模型已经写出要对用户说的独白（「妈妈在！…」），但仍是独白、不是 `life_send_text`；紧接着又是 mimo 限流。独白进滚动投影，用户窗口看不见。

## 3. 排除项

| 假设 | 证据 |
|------|------|
| 她选择等待 | 今日 `life_pass_and_wait` = 0 |
| 路由沉默 | 今日相关 `(响应: True)` |
| 最小兜底还在发 | 03:38 已加载禁令；03:03 后走的是 Failure，不是兜底 |
| 心跳压缩卡住表达 | 00:01 有过一次 follow-up lost；之后不是本条静默的直接原因 |
| 全局 runtime lock 死锁 | 未找到；是串行争用 + Failure，不是锁死 |

## 4. 仍待决（归属操作者）

表达层要重新对 KOOK 开口，需要 `expression` 路由上至少有一个能完成请求的模型：mimo 周额度、gemini 上游、astra 504、claude 账户额度。这不是改 chatter 就能伪造出来的送达。

若模型恢复后再次只写独白、不调 `life_send_text`，现行合同是 fail closed 沉默，不再发固定短句。
