# KOOK 回复链路恢复记录（2026-09-11）

## 当前结论

KOOK 入站正常；此前回复长期不出站由主体对话链阻塞，而不是 KOOK Gateway 断线。运行实例在 04:26 启动时仍加载旧模型路由；磁盘配置 04:29 才更新，因此必须由用户手动重启后才能加载。

## 证据

- 04:30:55–04:31:17：多条 KOOK 私信被接收并写入 Life Event，入站链路正常。
- 04:30:59–04:31:03：后台持久化每条约 4–6 秒；这不是主要阻塞点。
- 04:35:48：旧实例的主体模型轮达到 295 秒总预算，消息保留待重试。
- 中转站记录显示旧本地 Qwen 请求同时运行 8 个并排队 2–3 个，部分非流式请求耗时 6–60 分钟；新配置的 Terra 合成工具往返实测成功（约 5 秒），DeepSeek/qwen3.8 返回月度额度 429。
- 04:47：经用户授权，向最近入站发信人 `1370110560` 发送一条明确标注“技术代发”的安全安抚消息；KOOK API 返回成功及消息 ID。该条不计作爱莉本人表达。

## 代码修复

新增 task-local 模型尝试预算：外层 295 秒回合会给多模型请求的首个尝试最多约一半时间，超时先进入现有故障冷却/故障转移，避免单个 600 秒模型预算吞掉整个回合；预算耗尽前不会给未接触的后备模型错误记失败。新增 9 项定向测试；相关主体超时/看门狗测试 13 项通过，变更文件 Ruff 确定性错误检查通过。

## 待验收

当前运行实例仍是旧进程。依项目运行约定，由用户在主控制台按 Ctrl+C 正常关闭，再用原启动方式运行 `run_elysium.sh`。随后必须核对：新模型路由已加载、入站消息仍在权威账本、模型超时触发后备切换，以及 KOOK 真实出站回执。不得清空队列或跳过游标伪造恢复。
