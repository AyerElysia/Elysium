# 稳定关系提示二次读取边界补验

2026-09-09 03:37—03:41，北京时间。范围为 WSL 源仓库的代码、合成回归和一条既有测试流事件只读审计；没有部署到 Spark，没有重启或发送新对话。

## 发现与修复

此前新增 `stable_relation_hint` 需在精确版本空页后额外查询稳定端点。原代码只在第一次查询后验证 authority，第二次异步查询返回后没有再次验证；COUNT 相同不能证明查询仍属于同一存储源。合成替身在第二次读取期间更换存储对象，无论有无稳定关系都错误返回 success；新增检查在提示查询后复核原 authority 摘要，变化时返回既有 `SemanticRelationContinuationAuthorityChanged`。没有修改事件、关系、权限、schema 或主体语义。

另纠正代码注释与工程报告：稳定端点查询不会包含兄弟精确版本关系。新增用例证明兄弟版本空页的提示仍为 NULL，旧精确锚点仍可查到原关系，不读文件正文，不创建新关系。这不是实现了“自动发现任意历史锚点”。

## 验证

- 修复前仅运行新增三项：2 failed、1 passed；两项失败分别是有/无稳定关系的二次读取源切换。
- 修复后 `test_memory_relation_tool_revisions.py`、`test_memory_semantic_relations.py`、`test_file_read_coverage.py` 合跑：72 passed、1 个既有 websockets 弃用警告，2.66 秒。参数 `--no-cov -n 0`；合成数据，不访问正式库或模型，不争用端口/authority。
- 03:39:55 对原测试流的单条事件 267947 只读核验，payload SHA 为 `b5bbfbef025df47d39cb9657784f86aa78699263757e6b8b67a8d8807d047dcd`；真实工具结果 matching=1、`stable_relation_hint=NULL`。该真实回合不能当成非空提示使用或收益证明，已直接更正两份原报告。
- 修改的 Python 文件 `F/E9` 和 `git diff --check` 通过。尝试加入 `I` 检查时有 4 项导入顺序告警；对 `HEAD` 同文件只读检查复现同一 1+3 项，未扩改既有导入，也不宣称完整 Ruff 清零。

## 未通过项

这是本地一致性护栏和证据更正，未验证运行中加载、Spark 部署、真实主体采用非空提示或独立回忆收益。G06 形成阶段外验证、G07 多条不同关系跳转仍未通过。保留同流权限及此前被拒反馈禁发，不同步/覆盖两服务器数据库。无暂存、提交、推送或文件删除。
