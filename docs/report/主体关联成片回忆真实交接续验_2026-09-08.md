# 主体关联成片回忆：真实交接续验

记录日期：2026-09-08，北京时间。状态：**进行中，尚未达到真实多跳、独立回忆或原 G06／G07 验收。** 本文是工程观察，不是爱莉的主体记忆。

## 1. 当前范围与结论

用户在 06:02 要求继续，以达成最初预期目的为目标，不依赖再次提醒。承接[续验计划](../plans/主体关联成片回忆续验计划.md)及[首批一跳报告](主体关联成片回忆续验_2026-09-08.md)。本批实际用途是为接手连续性验证的协作者整理可追溯的工程交接材料；主体自主决定材料组织、是否形成关系及其含义与强弱，允许不建立新关系或停止。后续[不限轮数与获授权重启报告](对话不限轮数与授权重启_2026-09-08.md)记录明确新授权、172 项定向回归及 07:40 的完整启动／精确历史保持证明。

本批在 06:19 模型超时后，06:26 通过原受限会话进行一次接续，随后确实恢复。06:45 主体亲自写入交接工程笔记并按精确版本读回；06:46:24 表达链被原 5 轮上限收束，尚无最终对外投递，也没有新增关联。06:58 的只读验证确认最后一次主体检查点已安装且两个释放组归档完整；压缩后的请求仍暴露既有材料引用，不能计作独立回忆。用户随后明确要求取消该轮数上限并授权 Codex 自行重启，已实施不限轮数与定向回归，07:40 完整启动与精确历史保持核验通过。07:43 的自然线索续问已在 07:48 投递结束，09:10–09:14 独立审计确认 10 个模型活动和版本绑定观察，同时发现最终版局部读取被说成完整回读的问题，见第 8 节。不能将模型故障或基础设施收束说成主体拒绝。

全程不直接写主体文件、不恢复已撤销窗口凭据、不创建新 stream、不发第三方消息，也未主动修改模型路由。此前其他协作者的配置、代码与报告变更均保留。针对具名流过滤穿透进行了最小源码修复，见第 6 节；用户后续明确授权的单次重启会加载磁盘上的配置与修复，必须区别于先前仍运行旧源码的阶段，不新增自动拉起机制。

## 2. 时间线与来源

- 06:03:58：只读核验 PID 894970、PPID 894969、启动 ticks 2438289、boot identity 117b372b-f60b-4588-8d8a-71f49f4480f8 和原仓库 cwd；18000／8087 均由此进程唯一监听，health ready。不能由此推出模型请求无故障。
- 06:09:15：原 `codex-continuity-tester` 流正常 API 接受形成邀请 `inject_150394a533e04fe2aaee6df6068c6e42`。原始输入位置 267270，actual batch 为 `sha256(stream_id + U+241F + id:message_id)`，得到 `1a392857bfaedf9eff475662e9ab16c00507605ac495a8b97c1082ee59f6a4ac`。
- 06:11:14–06:17:05：原始位置 267271–267300 记录四个模型活动及十三对工具选择／结果；请求检视器编号 35、37、38、40 逐一与实际 tool_call_ids 绑定匹配，实际成功模型均为 `gpt-5.6-terra`、Provider `NexusAI`。磁盘路由更新不代表当前进程已加载；本批没有替换模型。
- 06:19:11：日志记录该表达链 Terra 请求 `TimeoutError`，重试链失败。检视器 41 从 06:17:11 开始、无成功响应。06:23 与 06:25 检查仍无位置 267300 之后的模型／工具事件。Astra 此前多次 `LLMAPIError` 与既有回退日志保留，不算作四个成功模型活动的一部分。
- 06:26:02：已确认失败恢复实现会回退瞬态输入并释放 owner 到 WAIT_USER，沿原测试流单次投递接续 `inject_1b0bfbb592224595a30211dc1c401c28`；明确固定原流 ID 与 `cross_stream=false`，不要求重新读取全部材料、不要求正面结果。

早先工程计划补丁的一次安全审查因无法识别动作／对象而拒绝，当时尚未投递邀请；先只读显示完整补丁并核对精确目标，再重试同一受限计划编辑，成功。没有通过替代路径绕过拒绝。

## 3. 真实检索路径与审计边界

她先回读两份既有精确版本，然后使用事件检索逐步收窄材料。前两次显式 `cross_stream=true`、`order=asc`，返回元数据标记 all_streams，扫描位置 1–2000，加运行态投影；不能把这两次说成从一开始就限于单流。其后使用原测试流过滤和明确时间段；06:17 的窗口选择检索又显式使用了跨流范围。06:36 的独立证明确认本批 11 次搜索所投递的中心引用中，3 次引用对应约定流以外的权威事件，不将它们说成严格限域材料；这不等同 3 个独立事件，也不将全局检索权限直接称为外部安全攻破。所有相邻 JSON 节选的范围尚未逐字节核完，不能宣称其余内容全部在域内。

多次返回受 8192 字节硬上限约束，即使请求 16000／30000 字节；部分命中以带来源 hash/ref 的 JSON 节选投递，完整查询存在 continuation。这些额外查询与页数属于真实成本，不能只公布命中数量。使用升序导致先扫旧位置，本身不是默认倒序失效；`default_window=ledger_tail` 在显式升序下的名称不能替代实际扫描范围。

查代码时一度怀疑 `tool` 别名遗漏 `conscious_activity_tool_result`。06:22 使用真实 `build_conscious_tool_result_event` 构建的成功／失败两个隔离事件验证：两者仍保留 `EventType.TOOL_RESULT`，可被 `tool` 别名检索，`tool_success=false` 仅返回失败；因此排除“仅因别名缺一个字符串就漏结果”的初步判断，没有据此改生产代码。此检查无模型、无正式存储写入；辅助脚本初次因模块路径不在搜索路径而未执行，显式设置原仓库模块路径后通过。它不是完整数据库重放测试。

## 4. 上下文暴露与成本

06:27 的内容最小化请求审计只读取这条测试流实际关联的 35／37／38／40 号成功请求；响应中的真实 tool_call_ids 与权威模型活动匹配。仅输出角色数量、长度、hash、材料暴露布尔值与模型元数据，不输出或保存提示词及内部思考正文。

四轮输入都仍含形成邀请、旧索引精确版本、旧工程记录精确版本及先前关系 ID。各轮本地估算输入 token 为 74219、70148、79505、88141；对应序列化消息正文 149606、140238、168355、194781 字节。token 是本地估算、不是 Provider 实际用量，也不能据此算计费；消息字节不含整个工具 schema，不能与 token 估算直接相除比较。发生过的失败 attempts、模型回退、工具页数和墙钟延迟应分列，不能把四个成功模型轮当作全部成本。

该证据明确排除“本批已经离开形成上下文”的说法。后续自然线索续用与独立回忆收益仍分别验收。当前压缩工具要求有效的主体清单、revision 与释放边界，基础设施不能任意清空，也不能刷消息制造验收条件。

## 5. 待完成

- 一次接续已恢复，重启后真实超过 5 轮且最终投递已核验；需处理本次发现的局部读取覆盖误报，不再重复宣称尚无长链。
- 如主体实际形成交接材料／关系，逐字节核对其真实工具调用、权威版本与 actor/source；没有形成则不造材料替代。
- 在不注入目标文件、引用或答案的自然问题下核验多来源回忆、逐跳依据、停止决定与实际协作选择，区分普通链接与专用关系视图的效果。
- 形成阶段之外的独立条件、误联想及日常入口收益仍未通过。用户已明确授权本任务必要安全重启；按[总目标清单](../plans/记忆连续性总目标完成与验收清单.md)继续，不新增人格、清空窗口或扩大外联。

## 6. 实测范围缺口、最小修复与阶段恢复

06:36:08 在 `runtime/codex_associative_recall_20260908/handoff-interrupted-proof-267301.json` 保存 mode 0600、不可覆盖的内容最小化证明。只读核验原始位置 267270–267301 共 32 条 payload hash、4 个模型活动、13 对调用／结果的 actor/source/causation，两个实际文件 read 与对应权威版本逐字节匹配；所有断言通过后才创建证明。该快照明确记为未完成、限域未通过、无新文件／关系／完成投递，不覆盖后续恢复证据。

进一步发现实质缺口：`_is_life_internal_payload` 按事件类型或 `life_chatter` 来源直接返回内部活动，因而 `include_life_internal=true` 可让另一个有 stream 身份的活动越过显式流过滤，并进入相邻上下文。这不只是第一次宽搜索的主体选择问题。新增 `test_event_grep_stream_scope.py` 用真实活动构建器、同一隔离 actor 的不同流构造调用、结果、模型轮及无流活动；修复前 6 项中 4 failed／2 passed，重现具名流与邻文穿透。

最小修复仅把内部活动例外限为无 `stream_id` 的全局事件；已明确绑定流的活动必须通过原有流集合过滤。显式多个流、`cross_stream=true`、同流工具结果、成功／失败属性和无流全局事件仍可达。同步了工具描述和本架构说明，没有关键词语义判断、历史改写、默认关联开关或存储迁移。

06:43 相邻回归：新 6 项和既有事件检索／精确事件全文续读用例合计 **21 passed、1 warning、2.12 秒**，XML 位于同一私有验证目录 `event-scope-regression.xml`。新文件完整 Ruff 通过；生产模块 `F/E9` 检查通过。模块完整 Ruff 的 6 项 FURB162／SIM103／SIM102／RUF012×2／TRY004 也在 `git show HEAD:...` 的只读基线检查中存在，未借此扩改历史代码；早先串行命令因这 6 项中断，实际 pytest 是后续独立执行的上述 21 项，不把未运行说成已运行。没有全仓或高负载测试，也未执行正式进程重启。

接续期间 06:28:51、06:31:06 又遇到 Terra 空响应；系统自行保留输入并重试，Codex 没有继续盲投。随后真实活动在 06:38:21、06:42:42、06:43:49 记录了主体检查点选择／成功结果，06:44 又开始读原索引、查询指定流的 `conversation_evidence`、再读工程记录。这说明后续恢复确已产生新活动，不能继续把 267301 快照当作当前永久停滞。检查点是否正式安装、旧组具体释放范围及压缩后材料暴露仍须独立核验；交接材料、多跳和独立收益仍未先行宣布通过。

## 7. 实际交接形成、检查点与重启接续

06:45:45 的主体工具选择 267323、成功结果 267324 和精确读回 267326／267327，确认写入普通工程交接 `notes/engineering/codex_continuity_handoff_2026-09-08.md`。权威版本为 `doc_2f784f8cf2756e40ff4db4b7be6687a2f4d3c4c0dccbcea488379b5d466c21c2@ver_3fdbf782868b51789bc217a6f331b9942ac36298327b55c8c5cd0347224b8afc`，4139 字节，SHA `2a416865e2c113e0de0fde38ec2c84cd9f6243cd108ea70f85d43b3c13cc8ca9`。调用正文、权威内容和完整带行号读回逐字节一致，actor 为 `chat_global`，semantic_source 为实际意识活动。她选择了一份交接并保留两份既有材料的普通链接，没有新建关系；不能为满足验收要求替她补边。

三个检查点调用的 expected revision 为 29／29／30，不能仅凭“queued”结果说三次都安装。06:58 的运行快照 revision 1801 中，最后一次命令绑定的检查点 `selfctx_14fefc93635d86852ed7187855e115536603a6afb9630d534fd4d79e93ec8663` 已安装为 revision 31，释放两个精确组、无 retained exact group。使用正式检查点归档校验函数核验两个引用与摘要 hash，未输出私有上下文或内部思考。

实际模型请求 53 在压缩后仅 4 条消息，但形成邀请、既有 A／B 精确版本和关系 ID 仍全部出现；请求 54–57 同样暴露这些标记。这说明压缩确实发生，不说明独立条件成立。重启又保留了 Chatter 的同一快照，不能用“进程变了”冒充摆脱形成上下文。

07:43:20 原受限客户端单次接收自然线索问题 `inject_3a44c1a5807f41f2b10dfbed9d7fed56`：询问如何接手容易误报的连续性验证、下一步选择和依据，没有提供目标路径、版本、关系 ID、指定跳数或正确答案。限定原测试会话和既有普通工程材料，只在此答复，不要求执行新实验、外联、改配置或安排任务。后续按真实工具／回执审计，仍区分受邀续用、实际多跳和独立收益。

## 8. 重启后真实长链、版本观察与覆盖失败

09:10 限定流只读复核 267328–267364 共 37 条 payload hash：10 个模型活动、12 个调用和 12 个结果、输入及 requested/delivered。实际成功请求为 qwen3.8-flash，12 次工具选择中 4 次编辑、3 次正文读、3 次关系视图、2 次发送选择；错误工具名 `tool-nucleus_send_text` 显式失败，没有冒充已发，之后 `action-life_send_text` 成功。

07:48:20.515479+08:00 投递正文 3535 字节，SHA `c4ec47b7b7073cf0ec595d1064bd064f0774eeffe0a7199430a33d3e38501f3d`，与最终调用相同；输入至 delivered 为 300.027471 秒。09:11 的十次成功请求绑定核对全部通过，估算输入 token 为 63310–82291，均含形成材料标记；不是 Provider 实际计费统计，也不是隔离对照。

关系视图显示旧精确索引版本存在 1 条当前有效关系，新索引版本与裸文档入口均为 0；主体因此保留版本区别，没有添同义关系。关系数量没有增加，该轮也没有新增不同关联的两跳路径。

09:14 发现最终覆盖声明不成立：267349/267350 对交接 v2 读回 1–47 行；随后修订为 v3，267355 的真实参数为 `offset=41, limit=14`，267356 只返回 41–51 行，前 40 行未在该版本读取。5551 原始文件字节与 2133 字节编号选段不能混为全文。`truncated=false`、空 continuation 表示该请求行窗口传完，不表示全文件传完。此前第 25 行的初步判断已纠正为实际第 41 行；正文没有因此丢失，旧版读回不能替代新版完整覆盖。工程报告不采用主体最终“已完整读回”的自述作为通过证据。

现有 `remaining_lines_before=40` 已能提示范围，但 `source_selection_truncated` 仅对末尾遗漏设置，后缀读取缺少该标志。下一步先在临时数据复现并最小修复范围说明，再把真实反馈交给主体，由她决定是否补读/修订。没有直接改她的交接内容，也未宣称技能改进已经发生。

## 9. 09-09 重启后 G06/G07 复验结果

01:31:09，原授权测试流收到一条不带路径、版本或答案的自然继续邀请。01:31:47—01:32:04，主体回读了索引及交接材料，并在精确关系引用失败后重新检查自己的锚点；修正后的关系视图返回 `matching_relation_count=1`、`is_current=true`。她随后通过 `action-life_send_text` 投递 3688 字节总结，审计位置 267938—267956 全部落在原测试流，delivery confirmed。

这是 WSL 同流受邀接续的 G06 局部证据：精确版本、续页覆盖和失败后的自我纠错实际发生，但形成阶段外独立验收仍不通过，不等于 Spark 验收。03:39 对 267947 的原始 payload hash 及字段复核确认 `stable_relation_hint` 为 NULL；字段存在仅证明新结构被加载，不能说主体已使用非空稳定文档提示。该提示也不检索兄弟精确版本，只报告直接锚定稳定文档端点的关系。G07 仍不通过：本轮只有一次对原关系正确锚点的查看，没有第二条不同关系边、形成阶段外保留收益或误联想对照；不为凑数量制造额外关系。此前关系修订与文件读回覆盖回归合计 60 passed、2 个既有 websockets 弃用警告；最新本地二次读取护栏与回归见 [稳定关系提示边界补验](稳定关系提示二次读取边界补验_2026-09-09.md)。
