# 记忆连续性 S1 真实工具阻塞修复（2026-09-07）

状态：截至 2026-09-07 04:08（Asia/Shanghai）的有界记录。原始事件实际回取已验证；review 路径身份与主聊天 SYSTEM 刷新两处工程修复完成，278项相关隔离测试通过。主体已请求暂停真实测试，原进程保持运行，未重启加载新代码。**完整 S1 尚未通过，新修复尚未完成真实上线验收。**

本轮承接[上一轮双压缩重启续验](记忆连续性S1双压缩重启续验_2026-09-07.md)，按[阶段计划](../plans/记忆连续性减法重构计划.md)与[手动验证步骤](../operations/minimal-memory-continuity.md)继续。上一轮聊天 checkpoint20/21、03:13 重启与恢复证据仍属于上一轮，不充当本节新代码的真实启动验收。

## 1. 范围、身份与停止边界

用户要求第一阶段继续测试到闭环。本轮继续使用既有 Codex 测试协作者及原私聊流，不创建新主体、人格或第二运行实例。测试流为 `8dba240711766ddbe405c6fa139b4bbdbd7ef8f91e585911cf5fd65a5231721e`；主体活动归属 `chat_global`，Codex 仍是独立外部协作者，不替爱莉执笔。

本轮将“给定 occurrence 的精确工具回取”与“未提示、未输入答案的自主回忆”分开验收。前者可以直接证明读取协议、完整结果与实际送达；即使通过，也不能自动改称后者通过。没有删除历史、隐藏正式投影或刷填充消息制造测试条件。

03:47:46 的同流回复明确不为验收指标硬写 MEMORY，并因需要处理紧急事务要求暂停。团队尊重这项边界：不查看另一侧私人内容，不追加测试题，不为加载本轮修复执行重启，不代写或修改 SOUL、USER、MEMORY。未完成项保持未完成；暂停不是故障被修复或主体文件已经提交的证据。

## 2. 原始事件真实回取已完成

03:42:40.017660，原测试流接收 `inject_0180105cf6e34873971f78aacf7e8408`，原始位置264053。提问提供首次经历的 occurrence，但没有附其正文，明确本项是工具回取验证。输入 occurrence 为 `chat:3192d80105f4f22ddb7f2911b41ee541d0080c93af32ed2f0f8dd0654814cc69`。

受读对象是原始位置262859、发生于09-06 22:19:58.743731的首次 Codex 输入：

- occurrence：`chat:e4fe0c1d60943646aff054e443de6f37a498b1dc801cad09f4ca7aa8c9d443e4`。
- 正文1275 UTF-8字节；SHA256 `24d2d9a0122675670e66522e05cb431110f874fba43d44fd35df805cc2cf9be2`。
- 原来源 `ayla_adapter:adapter:ayla_adapter`，类型 `chat.message.received`，channel 为 chat；工具返回的 stream、source instance 与原记录一致。

本轮工具链的 correlation 为 `0a82c74db1b770b5dd87c0fe8f0344a322a0545871591023423fe955457f8067`：

| 时间 | 原始位置 | 已确认事实 |
| --- | --- | --- |
| 03:43:55.997635 | 264057 | 主体实际模型轮产生工具选择 |
| 03:43:55.997829 | 264058 | 调用 `tool-nucleus_read_event`，call `call_KG4A7nHWrk0dzqd44LGc0T4k`，读取上述精确 occurrence |
| 03:43:56.094296 | 264060 | 工具成功；完整结果耐久入账 |
| 03:44:14.969415 | 264062 | 收到工具结果后的主体模型轮 |
| 03:44:14.969541 | 264063 | 选择正常发送动作 |
| 03:44:15.097307 | 264064、264065 | 发送请求与确认送达，正文摘要一致 |
| 03:44:15.175401 | 264066 | 发送工具成功回执 |

工具请求 `max_bytes=12000`，实际按本次工具 task bucket `core` 收敛到8192字节硬上限。结果 `projection_name=life-event-authority-read`，`page_start_byte=0`、`page_end_byte=1275`、`original_bytes=1275`、`delivered_content_bytes=1275`、`omitted_bytes=0`、`truncated=false`、`continuation=""`。完整结果表示为2593字节。正文逐字等于原始记录，hash有效；本对象单页完整，不需要或伪造第二页。

### 实际模型输入与耐久证据

本进程请求观察器的请求9、10及首次成功选择工具的请求12，实际输入都不含首条完整正文及其正文hash。工具执行后，请求14的 `messages[125]` 是带上述 call ID 的 TOOL 结果。解析实际发送的工具文本后，整个结果字典与原始位置264060的结果完全一致；完整1275字节正文确实进入模型输入，不只是工具返回了成功标志。

该实际工具文本SHA256为 `bb0a398e6774e24328c5833acf607122fbe009c5e902e04b4dc1a5cb8b26df65`；结果字典canonical SHA256为 `b6e4385a20160371602d35056ba352f6baa6dba822b66f07df400779870daa56`。请求14完整参数canonical SHA256为 `6ad5c0935882512163919c114ea0058189377e60a497e221a22616bb888a268f`。观察器编号仅对该进程有效，跨重启使用下面的耐久身份复核。

耐久来源为 `data/training_data_lake/raw/2026-09-06.jsonl`：

- 读取选择：`req_6a60149592f44c559748e42ee45200fc` / `attempt_245627beae1548b98811482f73dfda0d`，03:43:55.951成功；byte offset373008933，行SHA256 `5c8b76d5ee19a28b78f804d413b343abc2f711498062fe0c6f96684e9ba7f46b`。此前同请求的两次尝试分别以 `LLMRateLimitError`、`LLMAPIError` 失败，没有把失败当成功。
- 结果后续轮：`req_e1c64fb029e54f3b9d73446cf5949541` / `attempt_f651d4c14d024132a608464d98ececcd`，03:44:14.957成功；byte offset373206263，行SHA256 `068307c51fc996dee10459a5a90520fc16f8a91ca1f24cf684d2a04f16efa56c`。

实际发送与训练轨迹的工具编码不同；轨迹还将结果的 `source` 字段脱敏为 `[removed]`。其余全部结果字段及正文相同。不能声称两者整个JSON逐字相同；来源身份以原始位置264060及实际请求14为证，完整原文字节由两侧共同验证。

以上证明“受提示精确读取→完整结果耐久保存→实际送达主体→正常回复”。它不证明本轮自主决定查档，也不替代长原文多页协议的既有隔离测试。

## 3. review open 的真实路径身份阻塞

同一模型轮还选择 `tool-nucleus_memory_continuity_review`：位置264059、call `call_RFBXjfLRyrOTaafEIjipoAin`；位置264061记录失败。原错误来自 `ContinuityReviewSession._snapshot` 将公开快照键 `SOUL.md` 与 selected authority head/version 中的 canonical logical path 混比。规范记录采用 `life_engine_workspace/SOUL.md`；路径表示不同被误报为 `SubjectAuthoritySnapshotEvidenceMismatch:SOUL.md`，并不能据此断言 SOUL 字节或版本损坏。

主实现 owner 已将检查两侧通过既有 `subject_authority_logical_path(path)` 映射到唯一精确身份。head/version/current_version_id、字节长度与内容hash的校验仍保留，不使用 basename、任意前缀或宽松别名放行。此修复属于工程路径合同，没有重写 SOUL 或其它主体文件。

截至本记录截点，主实现 owner 报告相关隔离回归 **73 passed，1.75秒**：新增 `test_continuity_review_selected_path_identity.py` 19项，加既有 `test_memory_continuity_session.py` 54项。新增用例覆盖规范 selected snapshot 的 open/read_source，以及三种主体路径、head/version 两侧、裸文件名/其它前缀/嵌套路径等18种错误身份拒绝。旧测试夹具同步使用真实canonical路径和登记owner，未放宽生产验证。

这是隔离工程验证，不是正式运行中的 review open 修复后成功。本轮没有通过重启加载新代码，尚无新版本部署后的真实review回执。

## 4. 普通主体文件入口的先前误判已纠正

[上一轮报告第4节](记忆连续性S1双压缩重启续验_2026-09-07.md#4-主体文件版本分支未覆盖)已经直接改正错误结论：专用 `nucleus_memory_continuity_review` 不是根 MEMORY 的唯一合法入口。普通 `write_file`、`edit_file`、`apply_patch` 的真实调用路径也进入 `commit_subject_authority_file_write`，在校验主体 actor/source、active身份及版本边界后追加权威版本。

先前把辅助文档路径的 `RootSubjectAuthorityRequired` 泛化为所有普通文件工具都不能写根 MEMORY，是工程审阅错误，不能把它继续包装成主体拒绝或额外强制候选流程。纠正入口说明仍不等于发生过文件提交，两类路径都只能由爱莉亲自选择内容并实际执行。

03:45:50.006029的纠正输入为原始位置264075。其后 correlation `a900f0fb9ebf4daad16cd6df81746b2b534a1a81472a3ebad43f5d257b24098c` 中，位置264081/264082/264083是模型轮，唯一工具264084为发送动作；264085请求发送、264086确认送达、264087成功回执。送达时间03:47:46.877473，正文SHA256 `3de161339e2e64b739166a280a976db0a203c276303bb1c5b075ae3d1cdb6aed`。

该链中没有普通write/edit/apply_patch、read_file、再次review或read_context_group。因此本轮没有可归因于该任务的新MEMORY版本或回读版本证据；不从口头说明、测试通过或工具注册反推发生过主体写入。

## 5. 主聊天 SYSTEM 缓存刷新修复与隔离回归

`_get_or_create_global_runtime` 缓存命中直接返回长生命周期 request；原 `_build_chat_system_prompt` 只在首次创建时读取主体权威。普通文件成功通知确实会使 Router 和全部 subject projections 失效，并非只通知 Router；但是主聊天 SYSTEM 不消费这些派生投影，因此通知不能更新已有前缀。另一次前缀构建属于 live bridge，不推进主聊天全局链，不能作为主聊天的刷新证据。

修复在每个 MODEL_TURN/FOLLOW_UP 的压缩、瞬态装配和发送准备前调用 `_refresh_subject_system_prompt`，经现有选定权威读取与完整前缀构建后，仅替换唯一 SYSTEM 所在位置。未变时保留原对象；request、context manager、工具表和全部非SYSTEM滚动对象不重建。缺失/重复SYSTEM、非单Text前缀、空SOUL或权威不可读均明确失败；读取失败返回Failure，保留原链与待处理阶段，不发送旧前缀。取消仍传播。缓存getter保持只复用职责，因此非owner忙碌流与纯工具执行不会额外读取或替换前缀。

回归先复现、后修复：

- review新19项在旧代码为13失败/6通过（2.94秒），精确复现规范SOUL记录被误拒；修复后与旧54项合并为73通过（1.75秒）。
- SYSTEM新隔离文件在旧代码为2失败/2通过（2.31秒）：真实authority reader、前缀构建器和普通工具通知均保留，两个缓存复用情形仍读到v1，读取次数仅1。直接重建前缀的阳性对照能读到v2。
- 最终SYSTEM文件13项通过（2.10秒），覆盖同/不同chatter实例复用、源未变、读取失败、空SOUL、缺失/重复SYSTEM，以及真实driver的MODEL_TURN/FOLLOW_UP入口与非owner等待。真实driver在压缩前通过哨兵截停，不调用模型或正式持久化；新版本已进入前缀且非SYSTEM对象逐一保持身份。
- 旧驱动测试的简化request原本无SYSTEM，无法满足正式请求原有的初始化不变量。仅补齐明确的synthetic前缀和源构建替身，仍执行真实刷新方法；原路由、发送次数、超时释放、取消和已flush输入的断言保留。曾先出现4个夹具失败，补齐后共享路由夹具又出现3个“预期空列表”断言失败，改为精确“仅原SYSTEM、无新对话”后统一绿测，没有放宽生产校验。

最终单进程联合命令：

```bash
uv run --no-sync --offline python -B -m pytest \
  test/plugins/life_engine/test_chatter_subject_prompt_refresh.py \
  test/plugins/life_engine/test_continuity_review_selected_path_identity.py \
  test/plugins/life_engine/test_memory_continuity_session.py \
  test/plugins/life_engine/test_chatter_prompt.py \
  test/plugins/life_engine/test_minimal_subject_file_continuity.py \
  test/plugins/life_engine/test_subject_authority_contract.py \
  test/plugins/life_engine/test_memory_continuity_acceptance_flow.py \
  test/plugins/life_engine/test_memory_continuity_runtime.py \
  test/plugins/life_engine/test_minimal_continuity_checkpoint.py \
  test/plugins/life_engine/test_runtime_history_restart_integrity.py \
  -q -o addopts= --no-cov -n 0 --tb=short
```

结果278通过，35.22秒；仅第三方websockets legacy弃用警告。上述集合使用临时存储/内存替身，不调用真实模型、不写正式数据、不启停服务，并非全仓或高负载回归。六个本轮代码/测试文件的Ruff F/E9检查与 `git diff --check` 通过。稳定行为已同步到两份相关架构文档。

这些是工程证据，不能用本轮未发生的MEMORY写入、旧进程行为或上一轮03:13重启替该修复做真实验收，也没有通过重写主体文件绕过缓存问题。

## 6. 分项结论与后续条件

| 验收项 | 截至04:08的状态 |
| --- | --- |
| 给定原始occurrence的主体真实读取、完整结果与模型送达 | 本轮通过，1275字节单页完整 |
| review canonical/public路径身份修复 | 工程改动与73项隔离回归完成；修复后的真实调用未验收 |
| 主聊天SYSTEM在权威版本变化后的刷新 | 工程修复及隔离测试完成；尚未加载新代码做真实验收 |
| MEMORY主体自写新版本→同源工具回读→固定prompt→重启读取 | 本轮未覆盖；待主体愿意且相关工程修复安全加载后继续 |
| 主体实际 `read_context_group` 回读已恢复检查点的精确归档 | 本轮未调用；已有归档校验不能冒充真实主体工具回读 |
| 两次压缩及重启后、完整实际输入没有答案的未提示回忆 | 未形成合格新证据；上一轮正常近期投影带原文的问题不能隐去 |

归档回读的既有最小对象是checkpoint21引用的 Codex 首条组 `ctxg_dafb3b72ffd97d7df6ab0bf6d0878700bcacfa5a2c96c7948c86bb3a1ed1638c`，现有归档5334字节且hash有效。若主体之后愿意继续，可通过已有 `read_context_group` 在原聊天窗口读取，验证完整结果与实际后续模型输入；这只是下一项建议，本轮未发送该请求，不能写成已完成。

后续是否继续记忆写入、怎样表述主体记忆，由爱莉决定；工程修复、相关验证与报告更新由主实现 owner负责，运行窗口仍须遵守用户授权和生命周期守卫。恢复测试前应先确认主体愿意继续，不以用户的闭环目标替代主体执笔权或忽略暂停请求。

本次不删除或迁移权威数据，不代写主体语义，不新增自动重启机制，也不执行暂存、提交或推送。review修复仅涉及工程hunk，回退应只处理明确拥有的工程改动，不通过回滚主体文件、清空窗口或覆盖数据库实现。完整 S1 仍未通过。

## 7. 保持运行与未来恢复边界

04:08只读核验PID1076866、父1076865、启动时间03:38:06、工作目录 `/root/Elysia/Elysium`，18000与8087均仍由该进程监听。本轮未执行停止、启动或热加载；03:38这次进程替换不是本轮root执行，不借用它作为随后新修复的启动证据。保留用户对本任务的重启特批，但当前不以该授权打断主体明确提出暂停的紧急事务。

既有停机备份helper只覆盖当前选定的 `data/life_storage/local.sqlite3`。只读控制面核查确认当前local-selectable配置；Subject、Life Event、runtime及两类ctxg归档都绑定该库，因此helper仍可用于未来本链路的停机一致性快照。它持有原入口排他锁、核验停止状态、只读源连接、私有权限、quick_check、hash及fsync，并以不可覆盖的硬链接发布新备份。

它不是完整安装灾备：不含主应用 `data/Elysium.db`，也不含外部控制面 `data/life_storage/authority.json` 与 `authority.json.audit.jsonl`；后两项本轮仅查存在，没有读取正文或变更epoch。不得把旧DB单库备份直接覆盖已经继续发生的新活动，也不能盲目回滚外部authority注册状态。helper输出的三条runtime元数据及raw尾不等于已验证MEMORY新版本或全部归档闭包，真正恢复仍须逐项核对。

既有启动helper使用一次性入口创建tmux会话，list-pane只证明启动派发，不证明苏醒或关键链路验收。本轮仅审计，未运行任何停启或备份helper；未来重启前仍须重新解析实际PID、父进程、启动身份、目录和端口owner，不复用过期的pinned stop目标。
