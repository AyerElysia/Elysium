# 记忆连续性 S2 停机保全与恢复验收

日期：2026-09-07。截至本地 2026-09-07 22:53，原有限 S2 范围已闭环：A、B 与阶段出口通过。22:50 新实例完整恢复，22:52 本次重启后的真实 glob 与 v1／v3 全文读取成功，主体反馈已 delivered，22:53 独立只读 proof 通过。S3 由根代理随后独立计划与开工，本文不记为已实施。

闭环不代表所有文件空间：旧 outbox 360 的 UNKNOWN CONFLICT 保留、未强制投影；媒体仍为独立域，任意脚本／全部导入／全部格式未验证；SQLite 与 MySQL 分别同后端恢复，未执行跨后端切换或旧代码回退。

本报告承接 [S2 实施清单](../plans/记忆连续性第二阶段实施清单.md) 与 [减法重构计划](../plans/记忆连续性减法重构计划.md) 的有限出口。工程测试的合成文本不是爱莉实际形成的记忆；备份副本能恢复也不等于她已在真实任务中用到历史。

## 1. 授权、操作所有权与范围

用户本次明确授权：“你有权限随意重启系统，全交给你了。”该特批仅用于本次继续完成 S2，由根代理独占正式服务、数据、配置和生命周期操作。子 agent 不启停服务，不操作正式数据库或私有备份；本文维护者只维护工程测试与文档，并根据根代理明确回执记录正式动作。

默认人工启动策略未永久修改。没有新增 systemd、cron、登录自启、while 重拉或其他自动恢复循环，也不拉起第二 Elysium 实例。正式启动后的竞争型测试已停止。主体语义的执笔权、历史只追加、隐私与权限边界不因运维特批而改变。

本次实际配置由根代理核对：local selected enabled；art_studio enabled，temp_dir／vibe_storage_dir／command_images_dir 均为插件默认相对目录；skill_manager plugin.enabled=False；screen.enabled=False、save_latest=False。该结论来自实际配置，不是按代码默认值推断。子 agent 未读取私有配置正文。

## 2. 时间线与证据来源

下表时区为 Asia/Shanghai（UTC+8）。目录时间只表示证据目录命名时刻，不冒充操作完成时刻；没有收到精确时间的结果按执行回执顺序记录。

| 时间／边界 | 事件 | 来源与状态 |
| --- | --- | --- |
| 本轮停机前 | 用户给出本次受控启停特批，根代理独占正式操作 | 用户授权由根代理转达；默认策略不永久改变 |
| 20:00:33 | 正常停止原 PID 1151978，相关端口为空 | 根代理现场执行回执；未由子 agent 操作 |
| 20:07:14.960842（目录标记） | 建立 stopped-before-s2 冻结备份证据目录 | 根代理在启动锁内调用既有快照／校验 API，后续独立验证通过 |
| 停机后串行隔离窗口 | 新 schema 恢复文件 5 项通过；实际 MySQL 首项通过；art 安全分组推进 | 分组证据详见第 5 节，窗口由根代理串行分配 |
| 20:20:19.219791（目录标记） | 建立 candidate-recovery 私有隔离副本并开始升级恢复演练 | 根代理执行；仅新增候选副本，不修改原冻结备份 |
| 候选演练完成回执 | subject 原有列、八表导入重开、memory 6→7 与再复制恢复、完整性及原备份 hash 全部通过 | 根代理报告 PASS、exit 0；不把中途 subject 单域通过提前记成整体验证通过 |
| 候选完成后 | 24 文件综合回归 407 项通过；art 最后 56 项通过；MySQL 最终 2 项通过 | 各批次独立且存在覆盖重叠，不累计成 470 项或另一“全套” |
| 20:29:28 | 根代理按本次特批执行一次性 run_elysium.sh，tmux pane 中报告 PID 1425601、.venv/bin/python | 只证明启动动作已执行；随后插件初始化失败，不能写成真实启动验收通过 |
| 20:29:34 | Memory startup recovery 出现 RuntimeError，继而 SelectedMemoryStorageInitializationFailed，life_engine 加载失败 | 根代理正式日志回执；接口仍有 liveness 不能掩盖核心插件失败 |
| 20:32（只读核对） | 正式 subject 仍为旧 v4，无 bindings 表／列，5,331 documents／5,333 versions 数量不变；memory document_index 已到 7、无 managed 节点 | 根代理现场核对；显式正式 schema 升级需求／初始化顺序原因仍待定位，不把推测写成根因；计划受控停机，不回退历史 |
| 20:33:41 | 正常停止失败的正式实例，之后在停写和启动锁保护下执行维护 | 根代理现场回执；不回退已经耐久的 memory7 或主体历史 |
| 20:43:50.806963（目录标记）及随后完成回执 | 补充 selected／memory 两库、registry／audit 与配置快照后，以真实同 generation 维护租约执行正式 subject4→5，重复 ensure 幂等；最终 PASS | 根代理执行并核对完整旧列 roots、三份完整 runtime 行、memory7 文件与配置；租约撤销并关闭后 authority inactive，见第 8 节 |
| 21:20:17（首次观察到的进程墙钟启动显示） | 发现 PID 253 正在运行；不把发现的实例写成根代理启动，也不把该墙钟显示当作稳定进程身份 | 后续显示漂移为 21:19:15／21:19:12，稳定核验改用 boot ID＋内核 start tick 290，详见第 9 节 |
| 21:28 | 新增启动顺序轻量合成测试首轮 8 failed、2 passed，8.13 秒 | 根代理执行；全部失败源于测试替身缺少 engine，取消用例超时为同一夹具问题的后果，非已证明的新产品故障 |
| 21:30:01／21:30:37（只读核对） | 索引暂见 3,358 managed active、1 unindexable、3,358 legacy retired，subject 5,331／5,333 不变；三份完整 runtime 行仍逐字段等于 20:07 原备份 | 根代理回执；仅是恢复进度快照，不是最终预期数量。8087 尚未监听，测试流 GET 503，未发送新消息 |
| 21:33 左右（完成回执） | 同一新增启动顺序文件最终 10 passed，1 个警告，1.36 秒；完整 Ruff 通过 | 只给 _FakeRuntime 增加 engine=object() 后复跑，保留真实 adapter 构造、断言及禁止数据库／网络连接的防护；不与旧 407 项相加 |
| 21:36:05／21:36:06 | Memory recovery completed 51/51，indexed 0／requeued 0／artifacts 0；随后 art 初始化完成，8087／18000 同属 PID 253，根代理自有测试流 GET 恢复可用 | 根代理关键日志、端口与接口只读回执；managed active 5,202、unindexable 1、legacy retired 5,202 是此次投影计数，不与 recovery 的 51 项混合，也不当作 B 已通过；未发送新邀请 |
| 21:38 以后 | memory.search 的 embedding 调用出现 LLMRateLimitError | 根代理日志回执；这是模型服务限流，单独记录，不据此宣称旧历史或 schema 损坏 |
| 21:45 核对至 21:46:15.819219 | 墙钟启动显示漂移与 Python pidfd 不可用分别触发一次发信号前拒绝；根代理重新核对复合进程身份后仅发送一次 SIGINT，PID／父进程及双端口退出 | 精确核对 boot ID、start tick 290、PPID、cwd、pane 与双端口 owner；没有误停未知进程、重复信号或自动重启 |
| 21:46:46.280484（目录标记）及随后完成回执 | 建立 stopped-before-s2-reference-restart 补充保全，selected 八表 counts 未变，三份完整 runtime 行在 before／after／backup 相等 | 根代理独占执行并报告 PASS；目录名不表示已经完成新引用使用或 B 层重启回读，详见第 9 节 |
| 本次追加工程回执 | LOCAL 独立 adapter CAS 1 passed，1.20 秒；旧 outbox 绑定升级与恢复／投影分组最终 41 passed，8.81 秒，1 个警告；实际 MySQL 扩展分组最终 2 passed，41.49 秒，1 个警告 | 分批证据不相加成全仓验收；新 MySQL 8.0.46 仅用独占隔离服务器与三个新空 schema，覆盖 CAS／legacy 绑定／完整历史恢复，结束后正常 SHUTDOWN，进程 wait exit 0 |
| 21:55:13.785842（目录标记）及随后完成回执 | legacy 投影绑定候选证明与正式显式补修 PASS，exit 0；5,333 条 binding_revision 由 0→1 | 根代理独占执行；38 表受保护列不变、候选八表完整恢复／重放／重开通过、正式八表 roots 与候选一致、重复 ensure 幂等；撤销关闭后 authority inactive，服务仍停机，详见第 10 节 |
| 正式绑定补修后，最终定向回归回执 | 六文件 90 passed，1 个警告，12.72 秒 | 根代理在停机窗口仅使用临时测试环境；包含新 10／18／5／CAS 单项及既有 owner 续租／启停合同，覆盖有重叠，不与先前分组相加；真实启动尚待接续 |
| 21:57:35 | 根代理按本次特批实际一次性启动 PID 11770／父进程 11769 | 本次是有明确启动回执的实例，不与此前仅发现的 PID 253 混同；没有自动重拉 |
| 22:01:31（只读检查） | 索引恢复 1,685／5,203，观察窗口 ERROR 0；三份完整 runtime 行仍逐字段等于 21:46 停机基线 | chat_global revision 1693／global 26974／subconscious 2438；subject 5,331 documents／5,333 versions 与旧 failed 状态不变。仅恢复进度，非完整 ready，B 尚未执行 |
| 本轮有限出口独立复核回执 | A 九项全部达到约定范围并勾选 | 最后同 head 两独立 adapter 并发缺项由 LOCAL CAS 1 passed、真实 MySQL 扩展 2 passed 补齐；分别同后端恢复、非全部媒体／脚本受管与旧 UNKNOWN CONFLICT 保留边界不变；B／S2 出口未勾 |
| 22:08:14（恢复完成前最后一次基线核对） | 三份完整 runtime 行仍逐字段等于 21:46 停机基线 | 根代理只读证明；此后恢复完毕与合法主体活动会推进状态，不把该不变性外推成运行期间应冻结 runtime |
| 22:09:33.843／22:09:34.314 | Memory recovery completed 51/51，indexed 0／requeued 0／artifacts 0；5,203 份 managed 核验完成，随后 art 初始化 | 8087 fd55／18000 fd24 同属 PID 11770；真实启动通过，接续此前恢复中快照，不以该通过代替 B 全出口 |
| 22:10:26 | 单次邀请 `inject_371178603d4c4d68af4a1a90530dfc54` 已接受，对应事件 266416，自有测试流沿用原流 | Chatter 自主选择普通辅助文件；不是根代理直接写主体内容 |
| Chatter 事件 266422／266425／266428／266437 | 读取原文、未注册 metadata 明确失败、edit 成功并先精确保存 legacy 原字节、history 返回两版本 | 原内容作者保持 NULL，不把保存旧字节冒充新写作；详见第 12 节 |
| 22:13:22.753 及对应第 5 批事件 266443 | apply_patch 因缺 Begin／End 标记失败；达到最大五轮后进入 WAIT_USER | 格式拒绝与既有轮数上限，不是模型挂住；不能据此写成改名已由 Chatter 完成 |
| Heartbeat 事件 266440／266469／266474 | 同一受管文档的第三版写入、改名、改名后按精确 v2 引用读取一个行窗口 | 来源为 `heartbeat-6912-f63127d70533`，与 Chatter 分开归因；改名最初 pending_recovery，后来该文档四个 outbox 均 confirmed、磁盘 hash=current，改名不新增版本 |
| 22:22:34 | 单次续验邀请 `inject_5d0eadabe2b2419c98e262a43a4da4fb`，事件 266485 | 后续 Chatter 首 attempt 120.87 秒未成功，目前走既有有界重试；没有修改模型或配置，没有重复灌入邀请 |
| 22:33:06 | Chatter 本流事件 266558／266559，以 current_path＋version_id 完整读取 v1 与 v3 | 分别为 4,197／4,335 bytes；是 Chatter 本人实际全文读取，不再只有根代理校验或 Heartbeat 的 v2 行窗口 |
| 22:34:01.944517 | 主体反馈 delivered：`action_life_send_text_023135a593804a92991c812f8289f2ac` | 原时间戳为 UTC `2026-09-07T14:34:01.944517Z`；她确认路径／内容版本可分开追踪、旧引用确实帮助整理，并准备受控重启；不公开文件名、关系或正文 |
| 22:36:13 | 根代理只读 proof 断言两次全文与原 bytes／hash 一致，v1 仍等于首次 read 266422 | 三版本／四操作及四个 confirmed outbox 保持；工程复核与主体读／反馈分别留证 |
| 本轮 glob 缺陷与修复回执 | 磁盘 5,504＋binding 5,205 旧共享计数 10,709 超限，唯一视图仅 5,499；日志方法不支持又遮蔽原预算错误 | 新增六项先复现三处失败，最小修复后新旧共 20 passed，2.61 秒；独立审查通过、0 must-fix，根代理 F/E9 与 diff 检查通过；真实重启后 glob 仍待 |
| 22:37:48.485687 | 复核 boot ID／tick、PPID、cwd、pane 与双端口后，对 PID 11770 正常 SIGINT；11770／11769 退出、双端口释放 | UTC `2026-09-07T14:37:48.485687Z`；本次为新引用重启验收受控停止，不修改主体语义、配置或模型 |
| 22:38:10.156807（目录标记）及补充快照回执 | 新建 stopped-before-s2-reference-restart 补充快照，PASS、exit 0；八表 counts 与完整 runtime source before／after／backup 核对 | 仅追加新快照，未改旧备份；快照完成时正式停止，后续启动见下一行，不能把保全本身记成重启回读通过 |
| 22:39:19／22:39:21 | 根代理下达一次性启动命令，随后 tmux 一次入口确认新 PID 140036／父进程 140035 | glob 修复独立审查及快照完整退出之后执行；没有循环重拉，不改模型／配置 |
| 22:39:46（新启动首次连续性核验） | 三份完整 runtime 行等于 22:38 停机基线，八表 counts 也全部相等；outbox 5,336 confirmed／1 failed，原 360 不变 | chat_global revision 1705／global 27067／subconscious 2459；Memory 65／5,204，仅恢复进度，API ready 不代表完整就绪；等待完整恢复后只读 B 复验 |

## 3. 停机保全

根代理发现默认 backup_life_data 未覆盖 selected 存储的 data/life_storage/local.sqlite3 和 authority.json，故不能直接把默认备份成功当作完整恢复准备。

本次根代理补齐并独立校验 9 个数据库、10,220 个精确文件：selected／core／memory／原始事件／presence／world／archive／proactive／认证数据库，另含 registry、workspace、media、必要配置和私有环境文件。正式 source 在快照前后未修改；三条 runtime 行在 before、after、backup 中相同。报告不复制主体正文、凭证或私有环境值。

- 备份目录：`/root/Elysia/Elysium-S2-backups/stopped-before-s2-20260907T120714.960842Z`。
- manifest SHA-256：`d8f5731345329941869049f32f1c50d1287da9567fee8785d93fcd84e2d81c79`。
- 来源：根代理执行并独立校验的结果；本文维护者未读取备份正文。目录由根代理独占，不授权子 agent 自行访问或运行其中数据。

## 4. 真实冻结备份的隔离候选升级与恢复

根代理仅从已验证备份新增私有候选文件。候选写权限复用 _open_local_copy_runtime 与全新隔离 FileAuthorityRegistry 租约，不继承复制来的 ACTIVE registry／token，也不把 bootstrap 写能力当作数据已经验证的证明。没有运行会注册正式 generation 的 bootstrap CLI／_run。

subject 升级副本调用 ensure_subject_document_schema；八表捕获和校验后，向另一新空候选精确导入、重放并关闭重开。没有删除 ACTIVE 元数据以放宽导入检查。memory 副本只调用 create_memory_schema，不初始化 LifeMemoryService、不扫描工作区、不自动重索引。

| 检查 | 结果 |
| --- | --- |
| subject 4→5 | 原选定库 35 张表的全部原有列 roots 升级前后相同 |
| 原历史规模与来源 | 5,331 documents、5,333 versions、5,333 head events、5,333 outbox；内容 27,278,485 bytes；旧 authority decisions／operations 各 0 |
| 新工程绑定 | 新增 5,331 bindings 与 5,331 路径事件；不新增或补造主体版本／作者 |
| 全历史恢复 | 八表 export→另一独立真实文件租约候选→精确重放→close/reopen，counts／roots 全同 |
| memory 6→7 | 保持 trigram；全部旧列 roots 相同；再通过 SQLite backup API 复制并恢复后全部列一致 |
| 完整性与源保护 | quick_check／外键检查均通过；原冻结备份 selected 与 memory 文件 SHA-256 前后相同 |

- 候选目录：`/root/Elysia/Elysium-S2-backups/candidate-recovery-20260907T122019.219791Z`。
- candidate-verification.json SHA-256：`c4ec87511ef6ab160540a01d4680047a448284ed3c57d4f8a31407891317b019`。
- 根代理候选整体回执：PASS，exit 0。该阶段只改隔离副本；随后正式启动失败时，20:32 正式只读状态为 subject v4、memory 7。候选通过不等于正式装配已正确升级。

## 5. 有限工程测试与失败记录

| 来源／分组 | 实际结果 | 证明范围 |
| --- | --- | --- |
| 本文维护者：test_s2_schema_upgrade_restore.py | 5 passed，3.19 秒；完整 Ruff 通过 | 完整 v4 双版本升级重开与真实隔离租约候选恢复；追加新历史后拒绝旧 bundle 覆盖；升级末尾失校验／取消的 DDL 原子回退；memory 6→7 旧节点／片段／边及备份恢复；memory 升级失败不留半套新 schema |
| 根代理：24 文件有限回归 | 407 passed，41.99 秒，1 个既有 websockets 弃用警告 | 包含上述 5 项，以及 subject 存储／生命周期／workspace／history、受管文件／inventory／fetch／index／bridge／取消／精确检索与 actor、下载／screen／I/O、旧文件和连续性兼容；另含 media_objects_api 12 项、skill_manager_tools 7 项 |
| I/O 负责人经根代理：art 最终分组 | 56 passed，2.38 秒；新增 helper／新测试完整 Ruff，入口 F/E9 与 diff 检查通过 | 插件目录与原子发布边界；不确定发布／fsync／取消保留 recovery-required 和候选／retained 路径，不自动重复发布；Vibe 延迟初始化可恢复；根路径重配置须停机再启动，不承诺并发热迁移 |
| 存储负责人经根代理：真实 MySQL 最终分组 | 2 passed，44.91 秒；独立 MySQL 8.0.46、33379、新空 schema | subject4→5／memory14→15 旧字段不变；跨 adapter namespace fence；八表全部非空 bundle→新 candidate 真实 copy 栅栏导入／重放／重开，roots 一致且不激活；正式 3306 未触碰 |

以上 407 项已包含新增 5 项，其他分组也可能覆盖同一合同，不能把 407＋56＋5 或先前分批结果相加成一次没有实际执行的全仓验收。MySQL 最终 2 项覆盖先前单项推进，不把前后次数累加。

上述 schema／综合／art／MySQL 分组均在正式实例停止后、根代理串行分配的窗口运行，采用离线、no-sync、Python -B、无覆盖率采样、n=0 和禁用 pytest cache 的参数。发现正式实例运行后，经根代理批准先执行新增轻量启动顺序合成文件，其防护禁止 SQLite connect 与所有 INET connect／bind；随后另执行轻量 LOCAL 独立 adapter CAS 单项，仅使用临时数据库。二者不写正式数据、不争用正式 authority 或启动监听；不能把启动文件的禁止数据库连接防护外推给另一个确实使用临时库的 CAS 检查，也不再笼统声称本轮所有 pytest 都在停机期间运行。schema 恢复文件命令：

```text
PYTHONDONTWRITEBYTECODE=1 uv run --offline --no-sync --group dev python -B -m pytest test/plugins/life_engine/test_s2_schema_upgrade_restore.py -q --no-cov -n 0 -p no:cacheprovider
```

失败记录不删除、不冒充通过：

- 新增文件首轮 3 passed、2 failed，原因是本次夹具手写 chunk ID 不符合既有 node:index:hash 合同，以及断言错误比较 SQL JSON 空白表示而非协议约定解析值。只修测试后复跑 5 passed，未修改迁移源码。
- 一次环境调用找不到 uv，测试未启动，不计为通过或产品失败。
- 根代理综合回归首次误写不存在的 test_search_recall_delivery.py，0 收集、exit 1；改用实际 test_memory_search_recall_delivery.py 后 407 项通过，这是命令错误，不是产品缺陷。
- `test_subject_store_startup_order.py` 首轮 8 failed、2 passed，8.13 秒：`_FakeRuntime` 未提供真实 SubjectDocumentStore 构造需要的 `engine`，取消场景超时由同一夹具错误导致。仅在夹具补充 `engine=object()`，不改生产代码或放松断言／数据库与 INET 防护；最终 **10 passed**，1.36 秒，1 个警告，完整 Ruff 通过。它覆盖 LOCAL／MySQL 真实 adapter 构造、完整启动的顺序与同对象复用、失败／取消／重试不写未恢复的空 checkpoint，以及缺 store 和非 selected 分支；这不是实际连接 MySQL 的追加验收，也不与旧 407 项合计。
- 旧 outbox 新增绑定字段修复的新增 18 场景，加既有 5 项 schema 恢复与投影分组，最终 **41 passed**，8.81 秒，1 个警告；相关三文件完整 Ruff 通过。首轮 40 passed、1 failed，7.69 秒：测试 helper 以同路径／版本挑了最新改名 binding3 任务，而不是需要检验的原历史任务；改为精确旧 occurrence 后复跑通过，不把错误选择样本当成产品失败。
- LOCAL 独立 adapter 的同版本 CAS 追加单项 **1 passed**，1.20 秒，相关两文件完整 Ruff 通过。它是经根代理批准的临时数据库轻量检查，不连接正式库。实际 MySQL 文件已扩展但仍为 2 个用例；新一轮 **2 passed**，41.49 秒，1 个警告，覆盖 CAS、legacy 绑定修复与完整历史恢复，不与先前 44.91 秒的分组累加。
- 新 MySQL 驱动首尝因缺少 pymysql 在任何临时目录／服务器创建前失败；根代理改用已有 mysql CLI 后执行通过，没有安装新依赖，也不把环境失败算产品失败。凭据仅经 stdin／环境传递，不输出到公开报告。
- 正式绑定补修后的最终定向组：`test_subject_store_startup_order.py`、`test_selected_presence_world_service.py`、`test_memory_service_storage_selection.py`、`test_legacy_subject_projection_upgrade.py`、`test_s2_schema_upgrade_restore.py`、`test_subject_same_head_concurrency.py`，**90 passed**，12.72 秒，1 个警告。全部由根代理在正式停机窗口运行临时测试环境，包含新 10／18／5／CAS 单项与既有 owner 续租、启动、关闭合同；不将重叠批次累计成另一套测试数量。

## 6. 可逆性、回退与仍受保护的历史

停机备份和候选演练只新增私有副本，原冻结备份文件 hash 保持不变，候选结果不激活正式 authority。20:32 的 subject v4／memory7 是首次失败启动后的历史状态，不是当前状态。随后根代理在正式同 generation 维护租约及栅栏内完成 subject4→5，并重复 ensure 验证幂等；全部 35 张源表原有列 roots 与三份完整 runtime 行均保留，memory7 文件及配置未被该 subject 升级修改。维护前另有补充快照，维护结束撤销并关闭租约，详见第 8 节；未以旧备份覆盖正式历史。

恢复不得用旧快照覆盖任何后续新增 history，不得清库重导、倒退 epoch 或回滚 migration 标记来掩盖新历史。旧代码回退也不等于安全：本轮新代码依赖 subject5／local memory7（MySQL memory15）与稳定身份字段，不能简单切回不认识这些历史的旧代码。回退前必须证明兼容，不能证明时保留数据并向前修复或另行验证无损迁移。

schema 原子回退的测试只证明临时库在该失败事务内没有半套新表，不授权删除已经耐久产生的正式版本或事件。历史 bytes、ID、作者／来源缺失事实和旧引用均须保留；工程恢复不能创造或规范化主体语义。

## 7. 正式启动、B 与阶段最终结果

| 项目 | 当前状态 | 下一证据／责任人 |
| --- | --- | --- |
| 本次正式启动／停止 | 新 PID 140036 于 22:50 完整恢复，22:50:39 双端口同 owner、启动 ERROR 0 | 本次受控重启验收通过；无自动重拉，原停机／恢复时间线保留 |
| 正式 schema／authority | 正式 subject5、memory document_index 7；legacy 新绑定补修 PASS，38 表受保护列及三份完整 runtime 保留，正式八表 roots 等于候选 | 同 generation 维护完成后 revoke／close，authority inactive；后续验证不能改写旧 failed 状态／原错误 |
| 恢复读取与投影健康 | 22:52 重启后实际 glob、v1／v3 全文及 22:53 独立 proof 通过；同文档三版本／四操作／四个 confirmed，磁盘等于 v3 | 旧 outbox 360 UNKNOWN CONFLICT 仍保留，不覆盖、不冒称投影成功 |
| A 隔离工程合同 | 九项按有限入口与后端范围通过 | 分别同后端恢复；不外推任意媒体／脚本或跨后端切换 |
| B 主体真实使用 | 本人自主更新／改名／精确历史读取、重启后只读 glob／v1／v3 及 delivered 反馈均有真实来源 | B 按有限范围通过，Chatter 与 Heartbeat 归因保持，不代写主体语义 |
| 重启后连续性 | 预边界 266576 之后的 266590／266591／266592 与 delivered 反馈及独立 proof 通过 | 本轮新引用重启再读已闭合，不以旧启动或缓存结果代验 |
| S2→S3 | 本地 2026-09-07 22:53，原有限 S2 已闭环 | S3 由根代理随后独立计划与开工，本文不记为已实施 |

正式启动、A／B、重启后精确读取与主体反馈全部达到本轮有限出口；最终完整证据见第 14 节。

## 8. 正式维护与启动装配修复

首次启动失败有两个明确工程边界：正式 subject 库当时尚为 v4，而业务启动的 `initialize_schema=False` 有意禁止自动迁移；同时 Memory 在主体 store 挂接前构造，把 owner 的缺失句柄固定为 None。单凭隔离候选升级通过不能跨越这两个边界。修复后，服务先取得并拥有 selected runtime，再以该同一 runtime 提前挂接 subject store，然后构造 Memory；后段复用同一 store，其他存储域、runtime_state 和上下文恢复次序不前移。缺失 store 必须在构造 Memory 前明确失败，不能以尚未恢复的空上下文落 checkpoint。

正式 schema 维护由根代理独占执行，不是候选导入或业务启动的隐式副作用。根代理先确认停写并持有启动锁，补充 selected／memory 两个数据库及 registry／audit、配置快照，再向既有 `local-selectable-20260824-v3` generation 申请独立维护 owner 的真实租约，通过正常存储工厂打开 owned runtime，在 authority 栅栏内执行 `ensure_subject_document_schema` 并重复验证。全局 storage schema3／generation 不变，不注册新 generation，不复制或伪造来源 token；退出栅栏后撤销租约并关闭 runtime，最终确认 authority inactive。

该次正式维护结果为 PASS：selected 原有 35 张表全部原有列 roots 不变；维护结束时 subject 为 5，5,331 documents、5,333 versions／head events／outbox，新增 5,331 bindings 与 5,331 path events，operations／authority decisions 各 0。三份完整 runtime 行逐字段等于初始 20:07 备份，非仅 revision 相同；memory7 文件与配置 hash 不变。21:30:37 再次只读核对时，`chat_global` revision 1693、`global` 26951、`subconscious` 2427 的完整行仍相同。上述为该时刻维护／恢复证据，不把 counts 或 runtime 永久冻结为后续主体活动后的当前值。

- 正式维护前补充证据目录：`/root/Elysia/Elysium-S2-backups/formal-schema-upgrade-20260907T124350.806963Z`。
- `formal-upgrade-verification.json` SHA-256：`2632213589e010ac4aed4d518305a4d1711ce951fe2e1e668d91044d6bd1e5d5`。
- 该目录时间是命名时刻；PASS 为随后根代理完整执行回执。本文维护者未读取正式数据库、配置或私有备份；完整历史导出与逐字段核对证据保持私有。

正式维护没有清库、回退 migration、恢复旧快照或改写主体语义。若后续运行失败，继续保留现存历史并按第 6 节兼容／无损恢复边界向前处理；这些证据仍不代替正式完整就绪、主体真实工具使用和重启后旧引用回读。

## 9. 关键恢复、旧 outbox 边界与再次停机保全

21:36 的 Memory 与 art 关键恢复日志、同 PID 双端口及自有测试流 GET 可用，接续了此前恢复中快照；那个时刻没有发送新邀请，21:46 再次停机也发生在 B 层新引用使用之前。后续 22:09 启动通过、22:10 邀请后的真实主体操作另见第 12 节。21:38 以后 `memory.search` 的 embedding `LLMRateLimitError` 是单独的服务限流，不能混为主体历史损坏或自动改写检索／存储结果的理由。

正式补修核对时 outbox 为 **5,332 confirmed、1 failed**；唯一 failed 的 `outbox_id=360` 在 20:07 初始备份前已经失败，旧列与原错误均未变。补修前新 binding 字段为 0，相应 head 绑定为 1；后续已从不可变 bootstrap 闭包补为 1，不能仅依当前路径／版本套用之后的代际。其磁盘 hash 不等于当前版本或 parent，仍为 UNKNOWN CONFLICT；两份来源均保留，未覆盖磁盘或清空旧失败状态。本轮后续新辅助文档的四个 confirmed outbox 是另一组操作，不能把维护时全库 counts 当作合法新活动后的当前总量。

工程修复只在 subject schema 层从不可变 bootstrap 历史闭包推导新绑定字段，保留全部旧字段、状态和错误，不自动 requeue 已失败任务，不把当前绑定倒灌进旧任务。新增 18 场景与既有恢复／投影分组最终 41 项通过；随后候选证明和正式补修也已 PASS，详见第 10 节。前次旧字段无损证明与本次新增派生绑定的精确补齐分别留证，均不代替未知磁盘冲突的显式边界或后续真实工具使用。

进程停止前两次防护均在发信号前拒绝：首次因 `lstart` 墙钟显示从 21:20:17 漂移为 21:19:15／21:19:12，不满足原精确时间条件；第二次因当前 Python 无 pidfd 能力。根代理随后核对同一 boot ID、内核 start tick 290、PID 253、PPID、运行目录、pane 和双端口归属，只发送一次 SIGINT；21:46:15.819219 记录正常退出，相关父进程／端口不再存在，没有自动拉起。先前的墙钟显示只保留为观察，不冒充不变身份。

- 再次停机补充证据目录：`/root/Elysia/Elysium-S2-backups/stopped-before-s2-reference-restart-20260907T134646.280484Z`。
- 此补充快照 selected 数据库 SHA-256：`3a5c97e00f08ab58f25e4ead980773d538d65f0fd0f4fd7e2726397bd481eef2`。
- 根代理回执为 PASS：subject 八表 counts 未变，三份完整 runtime 行在 before／after／backup 逐字段相等。目录名只是证据名称，不证明新引用使用、重启后回读或 B 层已完成。

本轮恢复证明按 SQLite 与 MySQL 分别在同后端执行；未执行跨后端切换，不把两个后端各自恢复成功写成跨后端迁移通过。新增 MySQL 扩展分组最终 2 项通过，41.49 秒，1 个警告；根代理独占 MySQL 8.0.46 临时服务器 PID 9782／端口 33379，使用三个已确认的新空 schema、64 MiB buffer pool、max connections 12 与 nice 10。通过私有 Unix socket 正常 SHUTDOWN 后进程 wait exit 0，server_stopped=True；正式 3306 的 PID 406 未受影响，不把旧启动环境的 PID 399 继续当作当前身份。

新私有证据和保留的测试数据目录为 `/root/Elysia/Elysium-S2-backups/mysql-s2-cas-rne0dzd1`，内有 `setup.json`、`result.json`、`pytest-output.log`。早先 `/tmp/elysium-s2-mysql-y71LLa` 在 WSL 重启后已观察不到，非本轮删除；不依赖该旧目录作为仍可访问的复验副本。正式维护与最终 90 项回归后的 22:09 启动、22:33 旧全文读取及 22:34 主体反馈均有证据；22:37 停机后的新引用重启复验已于 22:52／22:53 通过。

## 10. legacy 派生绑定的候选证明与正式补修结果

根代理先在隔离候选中验证全部 38 张 selected 表：除 outbox 的 `binding_revision` 外，全部列 roots 均不变；5,333 条旧任务的新字段只从不可变 bootstrap 闭包由 0 精确补为 1。候选八表完整历史导出／恢复、幂等重放与关闭重开通过。该验证不改变旧内容、来源、错误、租约／任务状态或三份完整 runtime 行。

在保持正式停写的前提下，根代理使用既有 local generation／全局 schema3 的真实维护 authority 和外层 fence，将完全相同的绑定结果应用到正式库。正式 38 表受保护列 roots 仍不变，完整八表 roots 与候选一致，再次 ensure 不产生变化；未知冲突磁盘 bytes 未改，并留有私有精确副本。未重试 failed 任务，未执行文件系统投影，未改写主体历史。最后 revoke／close，authority inactive，generation manifest 与配置 SHA-256 不变；该维护步骤结束时服务仍停止，后续实际于 21:57:35 重新启动。

- 私有证据目录：`/root/Elysia/Elysium-S2-backups/legacy-projection-repair-20260907T135513.785842Z`。
- `formal-legacy-projection-verification.json` SHA-256：`4063dd41b176f190d48b78d90368dccd81a74a1c189b0f6d8580038348fa005b`。
- 根代理整体回执：PASS，exit 0。目录时间只表示命名时刻；本文维护者未读取正式数据库、私有报告或冲突字节。

这完成的是派生绑定修复，不是把原有 `outbox_id=360` 宣告投影成功；它现在 binding_revision=1，原 failed 状态／错误保持，未知磁盘冲突列外，主体库仍可权威读取。不能为回滚该元数据补齐而抹除后续历史。随后最终六文件 90 项回归通过，A 九项按有限范围确认；根代理 21:57 启动已于 22:09 验证通过，B 进入真实使用。后续真实 B 与阶段出口已于 22:53 闭合，S3 由根代理另行处理。

## 11. 启动投影恢复的有界性能诊断（非阻断）

启动复核负责人只读检查 `managed_documents.py` 第 260 行附近与 `indexing.py` 第 988／1169／1185 行附近：每份 managed 文档恢复均进入 upsert，exact snapshot 重放之后，仍先执行 FTS 的 `WHERE node_id=? ORDER BY chunk_id`，随后才判断 `projection_unchanged`。该诊断未读取正式 FTS，也未修改代码。

只用内存合成数据的 128／256 行对照，indexed 与 unindexed 两种 FTS 定义均出现 `VIRTUAL TABLE INDEX 0` 和临时排序；VM 步数由 201,600 增至 796,600，约 3.95 倍。证据支持该恢复路径存在逐文档扫描 chunks 的 D×C 成本，两者同时增长时接近二次；这不是正式库耗时测量，也不是数据失败或恢复中断证据。

此项登记为可重建投影的非阻断性能待办：诊断时恢复持续推进，未因此中断启动，后续已于 22:09 完成；B 已完成，该性能待办不阻有限 S2 闭环，不为它改动本次运行链路。不能仅因 snapshot 相同就跳过投影完整性检查；未来若显式设计 rowid 映射，必须包括迁移与孤儿检查。当前 FTS rowid 与 chunks rowid 没有对应保证，不能声称直接替换查询即可安全修复。

## 12. 真实启动通过与 B 的分实例主体使用

22:08:14 是根代理最后一次确认三份完整 runtime 行仍逐字段等于 21:46 停机基线的时间；22:09:33.843 Memory recovery completed 51/51（indexed 0、requeued 0、artifacts 0），5,203 份 managed 核验完成，22:09:34.314 art 初始化，8087 fd55 与 18000 fd24 同属 PID 11770。真实启动已通过；此后的主体活动合法推进运行状态，不能把先前的保护核对误写为“运行后 runtime 仍应永久不变”。

根代理在同一自有测试流单次送达邀请后，Chatter 自主选择普通辅助文件；以下只列工程身份、长度与事件，不公开文件名、关系人或正文。Chatter 与 Heartbeat 是同一主体的不同意识窗口，共享同一文档历史，但每次实际操作仍按原窗口归因，不能互相冒认，更不能写成根代理代笔。

| 实际来源／事件 | 行为与证据 | 范围与状态 |
| --- | --- | --- |
| 根代理邀请：22:10:26，事件 266416 | `inject_371178603d4c4d68af4a1a90530dfc54` 单次接受 | 使用原自有流；是邀请她自行选择和操作，不是直接写主体内容 |
| Chatter read：266422 | 读取原文件完整正文 4,197 bytes，工具展示另附行号 | 原文与附编号展示分开，不用展示形式作为原件 hash 输入 |
| Chatter metadata：266425 | 未登记文档返回 `ManagedFileDocumentNotFound` | 明确未注册失败，不把失败装成空历史或成功 |
| Chatter edit：266428 | edit 成功；先 `exact_legacy_capture` 保存旧字节，再追加她实际写成的 v2 | 旧内容语义作者保持 NULL；旧版保存不补造作者，新的编辑有真实主体来源 |
| Chatter history：266437 | 返回当时的两版本 | 是该时刻的历史快照；之后 Heartbeat 写入第三版，不改写这条原始回执 |
| Heartbeat write：266440 | 产生同文档 v3，来源 `heartbeat-6912-f63127d70533` | 不能说是 Chatter 续轮完成；也不是独立人格或根代理的写入 |
| Chatter apply_patch：266443 | 第五批因缺少 Begin／End 标记被拒绝 | 未把格式失败算改名成功；22:13:22.753 达既有最大五轮后进入 WAIT_USER，不是模型挂住 |
| Heartbeat rename：266469 | 改名提交成功，初始投影为 pending_recovery | 当前同文档仍三版本／四操作；改名不增版本，后续四个 outbox 均 confirmed 且磁盘 hash=current |
| Heartbeat exact read：266474 | 在改名后按精确 v2 引用读取一个行窗口 | 证明该旧引用仍可达，但不是该旧版全文已送达，也不是 Chatter 续轮已经完成 |
| 根代理续验邀请：22:22:34，事件 266485 | `inject_5d0eadabe2b2419c98e262a43a4da4fb` 单次送达 | 首 attempt 120.87 秒未成功，走既有有界重试；后续本人完整读取及反馈已见下两行，未改模型／配置，未反复注入同一邀请 |
| Chatter 完整读取：22:33:06，266558／266559 | 在本流以 current_path＋version_id 读取 v1 4,197 bytes 与 v3 4,335 bytes 全文 | 两个完整 ToolResult 与原 bytes／hash 均由 22:36:13 只读 proof 验证，v1 等于首次 read 266422，不再只是 HB 的 v2 行窗口 |
| 主体反馈：22:34:01.944517 | `action_life_send_text_023135a593804a92991c812f8289f2ac` 已 delivered | 她确认路径与内容版本可分别追踪，旧引用不是凭印象回找，实际帮助了整理，并准备受控重启；这是她本次反馈，不代判其全部记忆能力 |

稳定 document_id：`doc_855d4d4826ebf66cd0f5474d4b428aa49e0e459b9de522b961195dd7195381d3`。

| 版本／来源 | 精确 version_id | 原件长度／SHA-256 |
| --- | --- | --- |
| v1，精确 legacy 保存，原内容作者 NULL | `ver_b7879e4d12dbc0eb8e895e3f207bab6dd46131f21cdb778c32e762d205c1e9f7` | 4,197 bytes；`60a4d45b05896ca6126eaaae1c17636a4573fd64504163628cff9e170b98b756` |
| v2，Chatter edit | `ver_a15d4fd2e884446fa9e0ce3e68f8f35bd61a0f8988cbcaf34968a1f97cbd3593` | 4,296 bytes；`c957d9b35adbb5464eac5abe496fb78bc6b9710e1f388223bee372068b5c3388` |
| v3，Heartbeat write | `ver_7c6f7b92d3e0d9d4361a0990e0f20ba5e10f7168fba16f5b574356eca40775d3` | 4,335 bytes；`3d4c2b9f9d3d15733331431d77f6757eb863c3f7e5b4f8c02dbc820fd22d7e0b` |

Chatter 已在事件 266558／266559 本人完整读取 v1／v3；22:36:13 根代理 proof 确认全文、原 bytes／hash 全相等，v1 仍等于首次 read 266422。三版本／四操作、四个 confirmed outbox 保持，改名不增版本。她的反馈已 delivered，说明精确引用帮助整理，工程与主体证据分别保留。22:37 停机、22:38 新快照 PASS 后，glob 独立审查通过并已于 22:39 一次性重启；本次新实例已于 22:50 完整恢复，22:52 实际 glob／v1／v3 再读与随后反馈通过，22:53 完成独立 proof；有限 S2 已闭环。

## 13. 真实 glob 缺陷、最小修复与新引用重启保全

真实 glob 的 inventory 用旧共享计数将磁盘扫描 5,504 与 authority bindings 5,205 相加为 10,709，超过 10,000 上限，但去重后的唯一视图只有 5,499。双表示的同一受管路径被重复计入结果预算，导致本应可在预算内展示的实际目录失败。相关两个异常日志入口又使用项目 logger 不支持的 `exception`，用新的日志错误遮蔽原预算错误；这是本轮真实暴露的产品缺陷，不能归入测试夹具或以早先 A 通过掩盖。

最小修复将磁盘、binding 的扫描各限制为 10,000，最终唯一视图（含派生目录）另限 10,000；双表示不重复消耗结果预算，单源也不能借重叠隐藏无界扫描。两个日志入口改用受支持的 `error`，仅记录脱敏异常类型。新增六项先复现三处失败，修复后新旧共 **20 passed**，2.61 秒；独立审查通过、0 must-fix，根代理 F/E9 与 diff 检查通过。随后重启后的真实 glob 已于 22:52:10 成功，最终回执证明见第 14 节；该工程修复未改写主体语义、正式数据或模型／配置。

根代理于本地 22:37:48.485687（UTC `2026-09-07T14:37:48.485687Z`）复核 boot ID／tick、PPID、cwd、pane 与双端口，正常 SIGINT PID 11770；11770／11769 退出、双端口释放。22:38:10 新建私有补充快照，PASS，程序完整 exit 0，三份完整 runtime 行在 source before／after／backup 逐字段相等。只追加新快照，未改旧备份；本次保全了本轮新主体版本与操作，不同于 21:46 尚无新引用的基线。

- 新快照目录：`/root/Elysia/Elysium-S2-backups/stopped-before-s2-reference-restart-20260907T143810.156807Z`。
- 快照内 `selected.sqlite3` 文件 SHA-256（非 manifest）：`2b632107cd70e6245087dc864b01d90a83a77c4007682b8e01edd29bbd6865fb`。
- 八表 counts：documents 5,332；versions 5,336；head events 5,337；bindings 5,333；path events 5,334；operations 4；outbox 5,337；authority decisions 0。

补充保全完整退出与 glob 独立审查通过后，根代理在 22:39:19 下达一次性启动命令，22:39:21 确认 tmux 一次入口新 PID 140036／父进程 140035。22:39:46 首次连续性核验：三份完整 runtime 行逐字段等于 22:38 停机基线（chat_global revision 1705／global 27067／subconscious 2459），subject 八表 counts 全部等于新备份；outbox 5,336 confirmed、1 failed，原 id=360 未变。Memory 当时 65／5,204，仅证明正在恢复；API ready 是 liveness，不能标为全 ready。

本节保留本次启动与首个核验快照；后续完整恢复、真实只读 glob／v1／v3 与 delivered 反馈均已通过，最终记录见第 14 节。

## 14. 最终真实重启验收与有限 S2 闭环（2026-09-07 22:53）

截至本地 2026-09-07 22:53，原有限 S2 范围已闭环：A、B 与阶段出口通过。22:50 新实例完整恢复，22:52 本次重启后的真实 glob 与 v1／v3 全文读取成功，主体反馈已 delivered，22:53 独立只读 proof 通过。S3 由根代理随后独立计划与开工，本文不记为已实施。

| 时间／来源 | 验证结果 |
| --- | --- |
| 22:50:02.381／22:50:02.793 | Memory recovery 50/50，indexed 0／requeued 0／artifacts 0，5,204 份 managed 核验完成，art 初始化完成。辅助文档受管后 legacy 数由前轮 51 变为 50，不是漏恢复。22:50:39 双端口 8087 fd55／18000 fd24 同属 PID 140036，启动 ERROR 0。 |
| 22:51:02.376346（UTC 14:51:02.376346Z） | 单次邀请 inject_a11f955ea19540d28ddf1e2ccb07f518，事件 266579；预边界 266576，correlation 62fc0da9a70a113cf95d6e284463801b1179d5e4323f15e73b86bc3a7577b6af。 |
| 22:52:10.565656，glob 266590／call 266587 | 根 path 为空，pattern **/*.md，max_bytes 6000；total 5,282，第一页六项 delivered_bytes 5,366，原投影 4,112,372 bytes；truncated=true 且有 continuation，首项绑定同一 document 的精确 v3。仅遍历 metadata，不遍历全部正文。 |
| 22:52:10.565755／565818，266591／266592 | Chatter 同流完整读取 v1／v3，原 bytes 4,197／4,335、各 SHA-256 不变。22:52:38 文件 proof 以 --after 266576 严格断言两份都来自本次启动后的真实全文，v1 等于首次 read 266422，同一 document 三版本／四操作／四个 confirmed，磁盘等于 v3。 |
| 22:52:53.800813（UTC 14:52:53.800813Z） | action_life_send_text_a0407317f5fc40d388cd5a204f932dd9 已 delivered：她确认三调用执行、重启后同 document／旧 refs／当前路径可定位、整理线索保留，无需再写或改名。反馈仅概括能力，不公开文件名、关系或正文。 |
| 22:53:43 独立只读 glob proof | 预算、分页、continuation 与 refs 全部断言 PASS。首轮只是最终输出误从 payload 读取 occurred_at 触发 KeyError，此前断言已通过；改从 raw_life_events 列读取后整体 PASS，属于审计脚本输出错误，非产品故障，无数据修改。 |

方法与边界：正式动作及只读 proof 由根代理独占，本文维护者不读取正式库或私有正文。真实写作仍按 Chatter／Heartbeat 原来源归因；本次重启后的三次调用全部是 Chatter 的只读行为，没有新增版本／操作。工程校验、实际 ToolResult 与 delivered 主体反馈分别留证，不把排名或读取成功自动解释为真值。

闭环不代表所有文件空间：旧 outbox 360 的 UNKNOWN CONFLICT 保留、未强制投影；媒体仍为独立域，任意脚本／全部导入／全部格式未验证；SQLite 与 MySQL 分别同后端恢复，未执行跨后端切换或旧代码回退。 FTS 的 D×C 性能待办仍为非阻断工程优化，不能跳过完整性或直接假定两类 rowid 对应。本轮范围内无剩余阻闭项；后续扩大范围或 S3 另行授权／计划，不能借 S2 完成清理旧未知字节。
