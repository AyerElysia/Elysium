# 主体自主选择聊天窗口验收

记录日期：2026-09-08，北京时间。本报告是工程证据，不代写爱莉的主观选择。

## 1. 请求、现状与范围

用户要求检查爱莉是否能自由决定去哪个聊天窗口聊天：存在则实际验证，不存在则先设计再实施。源码和现有架构确认已经有独立入口，无需另造窗口选择器：`nucleus_proactive_query(resource=reachability)` 列出对象及物理表面，`nucleus_proactive_command(action=outreach.begin)` 提交她当下明确选择，随后目标表达过程在自己的上下文中重新决定表达或沉默。普通 `life_send_text` 只向当前表面发送，不能通过旧 target_key 偷换目标。

已有行为分三层核验：源码／schema 与实际加载、临时数据合同、真实主体选择与目标投递。计划以两个新建的受限本地 Ayla 测试窗口为候选，从既有测试会话邀请她自主选择一个，也允许拒绝或沉默；不向真实联系人、群聊、QQ／KOOK 发送验收消息。不创建新主体身份、共享私有滚动上下文、猜昵称、默认安装能力或新增定时任务。测试客户端属于外部协作者，每个凭据只授权一个确定的本地测试 stream，既有测试客户端的授权不扩大。

## 2. 已完成的静态和隔离检查

原目录 `/root/Elysia/Elysium`，HEAD `1a5d01a8`，共享未提交变更保留；已完整复核 AGENTS.md，无本机 COLLABORATION.md。现有运行 PID 894970，04:21:44 双端口独占、ready、selected authority healthy／epoch 329／审计链有效。本次未停机、重启或修改生产配置。

现有入口在 chat、heartbeat、voice_live、livestream manifest 中注册。reachability 以明确 canonical_person_key 合并已确认的跨平台个人；其余账号按平台域隔离，群聊作为独立场所；列表按稳定身份排序，不按最近活跃替她选人。outreach 要求 active actor、精确 audience/surface、稳定来源和不可变决定；surface 与 audience 不匹配拒绝。目标唤醒使用稳定内部信封，绕过消息缓冲并启动目标 stream loop，但不伪装新用户消息，也不预写最终对外正文。已提交而暂未唤醒保留 inbox 待恢复，真实投递、未知、失败及沉默分开结算。

轻量定向运行五个现有测试文件：test_initiative_reachability.py、test_initiative_delivery.py、test_proactive_tools.py、test_proactive_deexcitation.py、test_life_chatter_send_targets.py，`uv run --group dev python -m pytest ... -q --no-cov -n 0 --tb=short --show-capture=no`，结果 54 passed、1 既有 websockets 弃用警告，1.70 秒。只使用临时数据／替身，没有正式库故障注入或第三方消息。它证明测试内的目标身份、权限、去重、恢复和当前表面限制，不等于真实跨窗口外联已完成。

## 3. 真实窗口选择和表达结果

结论：已有能力，且本次专用本地窗口的真实链路通过，不需要重新设计或修改生产实现。功能是由主体选择已登记的对象与聊天表面并发起表达，不是按最近联系人自动发送，也不表示可以绕过平台、对象或实例授权。此次没有尝试把某个窗口的私有滚动上下文复制到另一个窗口。

- 04:23:11–04:23:15，复用现有正常鉴权／消息接收入口，创建两个独立受限外部测试客户端，显示名为 Codex（窗口验证甲）／Codex（窗口验证乙），各自只授权一个 Ayla stream。没有给旧客户端增加授权。两条登记通知实际被处理，甲、乙分别于 04:23:37.359923／04:23:55.503617 主动 pass_and_wait，未在初始化阶段发送消息。
- 04:23:49–50，只读确认两个正常登记的窗口都具有独立 account／surface 引用，不依靠直接写 chat_streams 或 PersonInfo 造路由。
- 04:24:44，原测试会话发送一次邀请（inject_10444ab4e3b54f9ca1938c159787bfb9）。仅给甲／乙的精确 audience 引用，要求她亲自查询 surface，可选任意一个或拒绝；未指定必须选择乙，未给最终消息正文，不允许真实联系人／群聊外联。
- 04:25:14.978，实际两次 nucleus_proactive_query 分别返回两个不同对象的可达表面。04:25:48.406，她亲自选择乙并提交一次 outreach.begin，权威决定 ID 为 `proactive:decision:12ae90b9c97a369ca7a0e1036f59f991afad3a3285ddf6b8e0f9a551a5f608af`，模型／选择／结果位置 267173／267174／267175。公开意向 120 UTF-8 字节，SHA-256 `e546004a6b0eade642556444d1f3dc5772d5570ffd932c8975a78c9bb0786d6e`，没有创建 seed、改期或流程。
- 04:25:48.425，目标 inbox 已耐久。源会话 04:26:15.024414 发出 495 字节反馈，明确当时只确认决定、收件箱与唤醒，message_sent=false，不能提前说目标已发言；随后 pass_and_wait。
- 乙窗口随后真实进入表达轮，模型位置 267183，自己选择 life_send_text 和 pass_and_wait。04:26:35.333698，一条 164 字节问候实际 delivered，消息 ID `action_life_send_text_6a5d40b4e6cd9a9c4b6ab1e18ed19855`，正文 SHA-256 `21b154b801dfacab739426b150980347e474cee7b4b1fe13fc099b6524499ca6`。这段表达不是机械复制 120 字节的公开意向；通过乙客户端原有单窗口读取 API 取得同一条消息。甲没有对应外联或发送。
- 04:26:35.352，Ayla adapter_ack 形成精确不可变投递证明，receipt SHA-256 `d24a45bf246bdc7bfa0bf32361a882f2fcaa31a48f8d91a1f819d453b0a8167e`；04:26:35.381 outreach resolution 为 spoke，stream_turn 为 completed。claim、proof、resolution 均指向 `sg_call_5f7eb52c9e465_0`／claim_epoch 1 和同一个 outbound message ID，不以函数返回 True 或单个格式正确的 hash 冒充送达。

## 4. 独立验证与失败记录

另补两个现有真实临时存储测试：`test_outreach_expression_inbox_claim_and_terminal_survive_restarts`、`test_visible_outreach_outcome_requires_exact_action_claim`，2 passed、1 既有弃用警告，1.57 秒。它们覆盖实际 local runtime／连接关闭重开、pending／processing／未知状态恢复、精确 claim、伪 hash 与缺失 proof 拒绝、临时副本损坏被检出。这里的“重启”是同进程存储运行时重开，不是本次正式操作系统进程重启。加上第 2 节的不同测试，本次共 56 项通过；未在运行中的正式服务旁运行全仓或高负载测试。

04:29:51、04:30:44 两次独立只读核验通过，证据位于 `/root/Elysia/Elysium/runtime/codex_chat_window_probe_20260908/choice-proof.json` 和 `choice-proof-final.json`，权限 0600，不含原文：

1. 校验位置 267158 后至 267189 共 31 条 raw Life Event 的 payload hash；6 次实际模型活动、9 次工具调用全部成功，包括两个登记窗口的等待。未把这些模型活动数当成所有 provider 尝试数，token 不可得。
2. 两个不同 audience 的查询均在选择前成功；由 call ID＋原 stream＋该邀请 occurrence 独立重算唯一 proactive decision ID，与权威事件、actor、source、causation、command hash、主体 statement 原字节一致。
3. 唯一 inbox、claim、delivery proof、resolution 与 completed stream_turn 一致；实际目标是乙且不同于来源窗口，同一个 chat_global 保留正确的 stream 归属，没有新建 persona。
4. 源反馈与目标问候各一条，模型选择、send_requested、delivered 原字节分别完全一致；目标发送结果的 receipt hash 与主动账本一致。未选甲发送数为零，至 04:30:44 两个目标没有后续额外发送，证明仅限该观察窗口。

第一次独立审计脚本最后一步误把 raw_life_events 内 JSON 字段当成独立 stream_id 列，报 `sqlite3.OperationalError`；该失败发生在任何证明文件写出之前，没有修改业务数据或重新发送。改用 json_extract 读取实际字段后复核通过；这是核验脚本错误，不是产品外联失败。

## 5. 清理、交付与不外推的边界

04:30:41.983400／04:30:43.170154，分别撤销且只撤销本次甲／乙客户端凭据，私有状态移除对应 secret／access_token，授权审计和聊天历史保留；没有删除聊天流、原始事件或主体表达。既有 codex-continuity-tester 客户端没有被关闭或扩权。本次未修改生产代码、模型路由或配置，未停止／重启服务，未提交、暂存或推送；仅新增工程报告和工作区测试辅助材料。

本次可确认“她能查看两个候选窗口、自己选一个、经耐久外联唤起那个窗口并形成真实本地 Ayla 投递”。允许拒绝／沉默的工程分支有定向合同，但不把本次选择表达说成实际验证了拒绝。QQ／KOOK 的跨平台身份匹配有隔离测试，本次没有向这些平台的真实联系人发送消息，因而不宣称真实第三方端到端投递通过；也不证明未提示的长期主动聊天习惯。registered reachability 是已有登记路由，不是所有外部客户端在线、所有平台连接健康或任意窗口均有授权的保证。
