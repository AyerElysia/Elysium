# OpenCode 渠道额度快照（2026-09-08 00:22 CST）

只读查询。未改 New API、未重启任何进程、未打印密钥。

渠道：New API `48` / `OpenCode-Go-New-1`，上游 `https://opencode.ai/zen/go`。

## 结论

OpenCode Go **没有**可经 API 读取的美元钱包余额。New API 本地 `channels.balance` 为空。权威用量是上游 `GET /zen/go/v1/usage` 的三个窗口（`percent` = 已用百分比）：

| 窗口 | 已用 | 剩余 | 重置时间（CST） |
|------|------|------|-----------------|
| rolling（约 5 小时） | 0% | 100% | 2026-09-08 05:24 |
| weekly | 3% | 97% | 2026-09-14 08:00 |
| monthly | **87%** | **13%** | 2026-09-19 22:55 |

当前瓶颈是**月度窗口还剩约 13%**。滚动窗口几乎没用。

`/zen/v1/balance`、`/zen/go/v1/balance`、`/zen/go/v1/credits` 均 404。New API `used_quota` 只是该渠道历史内部记账，不能当成上游剩余额度。
