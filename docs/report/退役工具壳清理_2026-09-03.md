# 退役工具壳清理

> 日期：2026-09-03
> 状态：实现类已删除；历史名字按未知工具失败。不启动 Elysium。

## 结论

先前退役只做到“不注册、留 fail-closed 壳”。这些壳没有生产调用者，属于冗余代码。现已删除：

- `LifeEngineWakeDFCTool`（`nucleus_tell_dfc`）
- `LifeThinkAction`（`action-think`）
- `LifeScheduleFollowupMessageAction`（`schedule_followup_message`）
- `LifeEngineManageThoughtStreamTool`（`nucleus_manage_thought_stream`）以及空的 `STREAM_TOOLS`

旧 ThoughtStream 快照仍可通过 `read_legacy_thought_stream_page` 做只读有界诊断，不写 snapshot。`action-think` 的滚动投影剥离、agent denylist、以及 service 层 `schedule_followup_for_stream` / `schedule_autonomy_intent` fail-closed 保留。思考快照改由 `life_send_text` 结构化字段写入。

历史 LLM 再调用这些名字，走通用未知工具失败，不能产生 synthetic wake 或改写旧快照。

## 未改

- `LifeInnerDialogueTool`、`LifeInnerQueryTool`、`LifeRecordInnerMonologueAction` 仍在生产注册。
- `nucleus_download` 仍在 chat manifest 中，但不在本次退役范围。
- 不启动、不重启 Elysium。
