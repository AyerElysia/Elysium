# 2026-09-11 KOOK 回复延迟与上下文维护循环事故

## 结论与当前状态

02:30 的消息已经进入权威入站账本，但截至本次核查没有观察到它的对应对外回复或送达回执。不能把日志中的“响应: True”理解为已经生成或发送回复；它只是路由决定进入主体表达链。

正在运行的是 01:16:45 启动的 PID 2212265，仍加载修复前的 Python 模块。磁盘代码已修复，运行进程尚未加载；代码测试通过不等于线上恢复。本次没有停止、重启、热补丁替换进程，没有注入额外测试消息，没有修改模型选择、代理、凭据、主体记忆或消费游标。

## 证据时间线（北京时间）

来源：Spark `/home/ayerelysia/Elysia/Elysium/logs/elysium-2026-09-11.log`、只读打开的 `data/life_storage/local.sqlite3` 和 `data/new_api_relay/one-api.db`；仅采集技术元数据，不复制私人正文。

- 01:16:45：当前主进程启动，位于用户 tmux scope。`systemctl show elysium` 显示 inactive、MainPID=0，因此不能把该进程误当 systemd 已接管。
- 02:11:45、02:15:00、02:17:28：检查点调用触发 `FrozenInstanceError: cannot assign to field 'name'`；驱动器重置内存工作状态并重新加载 117 条 payload 的滚动快照。
- 02:06:53、02:16:27、02:26:27：原始事件位置 271618、271625、271632 的 `action-life_send_text` 结果为失败；内容包含 `context_stewardship_required`。这是连续性维护拦截，不是 KOOK 已接单后投递失败。
- 02:22:26、02:31:28：模型轮达到 295 秒总预算，日志明确标记未处理消息保留、待重试。
- 02:30:12.123：本次用户消息持久入站，原始事件位置 271633，来源流前缀 `55c67609`。
- 02:31:32.704：该流进入路由，距入站约 80.58 秒；前一流 02:31:28 才结束失败轮次，与统一运行窗口排队一致。

## 因果链与次要耗时

旧代码对 frozen `ToolCall.name` 原地赋值，导致连合法检查点调用也崩溃。检查点不能安装，容量维护不能结束；普通发送继续被维护边界阻止，重建/重试又占据统一运行窗口，其他入站消息等待。

模型耗时也是独立风险：近期中转完成记录大多为 `qwen3.6-35b-a3b`、约 105k–108k 输入 token，多次耗时 54–297 秒；02:33:45 另有一条耗时 1995 秒、输出达到 32000 token 的完成记录。该条没有在本次核查中按 request ID 精确绑定用户消息，不可把它直接当作本条消息的耗时。当前磁盘模型配置允许 32000 输出 token 并启用 thinking；本次没有擅自关闭 thinking、切模型或删减权威上下文。先解除维护死循环，再基于新的真实链路评估常态延迟。

## 已有修复与本次补验

- `plugins/life_engine/core/chatter.py`：规范化函数只返回名称，不修改 frozen 调用；仅在名称确实变化时，用 `dataclasses.replace` 创建执行副本，保留原始调用对象、ID 和参数。
- `plugins/life_engine/service/core.py`：心跳调用元数据使用同一个维护工具名称规范化函数。
- 本次扩展 `test_context_checkpoint_install_resumes_with_user_control_frame`，覆盖合法名称和混入 XML 的名称；检查注册名正确、原始 frozen 对象没有被改写、维护后回到待处理用户轮次且协议链有效。
- 2026-09-11 本次轻量定向检查：上述两例及两个心跳名称测试共 4 项通过，单进程、不访问正式网络/账本，不与生产争用 authority。测试不能替代真实部署后验收。
- 本次代码未新增存储/记忆迁移，不删除或覆盖权威历史；测试变更通过精确补丁应用在原仓库，其他未提交修改保留。未暂存、提交或推送。

## 必须由用户执行的下一步

依仓库 `AGENTS.md` 第 10 节，手动运行的 Elysium 只能由用户停止并重新启动。当前进程不是 systemd 管理，不能直接另起第二实例。

1. 在 Spark 当前主控制台（可用 `elysia-attach` 接回）按 Ctrl+C，等待正常关闭完成。
2. 用原来的手动启动方式重新启动 `/home/ayerelysia/Elysia/Elysium/run_elysium.sh`。
3. 核查新 PID/启动时间、life_engine 正常加载、原消息仍可追溯、检查点成功安装、维护结束，并按本次流逐项核对实际回复和 KOOK 送达。不要先批量重发积压消息，也不要清空窗口或跳游标制造“恢复”。

若启动或检查点继续失败，保留故障状态与证据，不做强杀、数据清理或自动循环重启。部署恢复与常态延迟尚未验收。
