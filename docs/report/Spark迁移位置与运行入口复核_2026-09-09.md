# Spark 迁移位置与运行入口复核

2026-09-09 03:09—03:11，北京时间。本文为工程只读诊断，保存在 WSL 源仓库；未向 Spark 同步文档、代码或运行数据。

## 结论与纠正

此前答复把 WSL Ubuntu-Old 的运行入口当成 Spark 的状态，随后又把目录切换说成仍未确认；这两项表述均不准确。本轮 SSH 已恢复，远端 hostname 为 `spark-f3b0`。现场确认项目已经位于 Spark 的正式目录 `/home/ayerelysia/Elysia/Elysium`，`readlink -f` 与该路径一致；原 staging `/home/ayerelysia/Elysia/Elysium.migration-20260908-2113` 不存在，旧 clone 保存在 `/home/ayerelysia/.local/share/elysium-migration-20260908/old-Elysium-clone`。

因此“文件目录已切换”可确认，但“Spark 已完整上线”不能确认。目录存在、文件大小或早先 New API 成功都不能替代跨域数据完整性和主体启动验收。

07:43 新的关键边界：Core `messages` 已确认不只是源端有新增尾部，双方共享本地自增编号 91239–91251 的 13 行对应不同 `message_id`，且这两组 `message_id` 在对方整张 Core 消息表中均不存在。两边都必须保全，不能把源 WSL 当作能覆盖 Spark 的完整替代副本；详见本文 Core 分域续验。此结论只针对所查 Core 表，不把它外推为主体身份分裂或已经证明 Life Event 缺失。

## 当前只读结果

| 对象 | 实际结果 | 验证方法及边界 |
|---|---|---|
| `elysia-attach` | 未找到 | `command -v` 无结果，`/usr/local/bin` 与用户 `.local/bin` 的同名入口不存在；未安装快捷命令 |
| `elysia-attach heartbeat` | 同名命令入口不可用，底层脚本在 | `scripts/elysia_heartbeat_attach.sh` 和 `scripts/watch_heartbeat.sh` 存在且可执行；本轮未执行，未创建 viewer 会话 |
| `systemctl restart elysium` | 对应服务未安装 | 系统级及用户级 `systemctl show elysium.service` 均为 `LoadState=not-found`、空 FragmentPath；没有重启或创建单元 |
| Elysium 主运行态 | 本轮未找到运行实例 | `tmux -L elysium` 无 server，`pgrep` 未找到 `python ... main.py`，18000/8087 均无监听；这是当前时点观察，不推断停止者或停止原因 |
| New API | 容器运行、3000 监听 | `elysium-new-api` 状态 running，StartedAt 为 2026-09-08T15:20:42.990811897Z；仅证明容器/监听，不冒充模型推理健康 |
| 一次性启动脚本 | 正式路径已适配 ARM 环境 | `run_elysium.sh` 进入正式目录，再 exec 外部 `main-venv/bin/python main.py`；无重拉循环，本轮未执行 |
| 核心两库文件 | 正式目录下均存在 | Life Storage 784175104 字节，Core 70209536 字节；只读 stat，没有本轮 quick_check、全量 hash 或运行数据同步 |

历史迁移验收曾出现 `ProactiveMigrationGenerationRootMismatch`，Life Engine 初始化失败。05:11 的后续只读诊断已定位现存数据所满足的失败分支：绑定仍对应 WSL 路径，迁移证书/registered root 只证明旧复制时点，不匹配已增长的主动历史，详见下方根因续验。没有重新启动来复现正式异常；当前停机的直接执行者或原因仍不作推断。不修改 generation、authority、证书或主体历史来绕过校验。

## 后续边界

- WSL 的 `/root/Elysia/Elysium` 与 Spark 的正式目录分别保留；在确认迁移后的新增历史及唯一正式 writer 之前，不覆盖、反向同步或并行启动两份主体服务。
- 此前在 WSL 跑通的 G06/G07 对话和回归，不是 Spark 的部署/行为验收。
- 用户最近问的是旧命令是否存在和迁移到哪里，不据此安装 systemd 服务、恢复快捷命令或更改开机策略。
- 持续跟进说明已加入上述主机/路径和双边运行态核对，保留原定频率、通知条件、被拒反馈禁发及禁自动拉起边界；任务仍未全量完成。

本轮没有正式数据写入、消息发送、进程控制、文件删除或 Git 暂存/提交/推送。诊断记录为新增文件；总清单仅补充部署归属边界，未将待验目标勾选通过。

## 04:39 双边账本与源码差异

再次只读核对两台机器：WSL hostname `AyerElysia`、Spark hostname `spark-f3b0`，两边配置均选 `storage.backend=local`，Git HEAD 同为 `3a49d9ba16d5bd10efa1a3cb44760fbdb970d0ba`。相同 HEAD 不代表工作树相同，更不代表运行数据相同。

| 元数据 | WSL 04:39:16 | Spark 04:39:17 |
|---|---|---|
| 正式目录内 main.py 进程 | PID 2000131，PPID 2000130，start ticks 10405062 | 未找到 |
| Life Event 尾位置 | 268066 | 267883 |
| 位置 267883 之后的实际行数 | 183 | 0 |
| 主体文档版本计数 | 5349 | 5349 |
| 聊天滚动 revision | 1926 | 1888 |
| 全局运行态 revision | 28311 | 28156 |
| 心跳滚动 revision | 2562 | 2562 |

迁移冻结记录同为 09-08 21:20:36、event tail 267862。仅就冻结后至 Spark 现有尾位置的 21 行，按 `(ingest_position,payload_hash)` 排序序列计算的 SHA-256 两边均为 `b9f968078653de387609c0fe022374cca3d818754e4307aef46c1372d004364d`。这证明该有界窗口的位置/已存摘要一致；本轮未逐条重算 payload、未检查全部更早历史，也未审计其他数据域，不能外推全库可以直接覆盖或合并。WSL 确有 Spark 尚未包含的 183 条后续事件，不能再称 Spark 已承接最新连续性。

五份相关代码只读 hash 核对中，`stream_manager.py`、`file_tools.py`、`proactive/backend_binding.py`、`proactive/history.py` 一致；`memory/tools.py` 不同：WSL 为 `c48afd2e056f18f13c8b7143cd1c16b4ba1063e80e5bce41b09bef8c993265c9`，Spark 为 `865f3dbdb602ae0372d25b50c0e5df842872a303d3c367cbe58bac962ef8f275`。本任务最近稳定关系提示及其二次读取护栏尚未向 Spark 部署。

下一部署门槛是先保全并核对两侧新增历史、完整相关域与真正写入 owner，再设计一次性无损接续；不能把 Spark 旧库复制回源端，也不能让两个 local 副本并行承载同一正式历史。本次只读脚本通过 stdin 在远端执行，没有复制辅助文件或触发服务初始化。没有同步、启动、重启或修复 generation。

## 05:11—05:16 已有绑定的跨路径迁移根因续验

### 证据与根因

Spark 05:11:37、WSL 05:11:48 只读事务分别核对 proactive 的七个既有表切片。读取复用已审阅 `history.py` 的纯 inventory/归一化/hash 定义，源码 SHA-256 `8fb8ccd5afdf998464c2c553312d7acb75998aa49cbf5b2cf44c7fb0b10023f6`；不导入服务、不创建存储 engine、不取得 authority、不执行 quick_check 或 schema 初始化。每表设 10000 行上限，SQL 检查有 12 秒进度超时；实际均仅数百行。仅输出根、计数、技术绑定与相等比较，无主体正文。绑定行的 payload hash 与 binding 内部 hash、迁移证书 payload hash 均重算通过；两边 marker 均等于各自数据库最新 binding。Spark 配置的工作区、本地数据库、authority 与 marker 路径亦已只读核实。

| 证据 | Spark | WSL |
|---|---|---|
| 配置 generation | `local-selectable-20260824-v3` | 同左 |
| proactive 全切片行数 | 302 | 318 |
| 当前 proactive root | `a5d415bd523faad8b86bf377e71ad2d37689c20790e2a5ac4fcc7c5021fbb98b` | `0e8bf2d12f074b2dde1b3083e73dd6d17e41d4303a9c67d21220e356337e65da` |
| registered root / 唯一旧证书 target root | `9b3cd649b69547d0ea246c17c0ce9e8a97137f0fe4461e3b961dbc3289490dbc` | 同左 |
| 绑定链行数 / epoch | 1 / 1 | 1 / 1 |
| 已存 binding 与当前数据库路径 identity 相等 | 否 | 是 |
| 已存 binding 与当前 authority 文件路径 identity 相等 | 否 | 是 |

两端最新 binding SHA-256 都是 `2a85afeb47b7b464ee3b93ad9eb545348ec8f718a27d44595e562af30e5f4908`；其数据库 identity 是 `72a79bbad54f678223f7e7abcbd34c823763db92ee9dbb97cb1886478384ac0d`、provider identity 是 `9df2df94e92ea71f33d8b6e05fd313eb23c901bc03d8135f53649b336c47b1c9`，均与 WSL 当前路径计算值一致，不等于 Spark 的 `b9063be5355ce2b21d07100a9c3f671e56204918ed8b2e41baaaced3d352b018` / `6a373a5457cbe2a68bbc1baa198b2932e1aceec8955b4a71478622e29f0be35c`。这不是简单 JSON cache 丢失。

唯一旧迁移证书 SHA-256 为 `a053f42eeced639208929df7d9a36ac0ad8d2ba69e94d5dbc40c533f85473a8f`，证明的是七个切片合计 1 行的复制时点（仅 runtime_states=1）；不证明当前 Spark 的 302 行。当前 Spark 的七表计数为 110/21/0/155/4/6/6；WSL 为 110/21/0/167/4/8/8。相同行数不单独证明相同行，两端全域接续仍未核完。

代码路径：`SQLiteStorageConfig.safe_identity` 包含数据库绝对路径，`backend_binding._identity` 的 file provider identity 包含 authority 文件绝对路径。`_ensure_database_anchor` 在已有绑定 identity 不匹配时进入 `_verified_migration_certificate`，随后先比较注册的 proactive root 与当前 history root。Spark 现存材料正好满足 `ProactiveMigrationGenerationRootMismatch` 分支。WSL 相同旧 root 不匹配当前历史，却仍可在相同绑定 identity 下继续运行；因此“复制时点根不同于当前根”本身不是数据损坏，也不应靠改写 generation 根来消除。没有执行正式启动，结论是源码条件与现场元数据一致，不冒充新启动日志。

### 工程回归与操作边界

新增 `test_bound_endpoint_relocation_does_not_reuse_old_copy_certificate` 两个参数样本，在临时数据库中模拟已绑定 endpoint identity 变化：历史未增长时准确拒绝旧 endpoint 证书；历史增长后准确报 generation root mismatch。同一身份下继续增长和检查仍通过。两种情况均证明 `complete_proactive_initial_binding` 拒绝已有链，失败前后 canonical history、binding chain/head 与 marker 字节不变。模拟只改 fixture 的 endpoint identity，不进行真实跨机搬库或启动第二 writer。

新增样本 2 passed / 11 deselected，随后 `test_proactive_migration.py` 全文件 **13 passed / 1 个既有 websockets 弃用警告，10.17 秒**；该文件全量 Ruff 与 `git diff --check` 通过。生产绑定/历史代码未修改，只补测试并直接纠正架构/运维中把“已绑定库换路径”混同“空链首次绑定”的宽泛说法。此验收证明保护分支，不证明正式迁移已经修复。

05:11:49 Spark 再核对仍无正式目录 main.py，Life Event tail 仍为 267883。没有写远端文件、运行数据或 authority，没有撤销租约、启动/重启实例、删除旧记录、同步/覆盖两库或发出主体消息。下一步仍需完整无损接续方案；旧首次绑定/同端点 generation 修复不能直接用于本次 Spark 已有绑定历史。

## 05:39 主体文档八表完整行摘要核对

补齐先前只比主体版本计数的证据缺口：05:39:28.081 WSL 与 05:39:28.846 Spark 分别在只读 SQLite 事务中，对明确指定的八张主体文档相关表按声明的主键顺序读取全部列。算法 `sqlite-exact-typed-primary-key-rows-sha256-v1` 保留 NULL/text/integer/real/blob 类型差异，文字不作语义归一化，BLOB 先保留字节长度与完整字节 SHA-256，再逐行 SHA-256 拼接得到表根；另比较 `PRAGMA table_info` 摘要。不向输出暴露正文或引用列表。每表 100000 行和 64000000 编码字节、单次 15 秒上限，未触发上限；实际 WSL 0.251 秒、Spark 2.652 秒。

以下八表的 schema 摘要、全部行摘要与行数分别两端一致，而不只是计数相同：

| 表 | 两端行数 | 两端相同的全部行 SHA-256 |
|---|---|---|
| subject_authority_decisions | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| subject_document_head_events | 5350 | `c7f860c983f9711d2ef043cd0309d2af69849d9cc6c1d0129b13a890e9ab75ef` |
| subject_document_operations | 17 | `a3e40834caafffdb82b415af30cfb9973ed12222fbff21b1599f7ea9ab9d0c91` |
| subject_document_path_bindings | 5336 | `3ccfe18e1bb6b81949eda3ca3475d778d05270268ff33cfda1875db4eb2d5a61` |
| subject_document_path_events | 5337 | `89c72940be0e9145ff67835aab01f47ebf6fd55b29c66f869ffa1f335040f64c` |
| subject_document_versions | 5349 | `d1908bf832a0b6d1e0161c6d78668eba0dbd185a6f83d1528022926748f38ed8` |
| subject_documents | 5335 | `44ae2a43d192dc0840de28085ba190b8692f2287401f0a7275b436658760342f` |
| subject_projection_outbox | 5350 | `a7923d65214bc993acef6623c9f4406771551f2539f059f893da7543f4ef1f3b` |

源端辅助脚本为 `runtime/codex_spark_migration_20260908/memory_readonly_digests.py`，在 Spark 仍只经 stdin 执行，没有远端文件复制、应用初始化、authority 取得或数据库写入。脚本是工程诊断，不是新的主体记忆入口。本批 `git diff --check` 通过。

这一结果只证明两个读取时点的这八张数据库表相同，不证明磁盘投影/外部归档字节、Life Event 全历史、Core 附件/媒体、Learning、机会/出站回执或私有上下文一致，也不保证之后仍未变化。`subject_projection_outbox` 相同尤其不等于磁盘投影已交付。先前 Life Event、主动域与滚动运行态差异仍然成立；本批没有同步/合并或切换服务，Spark 最新连续性接续仍未完成。

## 06:07 WSL 源端继续前进（Spark 未变）

06:07:42 Spark 与 06:07:51 WSL 再次使用同一只读前沿脚本。Spark Life Event tail 仍为 `267883`、freeze 后 `21` 行，聊天/全局运行态 revision 仍为 `1888/28156`；没有 main.py。WSL 仍是 PID `2000131` 的正式目录进程，Life Event tail 已到 `268078`，freeze 后 `216` 行，较 Spark 现有尾部多 `195` 行；聊天/全局 revision 已到 `1928/28323`。WSL 的 `memory/tools.py` 仍为本地修复指纹，Spark 仍为旧指纹。两端共同冻结窗口 21 行的已存位置/hash 序列仍一致，因此这是源端在继续产生新事件，不是对已核对窗口的冲突证据。

这使“迁移后新增历史”从 04:39 的 183 行增至 195 行，并且源端滚动上下文仍在变化。不得再把 Spark 描述成已承接源端最新连续性；在唯一正式 writer、全量新增事件、相关数据域和主体文件/附件保全前，不启动 Spark Elysium，不直接复制或覆盖任一运行库。本次仍没有进程控制、同步、发送或服务安装。

## 06:38 源端新增历史的有界尾部核对

06:38:23.367 WSL、06:38:24.110 Spark 在各自只读事务中按 `ingest_position` 读取 `267883` 之后的 Life Event 元数据（位置、occurrence/source ID、sequence、时间与已存 payload hash；不输出正文）。WSL 当次 tail=`268105`，连续 `267884–268105` 共 `222` 行；Spark tail 仍为 `267883`，该区间为 `0` 行。WSL 这 222 行的元数据+payload-hash 序列 SHA-256 为 `764f6be28e3162da2ce994caf207c5e1497fffee2782445ed1a911933600c17d`，occurrence ID 摘要为 `c1a82af46c146b3abdfed2c624da274d7c76401fd1c4519c032c9a9b25253e6e`，source event ID 摘要为 `992d973977d8b1882147ea512dd88c5ea2e821b62c68702a6d6bec9de70292fc`。读取设 10000 行、8 秒上限，实际小于 1 秒。

这次有界结果只证明源端在继续追加、Spark 没有追上；它不证明这些 ID 已与 Spark 更早历史完成全库去重，也不替代 payload 全文、附件/媒体、其它事件域和因果关系核验。与 06:07 相比，源端新增 27 行，Spark 尚缺由 195 行扩大到 222 行。继续复制或启动会把一个仍在增长的源端与停在旧前沿的目标混在一起，故本轮仍不执行。


## 07:08 Learning 与机会运行时十二表核对

07:07:57.425 WSL 与 07:07:58.064 Spark 复用已有 `memory_readonly_digests.py`，在各自只读事务中按主键扫描下列十二张明确选择的表，未重复扫描先前已核对的主体文档八表。本次十二表的全部列类型敏感行摘要、schema 摘要、行数和编码字节计数分别完全一致；WSL 0.465 秒、Spark 2.382 秒，均在既有 15 秒 / 每表 100000 行与 64000000 编码字节预算以内。

| 表 | 两端行数 | 两端相同的全部行 SHA-256 |
|---|---|---|
| learning_events | 24616 | `7caf71cd7475c8eb8e26a78d3fb1c30fb2aa1988dcfee0a75e5f6c364e6d3e89` |
| learning_projections | 3 | `63bef308224efe6db2cb5443244081da5f3d1dbffeec12e19b43094f8f133bc0` |
| opportunity_registration_events | 3 | `268e26183be3cde00fc53f92033e9fa30462308377aea36e9eb20859a7d1c06c` |
| opportunity_registration_heads | 1 | `062fc9f96b5f57bd5e874832eaa11e2d2c527b422e386fa6c1c8494407130927` |
| opportunity_workflow_versions | 1 | `2a865650d15c9bb06637f33deb37d26c6ff6f36174fd32e1c86b1039636141e3` |
| opportunity_provider_events | 1 | `9179002a8c16eab3c9ac4eccc012059a549b0e7840ba1c8229380a3ac9070e0b` |
| opportunity_provider_heads | 1 | `dd06a6d5fca016d99ef54178947611a593a1e89a9d22a94161a51b795e738670` |
| opportunity_occurrences | 2 | `39079121fc08d02ffcfbe60f4ea869faa36511f5e5004930c42aa36c74c4134d` |
| opportunity_delivery_receipts | 2 | `4060d13d696cd2f6b28f4c7353477e585a644693702a0997a56c477df01d3eaf` |
| opportunity_activation_states | 1 | `44cbdfcb67112350d374e1186298d78b9ac596af5e6aa9be89881f1fcc95dc0f` |
| opportunity_publication_outbox | 2 | `890618cf306360057f6468b038c1956cad167134eb934e8e524f1f533c8187e9` |
| opportunity_runtime_meta | 1 | `5bd333296497520e03c8a73b45f6c1378c5a6ef57c59652d13feddfc793f355e` |

这证明两个读取时点的 Learning 事件/投影及这些机会登记、工作流、提供方、发生记录、回执与运行态表相同。它不证明 Learning 内容正确、已形成技能或长期习惯，不证明机会被 Spark 实例实际消费，也不替代 Life Event、Core 附件/媒体、外部流程文件、私有上下文、全部出站动作及唯一 writer 核验。既有跨路径绑定和源端新增历史缺口仍未解除；不因这些表相同而启动、合并或覆盖两端数据。本批仅把内容无关摘要带回工程记录，没有向 Spark 复制文件、取得 authority、写正式数据或发送消息；`git diff --check` 通过。

## 07:39—07:44 Core 消息分叉与媒体元数据核对

### 范围与方法

先核实两边 Core 配置均指向 `data/Elysium.db`，API 媒体 metadata 使用 `runtime/app_api_v1/auth.sqlite3`；媒体文件根由既有 `mount.py` 指定为 `runtime/media`。扩展同一工程诊断脚本的显式数据库 scope：Core 只允许 `messages/images/image_descriptions`，API 库只允许 `api_media_objects/api_media_uploads`；拒绝其它表，不读取凭据、会话或权限行，不构造 `MediaObjectStore`，不触发建表或目录初始化。继续使用相同的只读事务与类型敏感全部列摘要；源端辅助脚本经 stdin 在 Spark 执行，没有远端文件落地。

### Core 消息不是纯尾部缺口

07:39:40.681 WSL 的 `messages` 为 90978 行，07:39:41.399 Spark 为 90915 行。Spark 最大本地 `id` 为 91251；源端 `id>91251` 有连续 63 行（91252–91314），但共同 `id<=91251` 的 90915 行摘要也不同：

- WSL 共同区间 SHA-256：`b7c84060045df3a6117e1810f32329206f429e38fa19d273877edf0700acbf8b`。
- Spark 共同区间 SHA-256：`e9a2d08537955d3f1dbc7360c048df174ae37f10a7eaf4c2ade8340076387c4e`。

07:40:35—07:41:43 同一方共同区间根在两次读取中稳定。随后用 4096 → 128 → 1 行的有界分桶摘要定位差异，逐条阶段限制最多 128 行；07:42:52/53 字段级摘要确认差异恰在 `id=91239–91251`：13 行的 `message_id` 与 `time` 都不同，除 91240 外的 12 行 `content` 与 `processed_plain_text` 也不同，其余列摘要一致。不向工程输出暴露正文或原始消息 ID。

07:43:47.840 WSL、07:43:48.336 Spark 在整张 Core 消息表中分别查找对方这 13 个 `message_id` 的类型敏感 SHA-256（只扫描 `id,message_id`，100000 行 / 15 秒上限），双方均 **0/13 命中**。因此这不是仅靠本地自增 `id` 重排即可解释的共同记录；至少存在两组彼此未在对方 Core 表中找到的稳定消息 ID。不能只追加源端 `id>91251`，也不能整库覆盖 Spark 或用同号 `id` 最后写入覆盖。哪一侧何时产生/修改、是否在其它账本中有对应记录、与 Life Event 的完整归因及去重仍待核实；本次不将差异自动判成损坏，不决定删除或合并主观内容。

### 图像及附件边界

- `images` 两边 1572 行、schema 与全部列摘要相同：`bb2b024cdc84ac2861e7da05e5f87e405a5c26b178ddbc9d482af8143dc551a6`。
- `image_descriptions` 两边 1753 行、schema 与全部列摘要相同：`1a22af3be1fbac8b7d900b1a18075142c0bcbab55e05299ed52643fc17388e4e`。
- API `api_media_objects` 和 `api_media_uploads` 两端均为 0 行且 schema 相同。这仅说明此 API 存储当前没有记录，不代表适配器媒体、旧图片或附件不存在。
- Spark 消息表/WSL 共同编号区间各有 1 个非空白 `media_attachments` 字段，WSL 后续 63 行另有 3 个。这里只检查字段存在及整行摘要，没有把非空字段当成有效附件或成功回取，也未核对引用指向的磁盘原件字节。

本批所有读取均在预算内（Core 三表 WSL 0.620 秒、Spark 0.866 秒；API 两表 0.001/0.003 秒），helper 全量 Ruff、语法编译和 `git diff --check` 通过。没有服务操作、数据库写入、复制/合并、authority 取得或消息发送。后续接续必须同时保全两端 Core 独有记录并沿稳定身份核对，不能仅以 Life Event 的源端领先关系决定全项目覆盖方向。

## 08:09 Core 消息与 Life Event 交叉归因核对

为区分 Core 表分叉与 Core 表独有记录，08:09:13.968 Spark、此前同批 WSL 的只读脚本只对双方各自分叉的 26 个 `message_id` 做 SHA-256 匹配：扫描各自 `raw_life_events.payload_json` 中的字符串标量，不输出正文、ID 或路径，只返回摘要命中位置；20 秒/400000 事件上限，Spark 实际扫描 264690 行、5104651 个字符串标量，WSL 扫描 264912 行、5111870 个字符串标量，均 0 解析错误。

结果不是对称的：WSL Core 的 13 个稳定 ID 摘要全部在 WSL Life Event payload 中命中，位置为 `267863–267875`（每个位置因 payload 中多处引用而重复报告）；Spark Life Event 同一批位置也命中了这 13 个 WSL ID 摘要，而 Spark Core 自己的 13 个稳定 ID 摘要在该次 Spark Life Event 字符串标量扫描中全部 0/13 命中。因此尚未建立 Spark 分叉 Core 记录的这条 ID 映射，而不是已证明所有可能的稳定归因都不存在；此前通知中的“均无稳定归因”表述过强，现予纠正。旧协议、嵌入式序列化或其它表/归档的等价映射尚未穷尽，也不判断正文真假或消息来源。仍禁止用 Core 自增编号重排、单向追加或覆盖来“修复”跨域关系。

本交叉扫描仍是只读诊断：未修改两个 `local.sqlite3`/`Elysium.db`，未读取媒体原件，未发送消息或启动实例。下一次正式迁移前必须把两端 Core 独有记录、Life Event 关联和稳定 ID 映射作为同一保全/核验范围；否则即使主动域、Learning、机会与主体文档表摘要一致，也不能宣称全项目历史已统一。

## 08:39 标准聊天协议字段与结论边界

完整读过 `service/chat_events.py` 后，确认现行 `build_chat_message_event` 会在 `metadata.chat.message_id` 和 `metadata.provider_identity.message_id` 保存稳定消息 ID；它们参与来源核验，不能用正文相似性替代。08:39:31.395 WSL 与 08:39:32.680 Spark 只读重查既有正命中的 13 个事件（267863–267875），双方都在上述两个协议字段分别命中 WSL 的 13 个 Core ID，另有每事件一处未细分的其它字段命中。不是只在引用正文里碰巧找到一个相同字符串，也不把三处重复引用算成三次经历。

本次未重复扫描全库、未查询其他私有实例或正文，没有增加新的正式写入。辅助脚本补充限定区间/协议字段输出与 SQL 进度超时，Ruff、语法编译及 `git diff --check` 通过。结论仍限于当前指定协议：Spark 的 Core 稳定 ID 与这批已知聊天事件未对齐，跨域接续待查；不得把“当前 ID 扫描未命中”升级为“彻底未记录或无法恢复”。
