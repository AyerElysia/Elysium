# 潜意识账本消费与检索验收（2026-09-03）

> 问题 1 工程落地记录。不替代 `docs/architecture/生命引擎核心.md` §2.3.1。
> Elysium 须由用户手动重启后，才能用真实心跳日志验收。

## 行为

- Consumer：`life_engine_subconscious_ingest:v1`
- Bootstrap：consumer 从未创建且 `heartbeat_context_cursor > 0` 时 seed 到账本 frontier，metadata `bootstrap=high_water`
- Grep：账本 `scan_window` + 运行时 pending/history；空 query 合法；扫不全 `stats.scan_truncated`
- Health：`health()["subconscious_ingest"]` 只含游标/backlog/gap/error_type，无事件正文

## 验证

定向测试（2026-09-03 本机）：

- `test_subconscious_ledger_consumer.py` + `test_event_grep_tools.py` + `test_life_event_storage_contract.py`：31 passed
- `test_service.py`：59 passed

命令：

```bash
uv run --group dev python -m pytest \
  test/plugins/life_engine/test_subconscious_ledger_consumer.py \
  test/plugins/life_engine/test_event_grep_tools.py \
  test/plugins/life_engine/test_life_event_storage_contract.py \
  test/plugins/life_engine/test_service.py \
  -q --no-cov -n 0
```

启动验收（用户操作）：手动重启 Elysium 后确认心跳 prepare 不再在「只写账本」路径上丢事件；health 出现 `subconscious_ingest` 且无 gap。

## 待用户决定

是否立即重启以验收心跳 catch-up。Agent 不得代启。
