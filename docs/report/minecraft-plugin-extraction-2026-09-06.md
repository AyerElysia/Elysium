# Minecraft 独立插件拆分验收记录

## 请求与边界

2026-09-06，用户要求 Minecraft 成为独立插件，清除 life_engine 内的 MC 代码，
并明确确认在保留现有未提交修改的前提下执行拆分。变更直接在原仓库完成，未创建
worktree、未暂存/提交/推送、未启动或停止 Elysium/游戏/OBS。

## 已执行

- 将原 `plugins/life_engine/minecraft` 的 Python 模块、两个 Windows 辅助脚本和设计说明
  迁入 `plugins/minecraft`；不保留旧模块转发层。修正 Python 相对导入、项目根路径与日志命名。
- 新增独立 manifest、MinecraftPlugin、MinecraftConfig 和 MinecraftService；
  配置、组件、会话、桥接、游戏事件 schema 和生命周期归新插件。
- life_engine 移除 Minecraft 配置、工具注册、会话字段/启停、事件构造器和专属判断；
  保留通用 SceneExtension、append_external_event、事件源命名空间检索及传输投影保护。
- 聊天工具入口和“工具回执先于同轮可见回复”通过启用插件的 owner-scoped 声明接入；
  正常卸载撤销声明，关闭失败保留所有权以供重试。控制台改为独立游戏服务提供者。
- 旧专属测试随插件迁移；增加加载/禁用/重复启停、缺失依赖、关闭失败、配置迁移冲突、
  缓存淘汰/服务重启重放、辅助脚本打包和依赖加载顺序契约。
- 更新当前架构、意识/世界协调、核心配置表、MC 架构、运行手册及 AGENTS 目录地图。

## 配置操作与可逆性

17:34（Asia/Shanghai）只读迁移检查返回 `ready`；17:36 显式迁移执行成功。
原配置逐字节备份为 `config/plugins/life_engine/config.toml.minecraft-migration.bak`，
SHA-256：`58e1898c3c6d9595132922632a686f104d335e83316da6e31e9c0942018c82ab`。
该备份及实际配置被 Git 忽略，权限为 0600。新配置位于
`config/plugins/minecraft/config.toml` 的 `[settings]`。

迁移先验证新旧字段值，再写独立配置，最后移除旧游戏段和旧证据预算字段；
无关配置保持原值。新旧冲突、不同备份或检测到并发变化会明确失败。
账号、端口、路径、自定义预算和启用状态不在本次迁移中重新选择。
主体文件、历史事件、私有上下文、世界数据库及游戏存档均未迁移或改写。

如需回滚，先由用户手动停止完整系统并保留当前配置，再恢复匹配的旧代码与备份。
迁移后产生的其他配置修改不能被旧备份覆盖；应单独核对。账本无需回滚。

## 验证证据

一次定向回归覆盖 Minecraft、控制台、事件检索、对话证据、World Projection、工具清单、
life_engine 配置与 service：333 项通过，仅有 websockets 既有弃用警告。
迁移专项 6 项通过，包含只读检查、完整备份、幂等、部分安装恢复、目标/备份冲突拒绝。
F/E9 静态检查通过。本任务范围内差异空白检查通过；全仓检查另发现未由本任务修改的
`test/plugins/life_engine/test_heartbeat_tools.py` 有 CRLF/行尾空白，未代为清理。

收尾补检（辅助脚本、插件加载顺序、launcher、Presence/World 选定存储契约）55 项通过。
只读配置复核确认：两份配置均通过当前 schema，旧游戏段已移除，新插件保持启用，
迁移输出与原备份生成的计划逐字节相同，备份和新配置权限均为 0600。
旧源码/测试目录确认只剩 .pyc 后已清理；所有源码、两个 Windows 脚本和设计说明保留在新目录。
上述测试组存在重叠，不把数量相加冒充独立覆盖。它们是本次运行证据，不代表未来版本状态。
真实插件启动仍待用户手动重启后的日志，不以单元测试冒充现场验收。

## 未执行与待验收

- 用户手动完整重启后，核验 `minecraft` 插件加载、独立服务和正式工具签名，
  并验证控制台 `preflight/status/start/stop`。本次未自动启动主进程来验收。
- 原生共享 agent 的旧配置连接参数按原值保留，仍需核对独立 ElysiaClient profile 的
  18768 监听、令牌路径和 0.3.0 模组锁；配置迁移不等于完成环境升级。
- 真实同服、游戏聊天、原生像素、任务终态和持续游玩仍未现场完成验收。
  身体内存 journal 仅支撑连接重放，不能宣称游戏进程崩溃零丢失。

早先“Minecraft 由 LifeEngineService 持有”的当前架构描述已直接改正。
历史报告可保留当时事实，不作为新插件所有权依据。
