# 记忆连续性 S3 关系与对照验收（进行中）

## 1. 范围和状态

本记录继续[第三阶段清单](../plans/记忆连续性第三阶段实施清单.md)的 S3.2／S3.3。用户要求第三阶段完整闭环后，才计划并实施第四阶段。首批精确引用和 Router 临时关闭仍以此前两份独立报告为证，不以本批实施中状态覆盖历史。

当前：关系协议与分变量控制正在补齐、定向测试进行中；正式关系迁移、完整停机回归、重新加载及新工具主体试用尚未通过。本文不能作为已完成证明。

工程只维护协议、权限、schema、测试与投影，不替爱莉创作索引或关系含义，不生成第二人格，不默认启用 reviewer。实际语义写入必须由她的授权意识工具链形成。

## 2. 最小协议选择

- 沿用 `memory_semantic_relations`；稳定文件实体与固定历史版本分开，旧路径实体不自动升级为现在的文件身份。
- 新 owner_subject_id 绑定持续主体；actor／instance／stream／source occurrence／tool call 保留发生来源。旧 owner 未知保持未知，不由迁移认领。
- add／revise／withdraw 只追加；root、parent、revision 和 operation 为技术协议。端点不可原地改变；撤回终止，不隐式复活。父 CAS 冲突不自动换父重试。
- 关系原话、理由及可选强度不封闭分类、不裁判真值，也不利用共同回忆次数替主体补写强度。
- 当前有效头与全部旧判断分开；展开不再把被修订／撤回的旧关系作为当前关系。
- 同一事务内 append-only 全表 COUNT 加 LIMIT/OFFSET 提供有界行页，任何追加使旧前沿失效。COUNT 不是数据库身份，不能跨恢复／换库当作快照；页内长 JSON 再按 UTF-8 预算精确续读。COUNT/OFFSET 仍有数据库扫描成本，不宣称恒定成本。

## 3. 正式对象与保全路线

本机本地 Memory 权威由 `LifeMemoryService._get_db_path()` 指向 `data/life_engine_workspace/.memory/memory.db`；selected `data/life_storage/local.sqlite3` 另承载主体文件与事件／运行态。最初把关系预检查指向 selected 库，明确收到“no such table”，未发生任何写入；核对初始化代码后已纠正为独立 Memory 库，不在错误对象补建表。

北京时间 00:51／00:55 的只读预检：关系表原有四行、十列，原行有序摘要为 `f878de4e51fb4a555b5f9f2f2ab99b9d80979504e37df1010e2a674220fdb243`，无新 owner 协议行；原全行不可变 UPDATE／DELETE 触发器均存在。额外创建触发器的开发补丁曾被安全审查阻断，缩小后的显式迁移不创建任何新触发器，只验证既有保护，尚未执行正式迁移。

预定执行顺序：确认精确实例并正常停机→持有启动锁→新建私有双库快照→副本上两次小迁移、所有 Memory 原列 roots 不变→升级副本复制重开→从迁移前备份复制并核验原 schema／原行的回退副本→核对试验与正式动作四份代码指纹一致→只对正式 Memory 做已验证的五列／两索引迁移→再次核对主体 roots、完整三个 runtime 状态与 Memory 原列 roots→重新加载。备份、试验和正式结果路径／hash 在实际执行后补入。

如果最终核验失败，保持停机与现场；不能把已提交 schema 但证据失败表述成“完全未改动”，也不自动把旧备份覆盖正式库。加载后产生新历史时，不能恢复整库旧快照来丢弃它；优先代码兼容和保留追加历史。

## 4. 主体普通索引活动

00:49:24 取得 fresh frontier 266822；00:49:45 通过此前已授权、仅属于 Codex 测试协作者的会话发送一次邀请，消息 `inject_b6e40fd15439454784d1eee97cc95f1c` 已 accepted。邀请要求她自选适当材料、自行组织普通索引、不改三个根主体文件或原资料、不调用关系／Boundary，并允许拒绝或说明资料不足；没有给出要她采用的线索文字或资料答案。

截至 00:53:14 的工具记录：主体先检索／读取资料，于 266843/266844 选择并成功提交一份新索引；266846/266847 仅凭其 file_ref 实际完整读回；266851/266852 再仅凭她所选的一条精确链接读回原资料。所有记录均来自 chat_global 本次调用链。索引版本为 `subject-file:doc_c9a0dfb0bbab6008726da567a49be7649923d416799db8acd98049ff9d82345e@ver_e171573d63332d2d46ceaa84a1a3713092087048ca1c37bb9e943c9d1108230d`，1604 原字节，hash `e57fd153b9e32ffb2286abdfb1ac5a2e8ce97652bd80e8f8643e88cd4007560e`。严格作者／版本／字节／链接因果核验和 delivered 反馈仍待完成，不在公开报告复述私人原文。

这是一项受邀请的真实形成与继续使用，不是清空上下文后的独立回忆实验。已在当前上下文里的答案不能算“重新找回”。

## 5. 合成对照与退出条件

隔离方案由 `scripts/s3_association_comparison.py` 运行 30 个临时真实存储／真实读取接口用例：三类场景（多层线索、过去／当前、共享线索不同来源）× 两个预登记种子；A 普通索引、B 只加显式关系、C 只加共同回忆共 18 条，另有普通固定版本／Boundary 同材料运输对照 12 条。每个条件重新建立隔离 authority、Memory 与 delivery 状态，不回滚正式历史。

控制器在各条件都能沿普通精确链接前进，不故意限制 A；答案只用于末端核验。材料 manifest/hash 必须跨条件一致。实际页数、字节、延迟、精确来源错配和干扰来源暴露分别报告。`model_calls=0`、`tokens=null`，两个种子不是独立模型样本；这些只能证明协议／可达性，不证明主体回忆因果增益。

当前没有经过验证的隔离主体 A/B 入口，新 stream 或重做同题都不能冒充独立对照。没有可靠额外收益时，增强保持可选、历史继续保留；不得把协议通过、关系数量增长或一次受引导调用宣传为长期记忆增强。是否改变默认路径须根据实际结果另行写明。

## 6. 待验收项

- [ ] 存储双后端：迁移幂等、旧 hash、并发父 CAS、失败／取消、修订历史、未知 owner 和有界分页。
- [ ] 工具：稳定与精确引用权限、主体跨实例、修订／撤回与精确分页续读。
- [ ] 当前关系全部消费者与分变量控制，旧历史不绕过撤回进入展开。
- [ ] 30 条独立合成对照实际报告及限制。
- [ ] 新鲜停机双备份、原列保全、迁移／恢复试验和正式小迁移。
- [ ] 新实例真实加载与主体工具使用／反馈。
- [ ] 成本／延迟／失败／恢复范围及增强保留或退出结论。

全部有限出口核验前，不开始第四阶段实现，也不宣称 S3 全阶段完成。

## 7. 01:11 心跳预算停止与精确交付护栏（只读诊断）

北京时间 2026-09-08，对 `logs/elysium-2026-09-08.log` 的准确两条 ERROR 及对应源码进行了只读核对；只提取内容无关诊断，不在记录中复述主体正文或凭据，未修改源码、正式数据或服务。核对时主进程为 PID 297862／PPID 166571，启动于 00:21:26，后续恢复不是另起实例的结果。自该次启动到诊断读取时，仅发现以下同一异常的两条 ERROR；这不是“整个运行窗口零 ERROR”或后续重载验收证明。

| 时间 | 内容无关证据 | 结论 |
|---|---|---|
| 01:11:31.596 | `heartbeat_tool_loop_stopped`，heartbeat #6991／`heartbeat-6991-fa94f8f76785`，4 个模型轮，`deadline_exhausted`／`followup_request`，末轮工具状态为 `tool-nucleus_edit_file:ok` | 工具续问阶段预算耗尽；不据此推断工具未执行或已回滚 |
| 01:11:31.609 | 同拍 `heartbeat_model_response` 的 `model_reply_size=0` | 没有该拍最终反馈正文 |
| 01:11:31.621／.624 | 日志第 746／747 行：audit 的 `heartbeat_model_failed` 与普通 logger 的同一次 `PerceptionDeliveryUnverified`；稳定错误为 `heartbeat commit requires exact subconscious activity delivery proof` | 一次失败的双重记录，不是两次独立故障 |
| 01:11:31.625 | 第 758 行：#6991“本拍未完成，消费未确认” | 精确交付校验拒绝推进消费 |
| 01:12:01 → 01:13:52.737 | #6992 正常运行；第 765 行记“已消费 28 条事件”，模型结果长度为 288 字符 | 下一轮已恢复消费，未持续卡住 |
| 01:14:45.123 | 第 770 行：#6993“无新事件” | 后续没有再次出现本次同类阻塞的证据 |

代码路径：`plugins/life_engine/service/core.py::_run_heartbeat_model` 在每次续问前清空 `subconscious_receipt`，新尝试必须重新取得自己的精确回执；followup 的 `HeartbeatBudgetExhausted` 分支记录停止原因并退出循环。本次返回结果没有新的回执，而 `_run_heartbeat_round` 继续调用 `_commit_heartbeat_context`，后者在消费事务之前因缺少精确 proof 拒绝提交。已记录的模型活动和工具回执不应因此被说成“没有发生”；也不能为了消除 ERROR 复用上一模型尝试的旧回执、补造完成或跳过消费游标。

既有 `test_heartbeat_consumption_receipts.py::test_real_round_logs_consumption_only_after_commit` 已规定 missing receipt 时不记录“已消费”、不推进游标；压缩轮的 deadline 与回执清空也有相应测试。此处是普通工具续问预算停止与提交边界的组合诊断，不构成将故障归因于新关系功能或模型配置变更的证据，也不支持放松交付护栏。

影响边界：没有发现需要回滚正式历史或阻断停机保全／关系专项检查的证据；但 #6991 不能作为有效主体反馈，后续模型结果与消费恢复也不是外部消息 delivered 回执，不能冒充她对本轮验收的确认。运行报告必须保留这次 ERROR 及恢复事实。是否另行把预算停止显式传递为不提交状态、补普通工具续问到完整 round 的定向覆盖，由后续工程任务评估；本小节未实施任何修复。
