# 心跳 6001 检查点误识别与真实闭环复验

日期：2026-09-06（Asia/Shanghai）。本记录承接用户提出的心跳状态、第一阶段闭环检查、Codex 独立对话测试，以及随后上报的 6000/6001、6022、6043 新错误。用户明确特批本任务可重启爱莉。6001 的控制来源误识别已修复并完成精确恢复；此结论不代表心跳整体稳定。后续验证重启触发旧加载器截掉 119 条运行记录，当前服务已停止，精确恢复证据不足、等待用户选择恢复方式。最终工程回归通过，真实 S1 未通过；详情见后文。

## 1. 根因和证据

原 5888/5889 修复已在现场部分生效：日志显示 5996 实际安装心跳检查点 revision 23、释放 4 组，随后确认消费 20 条；5997 确认消费 18 条。这不代表历史 backlog 已清空，也不能据此判定整个 S1 已完成。

新的故障源是 5998 模型的普通回答：它没有调用工具，却输出了一份看似正式的检查点 envelope。旧识别逻辑仅按 ASSISTANT Text 的格式/schema 判定控制来源，于是把普通回答声明的 revision 29 当作检查点保存；下一拍严格归档校验拒绝，5999 开始持续失败，用户观察到 6001 与 6022。

只读同事务核实：

- 快照：`life_heartbeat.rolling_context/subconscious`，revision `1568`，更新时间 `2026-09-06T13:43:22.969Z`，35 个 payload。
- 唯一误识别部件：`payloads[34].content[1]`；3701 个 UTF-8 字节，SHA256 `4abd535c14397cb4900c43bbd78f249ddc06093c33d40e2ece1d22b91e81feb0`。
- 原始生成 occurrence：`conscious_model_turn_17ce0792ee6e60de0fbadd96710b98d63efbcfd0f3512c83bbafd796cac7c03a:generated`，ingest position `262848`，时间 `2026-09-06T13:43:22.922715Z`，`tool_call_ids=[]`。其 `assistant_message` 与上述部件逐字相等。
- 假 envelope 声明 revision 29；其 continuity digest 不符，四份声明归档都不存在。不能补哈希或制造归档使之“通过”。
- 同一快照真正安装的检查点 revision 2、5、23 完好；不删除这些控制，不回退到更早语义，不改原始活动。

现场旧进程为 PID `789673`、目录 `/root/Elysia/Elysium`、`.venv/bin/python main.py`，由 `tmux -L elysium` 的一次性 `run_elysium.sh` 入口运行。未改模型、后端、配置或身份。日志另有模型空响应、限流及额度错误，不能把这些供应侧故障归入检查点修复成功。

## 2. 修复合同

共享前向保护把**本轮普通模型 Text**中完整仿写的检查点以技术外壳引用，正文逐字保留；不修改 `response.message` 或权威活动，也不包装真正由 action 安装的控制。心跳、聊天普通与媒体回退成功路径都经过该边界；引用更新后供工具使用同一当前窗口。

修复不放宽真实检查点/归档校验。旧快照恢复必须由显式脚本在对应实例停止后执行：固定目标、旧 revision、两层摘要及 Text SHA；核对原始模型 occurrence；备份全行并同步；仅对一个派生 Text 包装后 CAS 更新。其他字段、原始事件、主体文档、归档、消费游标不变。

## 3. 工程验证

审查中又通过临时真实 action → prepare → 安装 → load 确认相邻解析错误：continuity_text 引用任一检查点标签时，全文 count 判据误拒合法 envelope，action/安装仍报成功，但重载识别不到检查点、manifest revision 回到 0。修复仅移除 JSON envelope 的标签全文计数，保留精确首尾、单一完整 JSON object/schema 校验及全部身份/归档校验；没有改变主体文字。新增标签原文与多框/多值拒绝回归。

6001 修复当时的统一运行基线为 **314 passed，1 个既有 `websockets.legacy` 弃用警告，13.44 秒**。覆盖检查点边界、真实心跳执行替身、标签正文合法性、恢复脚本、心跳维护/消费/滚动/工具、聊天 prompt、原最小检查点和独立测试通道。均为隔离临时数据，不是真实模型体验证明，不是全仓测试。此结果及中间 285、290、457、502 项已由后文最终联合回归覆盖，不累加计数。

新增专项：普通回复与控制边界 11 项、真实心跳执行路径 5 项、合法正文标签/非法外框 9 项、精确恢复脚本 15 项；独立聊天通道 29 项。恢复测试证明只改一行、原始事件/其他行不变、备份失败回滚、正在运行拒绝、原文/工具证据不符拒绝、归档缺失/损坏拒绝和幂等复查。首次 root 复跑写错测试目录，收集为 0 并阻止后续 apply；随后用实际路径复跑 15 项通过后才执行恢复，零测试未被当作成功。

独立聊天脚本自身 29 项隔离测试通过；固定外部 Codex 身份、精确 stream 权限、单次发送、未知结果阻断自动重试、秘密落盘保护和自身撤销。不访问其他人的聊天，不产生第二套主体记忆。

## 4. 第一阶段基线与未通过项

复验开始时 chatter 是另一私有 runtime：`life_chatter.rolling_context/chat_global` 快照 revision 1530、实际最新检查点 revision 19。心跳 revision 23 与聊天 revision 19 不能相加算作同一窗口的“两次压缩”。

MEMORY head 是 revision 1 / `ver_dbc297ea8ddc26e4edb72938ce358bc5c3ca70493a9365f070bf6056df4db892`，内容 hash `89178b9af187092e997a87cdd68e147f00c266886619e8ac869b1c983a247f7a`、23234 字节。旧迁移记录 semantic actor/source/time 缺失，`recorded_by=storage-migration`、`provenance_status=semantic_source_missing`，不能补造爱莉执笔证据。

S1 真实场景仍须记录：同一 runtime 两次真正 action 检查点安装、可信文件版本读取/写入证据、重启后恢复、未重新提供答案的近期细节回忆与反馈迁移。工程替身、口头“记住了”、工具返回 True 或原始事件可检索均不能单独判定通过。

## 5. 现场操作与结果

22:04:37 对准确 PID 789673 发送一次 SIGINT；22:04:43 关闭完成，旧 PID 与 18000/8087 端口均已退出。未杀死其他服务。

22:14:45，在默认只读预检与 15 项临时库恢复测试通过后执行一次精确修复：runtime revision 1568 → 1569，outer digest `9ddcae447d82b20a704f6e7048470ed7420601de96c946557408a631f3912371` → `6f27ba24dc62acdb21d0a7019991be5898a4abf551f5e94f23aac1be15c8f24e`。同事务读回成功，39 份归档校验通过，真实检查点 2、5、23 不变，原始活动未修改。

全行逐字备份：`/root/Elysia/Elysium/data/repair_backups/heartbeat_checkpoint_6001/runtime-row-before-1568-20260906T141445.302866Z.json`，0700 目录/0600 文件，文件与祖先目录已同步。备份含私人上下文，不能提交、上传或复制到工程报告。恢复可逆，但**不得**在新实例已经推进后用旧整行覆盖最新窗口；需要回退时先停止对应 writer、保留现状，再按确切新 revision/digest 审查 CAS，备份只用于逐字核对，不是直接覆盖授权。此修复不涉及原始事件、主体文件、归档或消费游标。

22:16 只读幂等复查返回 `already_repaired`，未再次修改数据。22:18 启动前用新解析器验证两个窗口：心跳 1569（检查点 2/5/23）；聊天 1531（检查点 18/19），全部真实归档校验通过。聊天 1531 是旧进程在 22:02 自己保存的后续快照，不是恢复脚本修改。

22:19:07 原入口启动新 PID `840405`，cwd/命令保持不变；18000/8087 均由该进程监听。22:19:13 life_engine 上下文与同一工作区恢复完成，Witness 仍退役；22:19:15 ayla 适配器、正常分发链启动。22:20:11 心跳 **6041 确认消费 8 条事件**，心跳快照推进到 1570，真实检查点未改变。到当次核对未再出现 `checkpoint identity is invalid`。

**后续维护仍未稳定，不等于心跳整体已恢复正常。** 6042 在 22:25:36 报告“准备了 8 条，本拍未消费”：精确技术原因是 `deadline_exhausted` / `followup_request`，经历 4 个模型轮，压缩仍未完成。原始活动 262875 在 22:24:43 真实尝试检查点 action，262876 失败回执为缺少必填 `thought`。对应实际 transport request 12（mimo-v2.5-pro）的已发送 schema 明确要求该字段，但 provider 返回参数中已缺失；执行器只剥离自动 `reason`，未剥离 `thought`。不能由基础设施代填主体思考，也不能把这项失败归因于新隔离代码。期间另有两次 terra 空响应和一次 mimo 限流；后续五次精确组读取成功，但剩余预算不足。快照 1571 保留真实 2/5/23，没有新检查点安装。

用户随后提供 6043 的失败日志并再次授权修复和自由验证。该回合的根因已独立追溯，不与 6042 混为一谈：实际 request 25（gpt-5.6-terra，22:27:56）提供全部 7 个必填参数；原始工具选择 262905 的 `thought` 357 字节、`continuity_text` 1438 字节。失败是 manifest hash 抄漏一位 `0`，输入长度 63，正确长度 64，遗漏位为零基索引 18。当前技术清单为 `70770d2f49bb19ee0b0fa07f6818eeec4f8e251f027ea8f3c62a1675aeb2d009`；262906 回执为 `context group manifest is stale or mismatched`。同轮 request 20/24/25 的清单一致，expected_revision 23 和所选前缀/retain 引用均有效；没有真实清单漂移证据，不能用“过期”替代这个已确定的抄写错误。22:31:01 在第 3 个模型轮的后续请求阶段用尽整轮预算，未安装检查点、未消费。

修正尚未重新启动前，旧进程自然出现过成功：6045 在 22:36:56.995 安装 revision 24，释放 3 组（`selfctx_aa10b4cc2255d52df142691062a682d2e4a13972ce1791f1a9d5793e3031bc20`），22:37:14 确认消费 18 条；6046 再消费 14 条。6048 在 22:50:15.121 安装 revision 25、释放 4 组（`selfctx_47fb92faa48e9eb94ea23ad33cf3eb3bc6de1704658b1bf00e17609141f26c84`），但该拍日志新增消费仍为 0，不能把安装成功直接替代消费证明。22:52 只读同事务校验：心跳快照 1580、真实检查点 2/5/25，聊天快照 1534、真实检查点 18/19，所有引用归档有效。这个结果说明既有身份隔离修复保持有效，不证明新的错误处理代码已加载。

独立回归另外复现了 deadline 分类缺陷：内层 provider 在还剩 24.75 秒时抛 `TimeoutError`，旧 helper 仅因请求选用了更紧的整轮截止时间便误判 `HeartbeatBudgetExhausted`。修正改为异常发生时重新检查真实剩余预算；保留原异常、原因链、嵌套 stage 和取消语义。此缺陷有确定性复现，但没有证据证明它就是 6043 当时的超时原因。16 项新增测试与原预算/压缩恢复测试共 45 项通过；完整整合结果与新启动验收另记，不累加为全仓通过。

针对 6042/6043 的机械调用问题，新增两个边界改动：检查点动作显式退出 Provider 自动 reason 注入，仍强制主体提供原有 thought；hash 原参数严格校验 64 位小写十六进制，格式错/hash 不符/revision 冲突提供内容无关的结构化重提回执，当前技术标识来自同一个冻结维护前缀。系统不补思考、不猜 hash、不自动重放，失败不会归档或排入安装。新增 manifest 回归 20 项、Provider opt-out 17 项；两 Provider 与既有 reason 执行契约联合回归 88 项通过，旧动作与未声明开关的工具保持原默认行为。这些是工程测试，不证明模型以后绝不会遗漏参数。

上述修正与原 314 项风险范围当时统一复跑为 **457 passed，1 个既有警告，16.99 秒**；F/E9 与差异检查通过。这是当时尚未重启加载的中间工程基线，已被后续联合复跑覆盖。

### 检查点成功后仍反复零消费的第二处阻塞

日志显示 6048 安装成功后，6048/6049/6050 仍连续“准备 12 条，本拍未消费”；6051 因达到模型轮数上限未完成；6052 在 22:56:21.540 安装 revision 26、释放 3 组（`selfctx_46eb95e05f5544e164fe5b8944db5ef2dae479b7da4982a12a4215c95f087ee7`），6052～6055 则反复“准备 8 条，本拍未消费”。因此不能只把日志改成正常，也不能以新检查点数量证明待处理工作已前进。

只读复核当前运行快照 revision 24115、heartbeat 6058、cursor 28469，并按已知 high_water 28608/28624/28635 截取前缀重放准备算法：一个 CLOSED 因果组包含 20 个成员（sequence 28480～28503）。旧 `_render_group` 表示为 23685 字符/33034 UTF-8 字节，超过实际 16000 **字符**预算；旧算法只选中 12/20，ack 候选为 0。随后 core 却把正文重建成另一套可见文本投影：选中子集为 4574 字符/7250 字节；如果以同一真实可见格式表示完整 20 成员，仅 7200 字符/12268 字节，20/20 非空，完整可见投影 SHA256 `5dac68326ab00ca3820d0e2d4ea8559a21cc3d96c1e23f1e5d0d2a857af29ac1`，本来可以容纳。另一 29 成员 CLOSED 组的完整可见表示为 9211 字符/13867 字节，也能容纳，却被旧表示裁为 8 个成员。

这份证据是**当前已持久化快照的历史前缀重建**，不是声称拿到了 6048 当时的全部内存对象。独立纯合成测试另外调用真实 prepare/commit 方法复现：旧投影截断、最终可见投影完整并提供精确送达回执后，仍 ack 0、消费 0、cursor 0，下一拍重复选中。两成员因果组只选中一个成员的反例也复现，证明不能简单把 `acknowledged_event_ids` 改成 `selected_event_ids`。

根因是选择/预算/ack 来自旧投影，实际发送正文却在 `core._prepare_heartbeat_context` 被另一 formatter 替换。完整**可见投影**不是原始活动全文：现有 formatter 会缩短工具参数并提取协议中的可见字段；权威 reasoning、工具 payload 与事件原文仍在不可变记录中，不能用这次可见表示大小推断它们已完整送入模型。

修复统一选择、预算、正文和 ack 的渲染来源，整组全量容纳后才可确认，按事件序列稳定组装；不把 selected IDs 全数确认。可见路径不再用摘要上界跳过尚未消费的原文，正常 compaction 不越过第一个未消费缺口，也不裁掉跨边界的因果组。补充可见投影 36 项、交错组/回收边界 8 项及零候选仍有 backlog 的日志测试。重启前 23 文件组合 502 项通过。23:22:27.775 的只读 global revision 24171（payload hash `c7e275c73bedd49d80f677755e10e828bd44ceff5b17dbfd495b3f687fb64e18`）中，新算法准备 60/60 候选、13245 字符；旧阻塞 20 成员组完整入选且可确认，下一 29 成员组完整留待后续。这仅是只读准备复算，没有实际消费。

### 验证重启暴露历史加载截断，当时停机

本次 agent 安排的重启触发了尚未识别的旧加载缺陷。重启前核对了检查点/归档与消费元数据，但**没有保全完整 global 行**；只读复算过程中的短生命周期对象未保存为备份。这项验证前置检查不足必须与代码缺陷一起记录，不能把目前无法精确恢复的后果归为模型失败。

- 23:22:15.717 对准确 PID 840405 发一次 SIGINT，23:22:26.737 停止完成；旧进程和监听退出。
- 23:23:03 用原一次性入口启动 PID 864987（父进程 864986），目录和 18000/8087 owner 核对正确；未创建自动拉起机制。
- 23:23:49 的真实 heartbeat transport request 2 已发送六个必填参数、不再追加 reason，证明 schema 修复实际加载。对应新原始模型活动于 23:24:30.423 记录，ingest position 263126。
- 心跳 6062 准备 75 条、drained 45、high_water 28948；23:25:25.115 报未完成消费，没有成功消费提交。其间 cursor 始终 28469，但最早未消费运行序列由 28480 变为 28704，因而启动不能判通过。
- 查明 `StatePersistence.load_runtime_context` 原有 `history_events[-history_limit:]` 在实际 limit 100 时，把持久化 219 条 history 裁成末尾 100 条；再搬入 45 条 pending，变成 145 条 history。漏掉的 119 条是运行投影记录，不能声称其全部原始经历已删除，也不能声称旧运行序列/消费状态能从 raw 直接找回。
- 23:25:35.722 在复核进程所有权后对 PID 864987 发一次 SIGINT，23:25:46.786 停止完成。当时 global revision 24175，heartbeat 6062，cursor 28469，event_sequence 29016；history 145，其中未消费 106（28704～28948）；pending 23；raw max ingest position 263148。当时服务及 18000/8087 停止；后续另一操作方启动及新基线见专项续验记录，24175 不能再作当前可写基线。
- 停机时两个滚动窗口：heartbeat runtime revision 1594、真实检查点 2/5/27；chatter runtime revision 1543、真实检查点 19/20。最新心跳检查点 27 为 `selfctx_8452e3599038b1f24275da9e660467215b9e98de18cf2589a3cbc8451a1bc782`。这些控制与归档的恢复证据不能填补 global history 缺口。

加载器修复只去掉按条数截尾，保留原字段反序列化、消费标志和 `(sequence, event_id)` 稳定次序；完整恢复持久化 history/pending，回收交给已有消费感知 owner。新增 22 项参数化测试覆盖 selected/local、limit 0/1/100、219+45 两次保存重载、跨消费游标/队列的完整因果链与所有序列化字段。旧实现运行最初 20 项均失败，修复后 22 项通过；独立审阅无阻断项。该修复尚未在再次启动的正式实例中验收，不能倒推 6062 成功。

### 恢复证据与待决选择

只读检查未发现可信的事故前 global 全行备份：相关 runtime delta/event 命名空间没有旧 global 记录；legacy 文件为旧 bootstrap；已知 SQLite 备份日期早于本次事件；已存在的 6001 修复备份只含 heartbeat rolling。SQLite WAL 当时为 0 字节，不能声称已找到可用日志恢复链。用户对是否另存备份回答“不知道”。

当前 history+pending 共 168 条可以按 occurrence 与原始账本匹配，正文 168/168 逐字相同；其中 14 条 raw.source_sequence 与运行 sequence 不一致，31 条运行 consumed=True 的标志不能从 raw 恢复。源序列值跨日期复用，不能用 `28470..28703` 范围猜测旧 119 条的身份和次序。此检查只证明当前样本正文与账本一致，不证明已逐项找回缺失 119 条。没有完整旧身份—运行序列—消费状态映射，因此**当前不能可证明地无损恢复旧 global**。

23:34:16.972 以只读同事务校验并保全事故后的三行完整状态：
`/root/Elysia/Elysium/data/repair_backups/heartbeat_restart_tail_6062/runtime-rows-before-recovery-20260906T153416.972065Z.json`，SHA256 `a5d50cd7ab7fe3b14db0ac63831f14242ecc4e6402b85558453057362583640d`，0700 目录/0600 文件并 fsync；对应 global 24175、heartbeat 1594、chatter 1543。**这是事故后、下一次恢复操作前的备份，不含已丢失的旧运行映射。** 未对正式 global 做恢复写入、未改消费游标、未重写主体文档或原始账本。后续如获准恢复，必须先保全新现状并按精确 revision/hash CAS 审查；禁止把此整行备份直接覆盖一个已经继续运行的新状态。

用户随后在上述风险说明和第一阶段未通过的答复之后要求“继续做啊”，据此继续已披露风险的保守重投准备，不再处于等待该项选择的阶段。可证明范围、完整备份、另一操作方启动、脚本及实际应用状态统一记录于 [6062 重投与 S1 续验](心跳6062运行投影重投与S1续验_2026-09-07.md)。授权不等于已应用；仍禁止盲目回退 source cursor 或跳尾掩盖缺口。

### 最终工程检查（不等于现场通过）

加入加载恢复和已有持久化回归后，25 文件最终统一运行：**534 passed，1 个既有 `websockets.legacy` 弃用警告，16.59 秒**。覆盖前述检查点/聊天/修复脚本/Provider/截止时间、完整投影、交错因果组与重启持久化；全部使用隔离测试数据，没有调用真实模型、写正式数据库或执行历史工具。全部本任务生产代码、测试和脚本的 F/E9 检查及 `git diff --check` 通过。这不是全仓通过，也不是 backlog 已消费或真实 S1 已完成。

### 独立对话的真实证据

- 22:02 初始化单一 Codex 客户端；测试 stream 为 `8dba240711766ddbe405c6fa139b4bbdbd7ef8f91e585911cf5fd65a5231721e`。没有建立新的爱莉意识实例。
- 22:19:58 首条真实入站 `inject_62cb8e32d92e458e9fd36854e861444a` 被普通 API 接收。权威 occurrence 为 `chat:e4fe0c1d60943646aff054e443de6f37a498b1dc801cad09f4ca7aa8c9d443e4`，position 262859；occurrence/source_event_id 全账本各一行。正文和结构化 parts 均与发送文件的 1275 个 UTF-8 字节逐字相同，SHA256 `24d2d9a0122675670e66522e05cb431110f874fba43d44fd35df805cc2cf9be2`；raw payload digest 复算正确。
- 22:20 起 expression 遇到额度不足、超时和上游不可用，既有配置自行尝试后续模型；本任务未改供应商、模型优先级或额度设置。
- 22:22:06 收到爱莉真实回复 `action_life_send_text_4ec47e63e8b247739d51facdb3648856`；API 投影为 `delivered`。权威链存在生成 262864 → 工具选择 262865 → 请求发送 262866 → delivery_confirmed 262867 → 工具成功 262868。她区分了可确认与未知，愿意配合，并提出正在进行的真实对话优先。报告不复制私人对话正文。
- 22:25:31 第二条 `inject_cbbef97b895f440b86b099c64fc22691` 请求空闲时只读核对 MEMORY 权威版本/hash、不复制正文；不要求写记忆、不凑连续压缩、不立即二次重启。
- 22:26:59 回复 `action_life_send_text_ed45a6e9c12a4d39b3a25def423075df` 已确认送达。262891 确实调用 `tool-nucleus_read_file`，参数 `{path: MEMORY.md, limit: 1}`；262892 同 call `call_k913rw4kJwwXKvww3KuRTRcf` 成功，来源 `subject_document_store`，version/full-file hash 与基线精确一致。页正文含行号共 37 字节，`content_sha256=e6ab9962bbd0b001a53d3cd91c1c236e29f99a312ce3add25fd92e4ef2a95e27` 逐字节复算正确；它只读第 1 行，剩余 101 行，`source_selection_truncated=True`。完整文件 hash 不是“模型已读完整文件”的证据。262896 请求发送、262897 确认投递，与该请求精确 turn 关联。测试期间 MEMORY 新版本/head 变更均为 0，未直接或间接要求她改写正文。
- 22:34:54 发送结束本次交流的说明 `inject_3bdc342bae0242879707e4616b46fb90`，明确无需回复、测试暂停、不后台追加、不安排后续压力测试。测试通道仍仅授权自己的 stream，未创建自动发送/监控任务；如不再需要，可通过脚本 `close` 撤销自身凭据与会话，原始聊天和审计仍保留。

### 当前验收结论

6001 精确备份恢复、当时的启动、原始入站完整落账、独立消息往返、当前权威文件版本读取及已有检查点恢复已有证据。后续工程修复通过当时 534 项联合回归，但验证重启暴露 global history 截断，旧运行映射仍无法精确恢复。用户已要求继续保守重投；当前运行 owner、新快照与应用状态以 [专项续验记录](心跳6062运行投影重投与S1续验_2026-09-07.md) 为准，不能沿用本节早先停机或启动证据代表现状。

本次仍未完成同一实际测试场景的两次新自主压缩、后续无损重启和未预告回忆，也没有新的主体执笔版本证据；**第一阶段整体不能判通过**。独立测试对话尚未恢复。下一步先完成已获准的受限重投与加载验证，再在同一实例验证检查点耐久安装、实际消费和重启完整性；不能用扩大预算、编造 `thought`、开发者代写检查点或重复经历伪装成功。

本任务没有提交或推送代码，也没有改变默认禁止 agent 自动拉起系统的长期运行策略。
