# 滚动上下文与注入审查

> 日期：2026-09-03
> 状态：派生滚动投影已修复。未改主体文件、未启动 Elysium。
> 范围：表达层滚动 payload、本轮 suffix 注入、心跳潜意识投影、工具结果回注。

## 结论

三层不是同一种东西。suffix 本轮注入是干净的；问题在跨轮滚动链。现已只改派生投影：

- 退役工具名同时认无前缀、`tool-`、`action-`；
- `inner.return` / `outreach.begin` 信封本轮仍在 rolling，进入 `WAIT_USER` 和快照序列化后移除；
- 每次模型请求前对活链做退役工具剥离，不必等重启才看见干净投影。

| 层 | 身份 | 状态 |
| --- | --- | --- |
| Prefix（SYSTEM） | SOUL / USER / MEMORY 投影 + `TOOLS.md` + 静态工具说明 | 退役工具名干净；`TOOLS.md` 是她自己的习惯本 |
| Rolling（跨轮 USER/ASSISTANT/TOOL_RESULT） | 持久对话链 | 派生投影已规范化；权威轨迹不动 |
| Suffix（`<transient_life_context>`） | 本轮才挂上、发送后剥掉 | 设计正确，未改 |

## 已做

- `_expand_llm_tool_names` + 统一 `_derived_rolling_payloads`
- `_without_internal_wake_envelopes`：只认 `<new_messages>` 里 sender_id 位置的 `life_engine_inner_return` / `life_engine_initiative`，不扫回声正文
- 链完整性：信封 USER 前若是纯 `__SUSPEND__` 则一起删；否则必要时留下 `[internal_wake_envelope_omitted]`
- 模型请求前 `strip_wake_envelopes=False`；`WAIT_USER` 与快照 `True`
- N.E.K.O 实时指导不再点名已删的 `think`

## 未做

- 不改心跳 delta 的完整 TOOL_CALL 正文（那是本拍潜意识活动投影，另案处理）
- 不改 `dfc_message` / `autonomy_intent_*` 的 event type 或摘要标签
- 不改 SOUL.md / USER.md / MEMORY.md / TOOLS.md
- 不启动、不重启 Elysium

## 运行生效

Python 改动需用户手动重启后进入活进程。未重启前，旧活链仍可能带着历史 `tool-nucleus_*` 和已结束的叫醒信封。快照读写路径在重启加载时会规范化。
