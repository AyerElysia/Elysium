# MiMo 小米渠道现场（2026-09-08 05:30 CST）

只读诊断。未改渠道、未重启 Elysium / New API。临时探测脚本已删除。

## 结论

小米渠道 **可用**。`mimo-v2.5` / `mimo-v2.5-pro` 现在只走 New API channel 34（`MiMoCN-2`，上游 `token-plan-sgp.xiaomimimo.com`）。渠道启用（status=1），`auto_ban=0`，无 header override。OpenCode 渠道 48 的 `models` 不含这两个名字；钉住后 Elysium 日志里 **0** 次 MiMo `MissingSessionID`。

它不是“永远 200”。小米仍会短时 `429 Too many requests`。探测连打几次后立刻 429，约 25 秒后恢复 200。这是上游限流，不是渠道挂了，也不是又打到 OpenCode。

## 证据

| 项 | 结果 |
|----|------|
| 钉住脚本只读 | OpenCode 无 MiMo ability；Xiaomi ability 仅 channel 34、enabled、priority 100 |
| New API | `active`，PID 198784，`:3000` 监听 |
| 近 24h 成功消耗（type=2，均有 completion） | channel 34：`mimo-v2.5` 104 次（最后 05:14:44）、`mimo-v2.5-pro` 355 次（最后 02:42:48）；channel 48 的 MiMo 消耗最后停在 09-07 12:21（钉住前） |
| 近 24h New API 日志 | 无 MiMo 429/error 正文（429 不记账） |
| 现场探测 05:28 | 两模型均 HTTP 200，消耗落到 channel 34 |
| 紧随其后的连打 | 三连 HTTP 429 `Too many requests`，无消耗行 |
| 05:30:12 单次重试 | HTTP 200，channel 34，`mimo-v2.5` |
| Elysium 钉住后（00:11 起） | MiMo `MissingSession` = 0；`LLMRateLimitError` 32 次，最后一次 02:43:22（heartbeat / reflection） |
| 近 24h Elysium | 无 `Weekly usage` 字样；钉住前的 Console Go 400 最后一次在 00:03:31 |

短探测里 `finish=length`、可见 `content` 为空、但有 `reasoning_content`，是思考占用了 `max_tokens`，不是渠道错误。生产消耗里 02:42:48 的 `mimo-v2.5-pro` 仍是 89063/122 tokens、channel 34。

## 使用含义

- 路由正确：请求 `mimo-v2.5*` 会进小米，不会进 OpenCode。
- 可以正常当候选模型用；并发或短间隔连打会被 429，Elysium 会跳过冷却并走下一个模型。
- 不要为了“打通 MiMo”给小米渠道加 `x-opencode-session`，也不要把 MiMo 加回 OpenCode。
