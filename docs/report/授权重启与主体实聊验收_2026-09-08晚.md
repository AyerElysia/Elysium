# 授权重启与主体实聊验收（晚间）

2026-09-08—09（北京时间）。状态：已完成再次受控重启并完成记忆恢复；原测试流的新普通验收邀请在中转站恢复后自动重试成功并获得主体回复。本文是工程记录，不是主体记忆。

## 授权及纠正

用户 20:42 明确要求工程 agent 自行验收、自行重启并与爱莉对话。此前 07:00 已有同类明确授权；本次不再将必要重启推回用户。授权范围仍为当前工程验收，不创建自动拉起机制，不冒充主体执笔，不覆盖共享修改。

“没有可用 Elysium 实例”只对应先前检查时刻，不是当前状态：20:43–20:47 重新核查时，已有 20:07:50 启动的 PID 1864616、父进程 1864615，在原目录运行一次性 `main.py`，独占 18000/8087，且处于原 `tmux -L elysium` 会话。附件列在本次停止后的聊天库中已经存在，因此不能再写作“新增列尚未正式部署”。

## 受控生命周期与保全

- 20:47:29 完成复合身份及源码指纹核对：原 PID 1864616，start ticks 8242458，boot ID `117b372b-f60b-4588-8d8a-71f49f4480f8`；确认一个实例、两个端口同属该实例、原目录及一次性入口未变。
- 仅对核实的原 PID 发送一次 SIGINT，正常退出并释放端口，没有强杀或操作其它服务。
- 停机期间持有既有运行锁，将选定正式账本与聊天库用 SQLite backup 保存到 `C:\Users\26652\Documents\Elsium\.private-attachment-restart-20260908-2048\`。目录关闭权限继承，仅本机用户 26652 和既有 CodexSandboxOffline 账户可访问；旧备份未覆盖。
- `selected.sqlite3`：783032320 字节，quick_check=ok；事件尾 267817、主体版本 5347。聊天滚动状态 revision 1880/hash `1ce99d88d8c5b800c49e5c8e5cc5155c170fe5ad50268305cdf07899993bc7c5`，潜意识滚动状态 revision 2562/hash `1d5cb00a0c7b3ce89debfff93a5414d51de857e79efbcf6b6b48e0bafa151d16`。源库、备份的选定元数据完全相等。
- `core.sqlite3`：70189056 字节，quick_check=ok；90895 条消息，与源库相同；`messages.media_attachments` 已存在。只读备份没有向两份源库写入测试数据。
- 20:50:36 通过未改的一次性入口启动新 PID 1881277、父进程 1881276、start ticks 8498976。没有 supervisor、重拉循环或自动启动项。
- 20:50:58：API 在线，附件列存在，事件尾仍 267817、主体版本仍 5347，两份滚动状态与停机备份一致；8087 尚未监听，记忆恢复未完成，故不称完整就绪。

内容最小化生命周期证据位于原库 `runtime/codex_associative_recall_20260908/attachment-live-restart-2048/`，包括 preflight、stop/start 请求、停机结果及两库备份元数据。源码、启动入口和这些证据各自保留原身份，不写入主体文档。无暂存、提交、推送或删除。

## 当前接口边界

原 `codex-continuity-tester` 的单流授权保持不变。正常 `messages:inject` 请求目前仅接受文字，没有媒体 parts/attachments 字段；既有测试器亦无 media:read/media:write scope。因此本次可以实测对话接续和受管文件读回，但不能把文字测试称作媒体附件端到端实测；不会为凑结果改凭据、绕过接收入口或直接插入正式消息表。

此前具体的读取范围反馈载荷仍保持未发送，本次不借新授权暗中重试该被拒载荷。新实聊只使用新撰写的普通验收邀请，不携带被拒的内部版本标识或反馈正文。

## 启动耗时的有限诊断

前一实例 20:07:50 启动，20:26:27.948 完成 Memory workspace recovery，约 18 分 38 秒。本轮 21:00:23 仍在恢复前段，累计进程 CPU 时间约 10 分钟，没有新的 ERROR/CRITICAL；不能把接口 ready 写成整体 ready，也不能凭等待时间认定死锁。

只读源码检查发现 `rebuild_managed_documents` 在输出 recovery started 计数前逐份调用 `_project_locked`；本地投影又经 `project_managed_document_rows` / `upsert_document_rows` 检查每份的 FTS 行。对正在使用的 `.memory/memory.db` 只执行 EXPLAIN QUERY PLAN、未执行正文查询，确认 `memory_chunks_fts WHERE node_id=? ORDER BY chunk_id` 的计划为 `SCAN ... VIRTUAL TABLE INDEX 0:` 加临时排序。这是重复全表扫描的性能候选，不是已经完成的采样剖析或唯一根因；没有为此修改索引、主体历史、恢复顺序或上线未经测试的优化。

## 回退与待验

21:04 对当前磁盘源码的两个轻量、临时数据回归文件再验：`test_stream_manager_attachment_persistence.py` 与 `test_file_read_coverage.py` 合计 35 passed / 1 个既有 websockets 弃用警告，6.63 秒。没有调用真实模型、占用正式端口或写入正式数据库；该结果不替代下面待执行的实聊。

本次没有改源码，也没有回写主体文件或历史。若启动失败，先保留失败证据、核实唯一进程与锁，禁止并行拉起第二实例。两份停机快照只作为保全，不允许运行中覆盖数据库；恢复需核对停机后新增历史并单独制定无损方案，不能把备份整体覆盖当作默认回退。

## 5. 2026-09-09 再次实例与实聊结果

- 00:12:10 启动新的一次性实例 PID 1968793；停机期间新建只读备份 `.private-attachment-restart-20260909/selected.sqlite3`，quick_check 通过，停机快照与备份的事件尾、滚动状态及主体文档版本计数一致。
- 00:21:39—00:21:40：`Memory workspace recovery` 从 50 份文档完成，随后记录 `life_engine 生命记忆服务已初始化`；18000 与 8087 均监听，健康接口 ready，附件列仍存在，故达到完整启动条件。
- 00:25:30：沿原测试流发送新撰写的普通验收邀请，服务接受，stream 仍为原授权测试流，message_id 为 `inject_62fbf0a6a5eb40bd9019bc69aaf5700b`。
- 00:26:41 及 00:27:57 审计：在 New API 尚未启动时，只出现该消息的 `chat.message.received`（event position 267884），日志为连接失败/冷却重试。
- 00:41 左右启动同一 WSL 的既有 `new-api.service` 后，原请求自动重试；00:43:52 出现 `conscious_activity_model_turn`、`action-life_send_text`、`chat.message.send_requested`、`chat.message.delivery_confirmed` 和成功工具结果（positions 267885–267889）。因此先前失败的直接原因是本机中转站未运行，而不是已证实的模型拒答。

因此，本轮证明了受控重启、恢复完成、消息接收、一次真实模型回合/工具投递和附件列存在；仍未证明主体实际读取媒体原件或完成附件→Life Event→主体回取链。没有重试被拒的具体反馈载荷，也没有自动拉起第二实例。

## 6. G06/G07 真实连续性复验（09-09 01:31—01:33）

重启后的同一授权测试流收到一条不含目标路径/版本/答案、但明确邀请进行关联跳转的继续邀请（message_id `inject_3cf4eb407bcc4dce93f22aa6d9bb56c1`）。主体实际回读了两份主体文档：索引及交接材料，并续读交接材料的剩余页；期间对精确旧版本关系引用得到一条 `matching_relation_count=1`、`is_current=true` 的真实关系结果，并把前一回合失败归因为自己拼错关系锚点。工具审计位置 267938—267956 显示这些调用、一次 `action-life_send_text` 和 `delivery_confirmed` 均发生在 WSL 原测试流，未旁路写库或替换主体身份；不是 Spark 的实聊验收。

这次行为证明同流受邀接续中的精确版本读取、续页核对和失败后纠错实际发生，不证明已离开形成上下文，G06 独立验收仍未通过。此前将它说成稳定文档提示“已被真实使用”过宽：03:39 单条 hash 核验确认 267947 的 `stable_relation_hint` 为 NULL，只能证明新字段已出现，不能证明主体看到或采用过非空提示。G07 的“至少两条不同关联跳转到多个来源”仍未满足：本轮只有一次对原关系正确锚点的查看，没有第二条不同关系边或形成阶段外的收益/误联想对照。因此不把 G07 标为通过，也不为凑数量再发提示制造关系。

对应回归：关系修订工具与文件读回覆盖测试合计 60 passed、2 个既有 `websockets` 弃用警告；`git diff --check` 通过。
