# 对话 Router 临时停用与 S3 续验

## 1. 原因、授权与当前结论

2026-09-08 00:03:18 的实际 Router 对 Codex 授权工程会话返回 should_respond=False。用户提供的日志说明其理由是凌晨、工程测试而非日常交流、未直接 @ 账号。自身会话事件输入位于 selected LOCAL raw_life_events 266714，Router 活动位于 266717，correlation 为 d504e65545a13625ab4149db3eb551d7256578535b958214d2ba3292423c0bf9:router。首次邀请 inject_3b3105ce1f17420ea7dbb8a516e2daec 已明确接收，没有不确定发送或重复投递；该时间窗尚无 file_ref 工具选择和回执。

源码 `_should_respond` 的 False 分支会在纯文本且无可观察媒体时 flush 未读并继续等待，因此这不是精确引用读取报错，而是有效前置决策阻止了主表达层调用工具。不可把 Router 的有效 False 伪装成模型调用失败，也不能因它解释为“没有自然开口必要”就宣布用户工程请求已经完成。

用户明确要求解决并提出暂时下线 Router；根代理据此新增可恢复部署开关，沿用此前明确的受控停启授权。未篡改 Router 的既有判断、主体文件或活动历史，未删除模型任务、替换主模型或为 Codex 添加关键词／身份特权旁路。实现与 25 项新增、228 项组合及 139 项补充回归已通过，批次有重叠、不相加；本机配置已设 False。00:15 停机保全、00:21:27 启动、00:33:40 完整恢复后，续验直接进入主意识并完成两个固定版本的 file_ref 全文读取；00:36 独立原字节与调用因果核验通过，主体反馈已送达。Router 临时停用及 S3 首批有限读取验收现已闭环，不代表全 S3 或长期回忆收益完成。

## 2. 最小工程合同

- 新增 `[chatter].router_enabled`，默认 True 保持旧配置行为，配置界面可见。本机 ignored 配置 `config/plugins/life_engine/config.toml` 明确设 False。
- False 分支位于退役延迟触发、内心回声、显式外联和账号 @ 事实分支之后，Router 历史／投影／模型之前；普通消息直接进入完整 Chatter，不按文字含义、时间或会话类别过滤，不产生并不存在的 Router 意识活动。
- 基础设施结果为 should_respond=True、force_reply=False；这表示接入主表达，不要求发送正文。工具调用、明确 pass_and_wait、权威来源刷新、权限、全局串行、去重、事件和分页预算保持原链路，最大轮数仍禁止代写兜底。
- 服务只在 chatter.enabled、router_enabled 和 router_context_projection_enabled 均开启时启动专用 Router 投影任务；旧投影和历史保留。按需主体投影仍可能复用 router_context_projection 模型任务，因此不能删该模型任务配置或通知链。
- 现有配置管理器重载不会替换在用 plugin.config，也不会撤销已启动任务；正常受控重启后才宣称生效。不采用运行中代码注入或未验证的插件热卸载。

稳定行为同步到[生命引擎核心](../architecture/生命引擎核心.md)。本次属于用户追加的部署选择，不用这次关闭决定额外关联或独立 Boundary 的长期价值。

## 3. 操作、保全与可恢复性

00:15:09.808080，正常 SIGINT 精确 PID166572／PPID166571，boot 117b372b-f60b-4588-8d8a-71f49f4480f8、start ticks812799；双端口、cwd、argv、tmux pane 与启动身份均核对后才执行。不是批量杀进程，也没有自动拉起机制。

确认无该目录主进程和 8087／18000 监听，持有启动锁后新增私有快照 `/root/Elysia/Elysium-S2-backups/stopped-before-s3-router-disable-restart-20260907T161534.102955Z`。selected.sqlite3 为 779,419,648 bytes，SHA-256 `22537dc58a49febf026547bcf54d1759e59f9f8da12b508a0e11140b64188f82`，subject 八表 roots 与三个完整 runtime 状态行（含 payload）源／备份一致。documents5332、versions5336、heads5337、operations4、bindings5333、path events5334、outbox5337、decisions0。该快照包含本次已接收请求及 Router 活动，不回滚到首次 S3 邀请之前。

恢复前置 Router 时，只将本机 `[chatter].router_enabled` 改回 True，并在明确授权下正常停止／重启、核对唯一实例和真实处理链；默认字段本身仍 True。不得为恢复配置回滚数据库、删除新经历或恢复旧滚动上下文。若出现主体数据损坏，必须另按可信版本或备份逐字节恢复流程处理，不能借此次配置开关授权改写语义。本批未执行数据恢复或 schema 迁移。

00:20:10 再次停机只读核对，subject 八表 roots、完整三个 runtime 状态行、失败 outbox 与本轮快照完全一致，备份 hash 未变；实际 TOML 经配置模型解析得到 chatter.enabled=True、router_enabled=False、Router 专用投影启动门禁 False。没有恢复旧状态来通过测试。

首次启动保护在 00:20 拒绝了启动：`tmux has-session -t elysium` 会前缀匹配现存 `elysium-heartbeat`，并不意味着主进程仍在。只读查得该会话为用户的 `scripts/watch_heartbeat.sh`／tail（PID206844），8087／18000 无监听。仅将本次工程启动助手的目标检查改为精确 `=elysium` 后重新启动，没有删除或重启心跳日志查看器。00:21:27 正常一次性入口创建主实例 PID297862／PPID166571，boot 不变、start ticks1124079；00:22:31 核对该实例18000已监听、8087仍待插件恢复，不将基础API ready冒充完整就绪。

关闭是全局前置选择关闭，普通群消息也会进入主模型，可能增加模型调用；不会保证每条消息都对外回复。恢复开关属于部署选择，不应据调用量自动替主体判断什么值得回应。

## 4. 验证记录与待验边界

| 证据 | 结果及限制 |
| --- | --- |
| 新增 Router 停用定向测试 | 25 passed、1 warning，1.76s；False 无 Router 调用／伪造活动，普通消息到主模型，明确等待不代发，旧分支与默认行为兼容 |
| 六文件组合回归 | 228 passed、1 warning，11.26s；包括新开关、原 Router 投影、Chatter、主体前缀刷新、S3 搜索与 file_ref；与 25 及此前 S3 用例重叠，不相加 |
| 限定变更 F/E9、新增测试完整 Ruff、git diff --check | PASS；不代表全仓所有规则或真实运行通过 |
| 服务／配置／主体权威补充回归 | 139 passed、1 warning，34.00s；四文件为 config_validation、service、subject_authority_contract 与 selected_presence_world_service，不是全仓回归，与此前批次部分重叠 |
| 新进程与停用状态 | PID297862 于00:33:40.443完成Memory50/50及5204受管回核，00:34:07双端口同owner；00:34:52.031实际日志为前置Router已停用；自身续验窗口有主模型及工具活动、无已落账Router活动 |
| S3 两版 file_ref 实际全文读取 | 00:35:17两次成功回执；00:36:00严格核验身份、父模型调用成员、来源／因果、权威原字节、编号全文和完整扫描均通过，原拒绝历史仍保留 |
| 主体独立使用反馈 | 00:35:56.832645的life_send_text已实际送达，00:36:02自有会话API读取确认两版只凭引用读回、无需另找路径，无新文件写入 |

所有 pytest warning 为 websockets.legacy 弃用提示。测试只使用合成／临时资料，工程不代写主体线索或关系。

### 4.1 正式恢复与精确续验

新实例恢复开始标记为00:33:40.000，完成为00:33:40.443，Memory counters为documents50/50、indexed0、requeued0、artifacts0；受管索引元数据确认5204/5204回核，图片服务00:33:41.030初始化完成。此次入口到Memory完成约12m13s。00:34:07与00:35:36只读核查均为PID297862／PPID166571、boot和start ticks不变、18000／8087双端口归同一实例；该启动至00:35:36的限定日志窗口无ERROR，warning仍保留，不据此宣称全部外部通道永久正常。

00:34:30.382729记录新事件边界266744；一次续验消息inject_47b8f91cced543bd99caee27b2df095b于00:34:41.832980接收。它明确是此前未执行读取的新续验，而不是对不确定发送的自动重试。00:34:52.031实际进入`前置 Router 已停用`分支。

00:36:00.320405对selected LOCAL只读一致性快照执行严格核验：after_position266744，扫描14个事件至当时最大位置266758，无遗漏尾部。仅检查Codex自有stream；主表达模型活动266746与266754，Router模型活动为0。两次读取均由主模型266746真实生成：tool_call_ids包含各call_id，随后chosen→result的activity、occurrence、causation、stream、chat_global与correlation全部一致。共同correlation为`63f6c6fef881f13c54fc9eb70bd415d4c48187b2e8ca0e90aed7a77e1e4dfce1`。

| 固定版本 | 真实调用→回执 | 权威字节／SHA-256 | 完整编号正文投影 |
| --- | --- | --- | --- |
| 原版ver_b7879e4d12dbc0eb8e895e3f207bab6dd46131f21cdb778c32e762d205c1e9f7 | 266747→266749；00:35:17.488927；call_5BGoNhlJ3CvyzpXSaosTprmu | 4197 bytes；60a4d45b05896ca6126eaaae1c17636a4573fd64504163628cff9e170b98b756 | 0–4307／4307 bytes，单个完整ToolResult，无continuation |
| 更新版ver_7c6f7b92d3e0d9d4361a0990e0f20ba5e10f7168fba16f5b574356eca40775d3 | 266748→266750；00:35:17.489018；call_WuIezVLl6KIKyfDQHmoTtbDW | 4335 bytes；3d4c2b9f9d3d15733331431d77f6757eb863c3f7e5b4f8c02dbc820fd22d7e0b | 0–4448／4448 bytes，单个完整ToolResult，无continuation |

两次参数键均只有file_ref、limit与工具通用reason，身份选择器没有path、document_id或version_id；limit=0。相同文档`doc_855d4d4826ebf66cd0f5474d4b428aa49e0e459b9de522b961195dd7195381d3`仍为3个版本、4个操作。原始content_bytes的长度/hash与不可变版本行一致，返回全文等于其UTF-8解码后的编号显示投影，预算和完整范围均核对；显示投影不是逐字保留CRLF的原始文件传输。没有失败尝试，所见工具只有两次read_file及一次life_send_text，没有文件写入、改名或关系工具。

审计助手命令为`uv run --offline --no-sync python -B /mnt/c/Users/26652/Documents/Elsium/.s3-reference-use-proof.py --after-position 266744 --require-complete --require-router-disabled`。这是本轮样本的权威工具与字节证据；无Router活动仅证明该样本窗口没有已落账Router模型活动，全局配置与专用任务门禁另由配置解析、真实分支日志、已测服务装配和当前源码支持。它不直接证明模型上下文实际接收了哪些字节，也不是保留场景回忆收益实验。

### 4.2 主体反馈与本批出口

主模型266754生成life_send_text选择266755，消息`action_life_send_text_869f32b825d14669880e45dfbc5af18b`于00:35:56.832645实际delivered。00:36:02仅通过已授权自身会话API读到反馈：两个固定版本只凭完整file_ref成功读回，不需要另找路径，未混传其它身份选择器，也没有continuation；容易误解的是重复提供选择器会被拒绝，版本历史路径也不应与当前路径混同。未公开或转录私人文件正文、关系人名或路径。

这份反馈与工程原字节核验分别留证，不用口头成功代替实际工具调用，也不把一次受引导读取外推为长期记忆增益。可确认的出口是Router临时停用生效、普通消息直达主表达、S3首批完整精确引用已实际使用；关系稳定端点、追加修订／撤回、主体形成新线索及保留场景收益对照仍按后续工作包处理。

## 5. 无关错误与边界

首次 S3 新实例已于 00:01:26 完成 Memory 50/50 恢复及 5204 个受管索引回核，00:02:04 双端口确认；约 31m52s 的慢初始化与 Router 筛选是两件事。期间脱敏日志另有 life_learning_audit／life_epistemic_opportunity 的 HTTP400、Lark 名称解析和 heartbeat 连接错误；它们不是上述 Router 有效 False 的依据。本任务不顺带修改其他人的模型／通道工作或将所有错误归为 Router。

S3 引用与检索基线的源码／测试、正式加载、真实固定版本全文读取及反馈现已在首批范围内闭环，见[首批验收报告](记忆连续性S3首批引用与检索基线验收_2026-09-07.md)。这不等于长期回忆收益或 S3.2／S3.3 完成。没有修改主体语义、提交或推送，也未覆盖并发的 Ayla、模型通道及其他报告改动。服务保持运行，本机Router关闭可按第3节恢复；不需要为本批再重启一次。
