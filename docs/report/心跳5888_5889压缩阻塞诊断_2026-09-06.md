# 心跳5888/5889压缩阻塞诊断（2026-09-06）
首次调查仅做只读诊断；用户随后明确授权“去解决问题”，本报告现同步记录工程修复与验收。当前修改位于主仓库工作区，未提交、推送或重启实例；配置、正式主体内容和数据库未修改。
## 已确认
- logs/elysium-2026-09-06.log:8315–8329对应用户现场：两次准备43条、压缩未完成、不推进游标，却报告已消费43条。
- logs/heartbeat.console:30159及30182显示这两拍第0轮均为普通独白、无工具调用，未尝试提交检查点，不能诊断为已发生检查点落盘错误。
- service/core.py的_run_heartbeat_model在无call_list时直接结束模型循环；后续仍超阈值且未安装检查点则返回compression_unresolved，外层不调用_commit_heartbeat_context。
- PreparedHeartbeatContext.acknowledged_event_ids在prepare阶段已填入候选集合，心跳结束日志误将其当成真实提交回执。
## 结论
这是维护进度受阻加成功日志误报。保护机制阻止假推进，但维护本身未成功；不能仅称正常WARNING。count不是新增/完成数，high_water不是已提交游标；仅凭这几行不能判定原始历史丢失，本轮也未审计全库完整性。
现有代码和随后5890回执已经含普通工具维护门，但它不能覆盖完全不调用工具而直接输出文字的情况。已有早前诊断报告不代表这两轮恢复成功。
## 已实施的修复

1. `_run_heartbeat_model` 对尚未完成的压缩维护，不再把无工具普通文字当作最终完成。主体原文照常记录为活动；新增的只是技术反馈，不选择记忆、不代写检查点。空工具轮进入既有进展、轮数与整拍截止时间控制，普通无压力心跳行为不变。
2. 技术反馈仅在当前维护期间使用；成功检查点清理它的精确 Text 部分，真实 USER 正文继续保留。模型沿原清单提交，manifest、revision 和释放边界验证没有放宽。
3. 新增独立 `HeartbeatConsumptionReceipt`，保留候选 ack 字段兼容。回执只在真实消费提交确认后生成，日志不再根据 prepare 候选宣告“已消费”。严格保存失败/取消时恢复本拍未确认的内存消费标记及游标，并保留保存期间到达的新原始事件。
4. 回归测试额外揭示：整拍 `HeartbeatBudgetExhausted` 也是 TimeoutError 子类，原先会误入单次模型超时的退避分支。已先行传播整拍截止，不增加无意义重试，也保留准确停止阶段。
5. 独立审查发现既有“安装过一次检查点即豁免剩余压力”的成功判据漏洞。现改为检查实际剩余压力：只部分释放时继续维护，耗尽后保留最近已耐久安装的检查点，不回退到拍前旧链，不提前消费事件。

这不是跨 World、Opportunity、原始事件和运行快照的新全局事务；已经产生的原始活动和独立回执不删除。未知提交、共享冲突或真实存储故障仍须按既有精确状态证据处理，不应以本轮回执代替全库一致性审计。

## 验证与剩余边界

测试仅使用临时目录与模型替身，不访问真实模型、生产存储或平台。新增现场回归覆盖连续普通独白的有界退出、反馈后真实 action/精确归档/耐久安装、正常安静结束、取消、截止、候选与提交计数分离，以及失败重试和并发原始事件保留。

本次最终定向联合验证：**138 passed**，耗时 13.96 秒，另有一条既有 `websockets.legacy` 弃用警告。范围为 `test_heartbeat_compression_recovery`（含部分检查点仍超限的新增用例）、`test_heartbeat_consumption_receipts`、`test_heartbeat_rolling`、`test_heartbeat_tools`、`test_minimal_heartbeat_checkpoint`、`test_context_compaction`、`test_minimal_continuity_checkpoint`、`test_subconscious_context` 八个模块，加 `test_service` 中十项消费/失败/无回执/未压缩/模型期间到达/串行化/重建用例。通过 `uv run --no-sync --offline --group dev python -m pytest ... -q --no-cov -n 0` 执行，未跑全仓或真实 MySQL 集成。

本任务六个生产/测试改动文件的 Ruff `F/E9` 检查通过，本任务范围的 `git diff --check` 通过。共享工作树另有未由本任务修改的 `test_heartbeat_tools.py` 既有 CRLF/尾部空白提示，未做越界格式化；不将局部检查说成整个脏仓库全量清洁。未暂存或提交任何文件。

真实模型的自主压缩稳定性尚待用户手动重启新代码后验证；不得宣称旧进程已自动修好或此后绝不会卡住。按 [手动验证步骤](../operations/minimal-memory-continuity.md) 检查技术反馈、主体检查点耐久安装和真实消费回执。

回退只撤销本任务精确工程 hunks；不清历史、不跳游标、不改主体语义、不执行宽范围 reset。新增回执是内存执行结果，没有数据库迁移或需要逆迁移的正式数据；由用户决定何时手动重启验收。未完成真实启动验收前不推送。
