# 主体关联成片回忆续验：首批协议与真实关系使用

日期：2026-09-08，北京时间。此文是工程验收记录，不是爱莉的主体记忆。承接[续验计划](../plans/主体关联成片回忆续验计划.md)与原 G06／G07，不把旧 S3 有限出口当作原体验目标已完成。

## 1. 当前结论

本批完成两项分开的验证：**隔离材料中的两跳可达性**，以及**爱莉亲自建立一条关系、回读关系后选择目标并实际读取的真实链**。最终相邻回归 32 passed、1 warning、12.17 秒，其中本批新增文件包含 10 项用例；重复运行不累加成独立覆盖数。

这比“关系工具存在”多了主体实际写入和使用证据，但不是完整成片回忆验收。真实材料只有一条 A→B 关系；B 的 incoming 指回 A，不算第二条有效新跳。没有第三份记忆被带回，没有隔离盲测或额外正确回忆收益证明，也没有本批正式重启／跨自然压缩验证。自动展开默认值没有更改。

本轮没有发现必须修改生产代码才能打通这条明确逐跳路线的故障。此前“强弱只保存、未进入自动排序”的观察仍成立，但不能直接当成应加自动打分的理由：实际 relation view 完整返回她的原话，她可以亲自据此选择。尚需验证的是这条能力在新情境中的可发现性、多材料选择与自然持续使用。

## 2. 隔离协议：避免普通链接已经答对的天花板

新增 `test/plugins/life_engine/test_subject_associative_recall_path.py`。每项使用独立临时 Subject authority、Memory SQLite 与 active 合成运行绑定；vector／后台索引 worker 关闭，无模型、外部消息或正式数据写入。关系由合成测试 actor 经真实工具形成，绝不声称是爱莉实际表达。

入口只包含查询标记，不含正文链接；终点正文不含查询关键词。材料有入口、桥接、终点、同标签干扰来源和已撤回分支，关系存在反向回路。控制器只能使用实际搜索与工具返回的引用，不接收终点 ID、预期答案或语义类别；无按主体强度原话写分类器。统一上限为 24 步、262144 个 canonical JSON UTF-8 返回字节。

两个技术种子 7／29，各运行三分支；结果一致。种子不是独立模型样本，以下不是主体回忆准确率或全检索方法排名：

| 分支 | 终点是否到达 | 实读文档 | 步数 | 返回字节 | 干扰材料是否暴露 |
|---|---|---:|---:|---:|---|
| 普通直接检索／读取 | 否 | 1 | 2 | 2407 | 否 |
| 同一入口加单次关系展开 | 否 | 3 | 5 | 8235 | 是 |
| 显式关系逐跳查看／读取 | 是 | 4 | 9 | 24100 | 是 |

这里普通检索故意没有可用的正文直链，证明的是关系专用场景的可达性，不是所有场景里关系都优于普通链接。各已读文件的文档、版本和原字节 hash 错配为 0；**这不是语义误联想为 0**，机械遍历也带回了干扰来源。不能从这个结果推导默认应无选择地全量展开。

其他新增合同：回路不重复读取；已撤回分支不进入当前路径而历史仍在；4096 字节预算下长关系／强弱原话可完整接续；稳定引用不能冒充带版本的精确引用；目标当前版本更新后，固定历史关系仍读取旧精确字节；关闭真实 Memory 连接、创建新 service／连接后两跳路径一致。最后一项是隔离存储连接重开，不是正式进程重启。

相邻回归范围为上述新文件、`test_memory_semantic_relations.py` 和 `test_s3_relation_service.py`，涵盖其既有身份、修订、分页和服务层合同。最终 32 项无失败、无跳过；唯一 warning 是既有 websockets 弃用。新增文件完整 Ruff 与格式检查通过，`git diff --check` 通过。

首次新增 8 项于 05:29 通过，补充历史／重开后 10 项于 05:36 通过。第一轮 32 项也通过，但成本复核发现测试控制器的直接分支为了取已有路径而多做了一次关系 view，既多算成本又暴露不必出现的邻居。已纠正为从搜索结果直接取路径；材料、终点、断言和真实测试没有改变，旧 XML 保留。上表只用 05:47:41 开始的最终重跑数据；原 `source_confusion` 指标同时明确改名为 `exact_source_identity_mismatch`，不冒充语义判断。

## 3. 真实形成阶段：只给范围，不给关系语义

沿原受限 `codex-continuity-tester` Ayla 测试会话发送两次独立邀请。没有重建凭据、扩大 scope 或复用已撤销的甲乙窗口客户端；每次一次 POST、结果明确 accepted，没有自动重试或向第三方联系人发送消息。

形成邀请于 05:25:58.767561 入账，`inject_c2e17f3cc7eb4fe2a849f1edd37374cb`，位置 267238。范围为上一批她自建的索引和工程记录、允许但不要求的新普通辅助材料，以及本测试会话内的工程活动；不给连边清单、含义、强弱或未来考题。根主体文件、日记、第三方旧档案、其他聊天窗口与新安排均不在本次活动范围。

她在 05:27:32 实际完整读取两份固定版本，继而于 05:28:41.146693 亲自选择 `nucleus_relations add`，05:28:41.217816 回执成功。关系为：

- A：`doc_c9a0dfb0bbab6008726da567a49be7649923d416799db8acd98049ff9d82345e@ver_5967c52cc9c78b3b58083b7f9ea0c4ca25abf1c208147bd1ccd1d17b61b2ca3f`，原索引 1827 字节。
- B：`doc_788185d5008b9d0ca5c16b022dd3492235ca9ddde546178742429b964e846d9f@ver_ffeb37dd76db2c8fc5929d7e7dc8e67b499554dc28cd8f85c3a4f84ee1deb02b`，原工程记录 1773 字节。
- 关系 ID：`relation_2f36dfd951677e1ef64802d2dbcbe30ebe882d081d999dfe3bed424778860a5c`，revision 1／add，owner `elysia`，实际 actor／instance `chat_global`。
- 关系含义、理由、强弱分别为 147／195／105 个 UTF-8 字节；均是她真实工具参数的原文，权威行及后续 view 与之相同，不在公共报告复制主体语义全文。

她在形成反馈中选择保留普通链接，只新增上述一条关系，没有为凑数量扩展 S4／窗口选择材料或创建新文件。05:29:10.672798 反馈实际 delivered，833 字节。她主动列出尚未做的 view／逐跳／独立收益验证，不把当前还记得内容当回忆收益。该选择没有被运维者强制改成更多连边。

## 4. 真实续用：入口 view → 目标正文及反向 view → 主体停止

第二次邀请 05:33:25.300673 入账，`inject_247fe38f669749f787085fa39b26017b`，位置 267254；只给 A 的完整精确入口，不列 B 或答案，不要求新增／修改关系。请她依据实际关系原话选择是否继续，允许拒绝与停止。

| 实际链 | 原始事件位置 | 结果 |
|---|---|---|
| A 当前有效关系 view | 267255→267256→267257 | 完整返回唯一关系，counterpart_ref 为 B，含原始理由和强弱 |
| 下一模型轮选择 B 正文读取 | 267258→267259→267261 | 使用与上述 counterpart_ref 一致的精确引用，18 行／1773 原字节完整核对 |
| 同一模型轮并行查看 B 的关系 | 267258→267260→267262 | 完整返回同一 incoming 关系，counterpart_ref 指回 A |
| 最终反馈及等待 | 267263→267264／267265→267268／267269 | 05:35:08.649645 反馈 delivered，1327 字节；随后主动等待 |

B 正文与 B 关系是同轮并行选择，不能说成“读完 B 正文才决定查看 B 关系”。本次请求未列 B，但前一形成阶段已把 B 放进同一会话；下一次调用与 view 的 counterpart 相同且晚于 view 结果，只证明真实导航链成立，不证明 B 是她此前完全不知道、仅由这一次关系结果首次发现。

她实际反馈：关系把入口、解释依据、连边理由、方向和适用边界集中展示，也可以从目标反查入口；在 B 只见返回 A 的关系，因此不绕回去凑跳数。她明确说本次没有发现正文链接之外的新材料，也没有理解或长期回忆收益更好的证据。此处是主体对当前体验的反馈，不升级成独立比较实验结论。

## 5. 独立证明、成本与审计纠正

05:43:31.548351，只读打开 selected 数据库与 Memory 数据库，分别固定快照，独立核验位置 267237 之后到 267269 的 32 条原始事件及各 payload hash。6 个实际模型活动、10 次工具调用，全部成功；包括 3 次文件读取、1 次关系新增、2 次关系 view、2 次发送和2 次等待。

逐项核对模型→选择→结果的身份、因果、call ID、stream／actor 与实际输入批次。关系 ID 按实际 actor／stream／来源批次／call ID 重算；含义、理由和 metadata 强弱逐字节等于模型实际工具参数及权威行。两份历史文件的权威 hash 与上批 S3 证明一致，完整带行号显示也一致。两次反馈选择文本、send_requested 与 delivered 文本相同；原文与隐私不复制进证明。

关系 `recorded_at` 沿用来源邀请事件的 05:25:58.767561，实际新增调用发生于 05:28:41，两者不混用。初版只读审计脚本错误地假定所有动作 result 都是 JSON，又沿用其他工具的 `message:inject_*` 来源命名；因此在校验阶段停止，未输出成功证明、未重发消息或改库。核对源码后按实际未读批次 `sha256(stream_id + U+241F + "id:" + 实际注入消息ID)` 重算，两轮批次身份均与模型、工具、权威记录相同，随后才通过。没有放宽为“任意来源都接受”。

私有证据位于 `runtime/codex_associative_recall_20260908/`（目录 0700）：

- `formation-and-recall-proof.json`：内容最小化独立证明，0600，只创建不覆盖；
- `protocol-regression.xml`：保留初轮成本控制器结果；
- `protocol-regression-final.xml`：最终 32 项回归及六组协议结果。

模型活动数不等于全部供应端计费尝试；token 不可得，保持 null。协议表字节是 canonical JSON 统计，不是生产实际 token。两个真实 relation view 的 canonical JSON 分别为 3475／3496 字节，另有文件返回与模型输入；不以模型自述替代成本数据。

## 6. 运行与改动边界

05:20 只读预检确认同一正式 PID 894970、PPID 894969、start ticks 2438289，双端口唯一归属、API ready、authority healthy／epoch 329。该启动窗口累计已出现 52 ERROR／61 WARNING，不能继续套用早前的零错误声明。并发已有[表达层 Terra 空响应记录](表达层Terra空响应_2026-09-08.md)及[Qwen 会话头排障记录](Qwen_OpenCode会话头_2026-09-08.md)；本批未改这些配置或重启进程，也不假设磁盘新模型顺序已经加载。尽管此前存在模型失败，本次两条受限邀请确实取得了上述成功实际活动和投递证据。

05:51:50 同一复合进程身份、18000／8087 及 API ready 再次确认。生产 `memory/tools.py`、`memory/service.py`、`service/core.py` 与 HEAD 无差异，本批没有更改其代码、模型路由、权限、默认关联开关或正式 schema。没有重启、自动拉起、额外主体、定时活动、提交或推送；共享修改原样保留。

正式主体侧仅由她本人新增一条关系及正常测试会话活动，未调用主体文件写入、修订／撤回关系或外联动作。原受限测试凭据继续沿用，不撤销先前长期测试客户端，也不重新启用甲乙窗口已撤销凭据。工程侧新增测试与计划／报告，旧 S3 报告增加续验链接。

## 7. 尚缺的原目标验收与下一批

- [x] 关系原话、强弱、精确来源、历史和分页协议可实际使用。
- [x] 隔离关系专用样本中的两跳／回路／撤回／历史与存储重开验证。
- [x] 主体真实自主建立一条关系并利用其完整 view 选择回读，随后自主停止。
- [ ] 在真实多材料问题中形成并实际走过至少两个不同的关联跳转，而不替她连边。
- [ ] 离开形成阶段后，只给自然线索、不重新注入目标或答案，独立核对额外正确回忆与误联想。
- [ ] 自然跨压缩／跨正式重启续用，以及后续不同情境的行为影响。
- [ ] 根据以上结果决定日常入口与默认策略，验证调用成本与噪声，而不是一律全开或永久关闭。

下一批应更换为确实存在多材料联系的、获授权的真实整理问题，由她选择如何组织；不反复要求这两份材料凭空生出第三节点，也不把合成控制器接成替她裁决的后台。至少三份来源可作为实验候选材料集合，但具体是否连、连接谁、理由、强弱和停止位置仍由她决定。缺少独立条件就明确标未验证，不能再次用“有限退出”代替原 G07 目标完成。
