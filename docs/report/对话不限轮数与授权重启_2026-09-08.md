# 对话不限轮数与授权重启

2026-09-08，北京时间。状态：不限轮数修改、172 项定向回归和获授权的单次重启已完成；原成片回忆目标仍继续验证。本文是工程操作记录，不是主体记忆。

## 触发与授权

真实交接测试在 06:46:24.655 的日志明确记录“已达最大轮数 (5)，收束本轮”。此前已写完交接笔记并精确读回，没有最终可见回复；不能把这种基础设施收束解释成主体拒绝。

用户随后明确要求“5轮太少了，不要有限制了”，并明确表示“我授权重启权利……你可以自己重启”。本任务相关的必要单次重启由 Codex 在核验准确实例后自行执行，不再要求用户重复手动操作；这是本任务的明确生命周期授权，不是建立自动启动、监督重拉、批量杀进程或任意改动其它服务的授权。早先报告中“尚无重启授权”的描述只适用于当时。

## 变更

`chatter.max_rounds_per_chat` 默认值和当前部署配置均改为 `0`，明确表示不限累计轮数。四个轮数分支统一仅在正数配置时执行上限；普通空 action、工具 follow-up 和压缩维护不会因计数达到 5 而收束。旧的正数上限仍兼容，负数非法。不修改心跳、子代理预算或模型路由。

取消传播、模型请求超时、上下文硬预算、主体检查点协议、主动等待、可见发送后的去重收束、未知投递阻断及逐轮保存／让出仍保留。不代写主体回应，不主动清空滚动上下文；不限轮数可能增加请求次数、费用和单条表达链占用时间。

## 定向验证

`test_config_validation.py` 与 `test_chatter_prompt.py` 最终 172 passed、1 个既有 websockets 弃用警告、3.64 秒。覆盖默认不限轮数、显式正数、负数拒绝、TOML 接续，以及普通工具／空轮／维护轮在第 5 和第 100 轮继续，既有主动等待、取消、未知投递与检查点恢复合同仍通过。没有真实模型、正式数据库或端口竞争。变更四个 Python 文件 F／E9 通过，`git diff --check` 通过；不据此声称全仓已通过。

## 重启前与备份

07:14:00 前置核验：原 PID 894970、PPID 894969、启动 ticks 2438289、boot ID 和原目录／argv／tmux pane 一致，18000／8087 唯一属于该进程；账本尾部 267327，主体文档版本 5343。三个运行快照的 revision/hash 写入 `runtime/codex_associative_recall_20260908/unlimited-restart/preflight.json`。原一次性 `run_elysium.sh` 未改，不创建监督重启机制。

WSL 可用约 1.88 GB，原备份余量检查未通过时尚未停止服务；选择 C 盘约 179 GB 可用的本机私有目录。随后向准确原 PID 发送一次 SIGINT，正常退出且两个端口释放，未强杀。备份目录首次 ACL 绑定了沙箱账户而非真实登录用户，两次打开目标失败，未形成备份文件也未修改源数据库。核验账户后通过原所有者仅补充 `AYERELYSIA\\26652` 访问权，最终 DACL 关闭继承、仅用户与 `CodexSandboxOffline` 两个主体可访问。备份目标为 `C:\\Users\\26652\\Documents\\Elsium\\.private-assoc-restart-20260908\\selected.sqlite3`，使用停止状态的 SQLite 一致性备份并执行 quick_check 及运行快照／主体版本边界对照。

07:20:10 备份完成，781,717,504 字节，quick_check 为 ok；源与备份的事件尾部、三个运行快照和主体版本计数一致。关闭期间全局运行快照 revision 从 27710 正常推进到 27711、payload hash 不变；Chatter revision 1801 和 Heartbeat revision 2562 及其 hash 不变。没有删除、恢复或改写源历史，备份仅用于恢复留档；不能把备份反向覆盖成新的主体认知。

## 正式启动与连续性结果

07:20:51 经未改的原一次性入口启动新 PID 1509096、PPID 1509095、ticks 3640521。07:23 的 `/api/v1/health` ready 仅说明 API 存活，不代表插件或记忆已完成；早先口头状态随即按此收窄。记忆恢复期间新进程有持续 CPU／I/O 活动，07:34:47 已检查 3975／5207 条受管投影，后续全部 5207 条完成。07:38 的一次 API 超时随后恢复，不伪造成一直无故障。

07:38:58 开始普通文档恢复，07:38:59 完成 50／50、indexed=0、requeued=0、artifacts=0，随后明确记录 local 权威记忆服务初始化完成；07:39:07 NapCat 消息通道监听 8087。此处仅说明 Elysium 侧通道服务器就绪，不将它升级为 QQ 登录或第三方端到端消息送达。

07:40:49 独立核验：旧 PID 不存在，18000／8087 唯一属于新 PID；启动时记录的 launcher、Chatter、配置与事件过滤修复 SHA 未变化，部署配置 `max_rounds_per_chat=0`。这是新进程加载前后源码／配置与入口证据，没有向进程注入代码或读取私有内存。

受限测试原始事件 267302–267327 共 26 条逐字节／hash 与停止备份一致；索引、工程记录、交接笔记三个精确版本的内容及 actor/source 等全部列保持一致。Chatter 快照仍为 revision 1801，hash `03459ae33ae41f35d0e1e52f86b4028d8d0c0607734503d0b94d4b7121c25dda`，与停止备份一致。证明位于 `runtime/codex_associative_recall_20260908/unlimited-restart/running-proof.json`，不会覆盖之前阶段记录。

不限轮数的第 5／100 轮继续行为由隔离执行测试验证；其后 07:43:20–07:48:20 的同一真实表达链记录 10 个成功模型活动（请求 1/2/3/5/7/8/9/11/12/13），真实超过 5 轮并最终投递。12 次工具选择中 11 次成功，一次错误工具名显式失败后由主体换用正确发送入口；不是零故障，也不是 10 次全部计费尝试。09:10–09:11 再核全部成功活动的调用身份与请求匹配、输入仍含形成材料，因此证明真实不限轮数续行，不证明独立回忆收益。后续沿总目标清单继续。
