# 记忆索引批次刷屏（2026-09-07 22:19）

## 结论

`life_engine 记忆索引批次完成: claimed=4 completed=4` 是 **Chroma 向量投影 worker 在追积压**，不是她在说话，也不是在改 `SOUL.md` / `MEMORY.md` 正文。

SQLite/权威文档写入后，把待 embedding 的 outbox job 排进队列。worker 每批最多领 4 条（默认 `memory_index.batch_size`）。满批且全部完成时**不睡 60 秒**，只让出事件循环就继续，所以日志会一秒一条。合同见 `docs/architecture/生命记忆系统.md` §10。

22:19:12 那一批：`BAAI/bge-m3` 的 `life_memory_embedding` 撞上 `LLMRateLimitError`，整批 4 条标 `failed`。有失败就停止追赶、恢复常规间隔。约 60 秒后（22:20:10）继续领剩余 pending；随后出现 `claimed=3 completed=3`，队列已接近见底。失败的 4 条默认不会在本进程里循环重试（`retry_failed` 只影响启动后首批）。

同分钟心跳在验收一份关系笔记改名（旧路径读取 `ManagedFileDocumentNotFound` 符合改名后预期）。改名会重新入队索引，容易正好叠出这一串 catch-up 日志。

权威历史仍在。向量投影暂时对那 4 条 failed job 不完整，检索可能 degraded，不会反向删改原文。
