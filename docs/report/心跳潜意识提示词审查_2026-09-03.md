# 心跳潜意识提示词审查

> 日期：2026-09-03
> 状态：代码头已干净；工作区 `TOOL.md` 曾把已删工具名注入每一拍 system prompt，现已改写并加过滤器兜底。不启动 Elysium。

## 结论

退役工具从 Python 组件清单里删掉之后，**心跳 user 头 `_build_prompt_header()` 是干净的**：没有 `nucleus_tell_dfc` / 思考流工具，有 `inner.return`、`nucleus_proactive_query` / `nucleus_proactive_command`。

真正的污染在 **system prompt 里的 `TOOL.md` 包装**。审查当时工作区文档仍把 `nucleus_tell_dfc`、`nucleus_manage_thought_stream`、八个旧学习工具、旧 TODO 名等写成心跳用法。过滤器只挡“必须行动”口号，**不挡已删工具名**，所以包装后的 TOOL 段仍约 11.5KB，且点名约 20 个不在 `HEARTBEAT_TOOL_NAMES` 里的名字。模型会在 ROLE.TOOL 对不上的情况下被文案教去调用未知工具。

这不是主体文件（SOUL/USER/MEMORY）被改写；`TOOL.md` 是心跳脚手架。已改成与当前 21 个常驻工具对齐，并让过滤器丢弃含退役工具名的行。

## 结构（一拍心跳实际喂给模型的东西）

1. **SYSTEM**：SOUL + USER + MEMORY 摘要 + 包装后的 TOOL.md
2. **ROLE.TOOL**：`HEARTBEAT_TOOL_NAMES` 21 件
3. **USER**：固定头（约 6.9KB）+ 最近事件流 / 世界投影 + 段落（日记目录、TODO 板、机会页、能力目录、学习账本）+ 时段建议

有效上下文是有的：潜意识 prepare 有 16k 字符 / 5 个因果组预算，机会页、日记普查、TODO 板、world perception 都会进 USER。不够的是「文案层」以前太吵，会盖过事件。

MEMORY 心跳态仍注入全部 Durable+Active（当前工作区原文约 23KB），不像聊天态截断。这是有效记忆，不是退役工具污染；若以后要收预算，应另做有界投影，不能靠删条目。

## 已做

- 改写 `data/life_engine_workspace/TOOL.md`，只描述本拍常驻工具和对内/对外两扇门
- `_render_heartbeat_tool_prompt` 丢弃含退役工具名的行；不再误删“可以安静结束”
- `test_heartbeat_system_prompt_strips_retired_tool_names`

## 运行生效

- `TOOL.md` 每拍从磁盘读取：工作区文件改完后，**已在跑的 Elysium 下一拍就会吃到新文案**
- Python 过滤器需要**用户手动重启**后才进进程
- 未启动、未重启 Elysium

## 未改

- 不改 SOUL.md / USER.md / MEMORY.md
- 不删能力目录与 header 的轻度重复
- 不把 MEMORY 心跳注入改成聊天态截断
