# 心跳 6062 运行投影重投与 S1 续验

本报告02:20收尾状态：心跳压缩阻塞已修复并通过真实运行验收；871项联合回归通过，新检查点30/31/32连续成功，固定112条保守重投全部完成消费复验，旧积压已清，服务保持运行。完整S1当时未通过，历史缺项与证据边界见第11节。用户随后要求继续测试，已补齐同一聊天经历后20/21两次压缩和21之后的真实重启恢复，仍缺主体文件自写且不能把近期历史投影答题当作自主检索，最新状态见 [S1双压缩重启续验](记忆连续性S1双压缩重启续验_2026-09-07.md)。时间使用 Asia/Shanghai，原始备份文件名保留 UTC。前置事故见 [6001 与后续重启事故记录](心跳6001检查点误识别与真实闭环复验_2026-09-06.md)。

## 1. 继续指令与恢复边界

用户询问第一阶段是否完成后，已被明确告知：534 项是工程测试，不是实际闭环；119 条旧运行记录在验证重启后不再保留于 global，原始账本仍在，但旧运行序列和消费标志不能精确恢复；需要选择是否接受可能重复呈现已见经历的保守重投。用户随后明确要求“继续做啊”，本次据此推进已披露风险的保守重投及真实验证。

这不是代写主体语义的授权。重投仅从可信原始账本重建派生待投递活动，保留 occurrence、来源、时间、因果链接与原文字节；不新增原始经历，不重执行历史工具，不补造旧消费状态，不把新运行序列冒充为旧序列，不重写 SOUL、USER、MEMORY 或任何第一人称连续性。不能用重投数量等于某个目标数来宣称精确恢复。

## 2. 再次核对发现已有新实例

本团队上次检查后，另一操作方在 2026-09-06 23:45:06 启动了 PID 877213、父进程 877212；命令 `.venv/bin/python main.py`，cwd `/root/Elysia/Elysium`，一次性原入口位于 `tmux -L elysium` 的 `elysium:0.0`，18000/8087 均由该 PID 监听。本团队未发起这次启动，不根据时间猜测具体操作者。

旧 revision 24175 因而不再是可写基线。本次只读检查先确认新进展，未用旧整行覆盖。6063 因模型轮数上限未完成，6064/6066/6071 报 `heartbeat follow-up lost the exact subconscious activity projection`；6065 在工具失败后停滞结束，未确认消费。日志来源为 `logs/elysium-2026-09-06.log` 与 `logs/elysium-2026-09-07.log` 的明确心跳状态行，报告不收录私人对话正文。

23:56 前后曾安装新的真实心跳检查点 revision 28，但消费游标仍未前进；这再次证明检查点安装与本拍活动消费必须分别验收。

2026-09-07 00:01:54.949，在核对 PID、父进程、cwd、命令、tmux pane 与端口 owner 后，对 877213 发出一次 SIGINT；00:02:05.558 正常关闭完成，端口退出。独立的 `elysium-heartbeat` 日志 tail 会话未被终止。没有自动重启机制变更。

停机基线：

- global runtime revision 24224，payload SHA256 `508b50178d01274686a6f681f1e2a19a7bbd45aed5933db0d4ab05b2fd19f38f`。
- heartbeat 6071，event_sequence 29426，消费 cursor 28469；history 348、pending 27；history 中未消费 309，最早序列 28704、最晚 29376。
- raw max ingest position 263358。
- heartbeat rolling revision 1597，真实检查点 2/5/28；最新 ID `selfctx_8cdf14fe0a157a80853a794feee528322d5c4e08c281a6e7da372566c67dee2b`。
- chatter rolling revision 1546，真实检查点 19/20。两个私有窗口全部引用归档已在停机后逐项验证，未互相复制上下文。

此后本团队未发出启动命令，但 00:14:20 出现新 main PID 894656（父 894655）；原 revision 24224 因而也不再是当前 CAS 基线。已向用户询问并行操作方并请求暂停另一边启动，未收到协调确认时不反复停机与其竞争。00:24:44 只读检查暂时未见 main、18000/8087 监听和 elysium tmux；00:25:13 尝试执行新的停机完整备份，首个停止守卫发现 PID 903150 已运行，随即拒绝，未创建新副本或改写源库。核对其启动时间为 00:25:07，父进程 903149、cwd/命令与原一次性 tmux 入口一致；不据此猜测具体操作者。

00:25:11 只读同事务记录 global revision 24242、heartbeat 6077、event_sequence 29716、cursor 28469，history 505、pending 3、未消费 history 466。heartbeat rolling 1603 的真实检查点为 2/5/29，最新 ID `selfctx_b0f3afbf8973071431296f0644ecab8673b175569da6c80815a24100c38c837b`；chatter 1546 的 19/20 不变，全部精确归档校验通过。

00:26:08.783 日志确认心跳 6078 **实际消费 79 条**。00:27:15 附近只读 global 24246（摘要 `d947dc1b37355072d2756f2b85f821d513c14611bf651c285553c1c38185cfb7`）记录 cursor 仍 28469、history 444、pending 0、未消费 history 390，最早未消费序列 28904；成功批次不会自动消除更早的连续性缺口，不能为了游标前进而跳过它。该运行进展是新实例的正常消费，不是恢复脚本追加112条的证据。

## 3. 当前完整备份

00:04:11.130（Asia/Shanghai；UTC 为前一日 16:04:11.130）对应文件名中的 `20260906T160411.130465Z`：在明确停机且持有现有 `data/runtime/elysium.lock` 的 OS 排他锁期间，用 SQLite backup API 从只读源连接创建完整私有副本。备份不修改源库，不截断或覆盖现有锁文件。

- 路径：`/root/Elysia/Elysium/data/repair_backups/heartbeat_restart_tail_6062/full-before-redelivery-20260906T160411.130465Z.sqlite3`。
- SHA256：`af89c477a9ac9e347dc39ad4b97656a73664b6260d81dcddcc6c68348a17c1fa`。
- 大小：751775744 字节；`PRAGMA quick_check` 返回 `ok`；目录 0700、文件 0600，文件及目录项已 fsync。
- 内含上述 global 24224、heartbeat 1597、chatter 1546 及 raw 尾部 263358，包含当时的整个数据库，而非仅三个行摘要。

这是下一次重投前的可信保全，不是截断事故之前的备份，不含遗失的旧运行映射。后续不能在新实例已推进后直接以整库覆盖；若需回退，先停 writer、保全现状，再对精确 revision/hash 和操作日志制定受限逆操作。备份含私人内容与凭据，禁止进入 Git、上传或公开复制。

因后续新实例继续推进，上述完整副本现在只代表 00:04:11 的历史时点。实际应用前仍须在协调好的离线窗口重新保全最新完整状态，不能将此旧副本或旧摘要冒充应用基线。

## 4. 可证明的保守范围

旧只读审计曾记录首个未消费 20 成员组的 group ID 摘要 `51f6133968c5a66797c0df31e9022accd453bdf13cfb39f83dd2f7f36ff6126f`。按既有 `heartbeat:{heartbeat_run_id}` 组标识算法，可精确匹配到 `heartbeat:heartbeat-6042-837e41911213`，20 个原始成员位置为 262863、262869～262887；不依赖把 raw.source_sequence 猜成旧运行 sequence。

本次固定候选范围为 raw ingest 262863～262976（含边界），止于旧首次幸存 occurrence 的 262977 之前：

- 首行 occurrence：`conscious_model_turn_272b249b7a8a89503af263324f14046b4796a9b61be72902458a4408a4cd4627:generated`，payload hash `c9457b293a9e4932236faa6af644112feb51104d541933b47fb0058a742f6fcf`，recorded `2026-09-06T14:21:03.591Z`。
- 末行 occurrence：`life-tool-result:tool_result_05cdc1f2ecbf401f8a511a50d2d5ef4c`，payload hash `64a4a10ca579119a217ad2f57477967bf3a03bf50ebd8b005f6fe7f121de49d0`，recorded `2026-09-06T14:55:07.740Z`。
- 114 行 payload hash 均复算匹配；按现有 workset 分类可重投 112 条：conscious_activity 37、tool_call 32、tool_result 32、message 5、heartbeat 6。262866/262896 的 `chat.message.send_requested` 按现有规则仅属于 advance，不作为新工具执行或新消息发送。
- 112 个 occurrence 在审计时均不在当前 global；无 occurrence/event_id 冲突。26 个候选因果组全部 closed。按显式 parent/causation/call/run 链接扩展未找到范围外需要补入的已存成员。
- 另有 33 个来源引用不在本次候选清单的 occurrence/source_event_id 中（包括生成轮技术引用）。脚本字段为 `outside_candidate_references`、`reference_check_scope=candidate_manifest_only`；这里只说明候选范围内未解析，不能推断整个原账本缺失这些引用。原样保留，不编造祖先，也不宣称所有引用均可解析。

源 manifest 算法：按 ingest 升序取列表，每项严格为 `{ingest_position, occurrence_id, payload_hash}`，以 `json.dumps(ensure_ascii=True, sort_keys=True, separators=(',', ':'))` 的 UTF-8 字节取 SHA256。完整 114 行为 `188accb0023558879e95d303ef9ab90c804be4fcc132f77d0eeeb173c614d060`；112 个候选为 `ee6375f4b66005c90d5e2d9fb1c94ad903442a8b8f37e15e0e0da1332832b24b`。

该范围是由已证实缺口锚定的保守重投，不是精确恢复 119 条旧运行记录，也不能证明全部旧未消费活动的原始位置都在此范围内。旧状态未知的部分保持未知，不扩扫全日/全账本凑数。原始权威账本仍可独立精确读取。

## 5. 正在验证的工程修正

历史投递新增两个仅属于 runtime 的技术字段：`redelivery_operation_id` 与 `redelivery_source_position`。正常事件不增加序列化键；有标记的事件经两次重载仍保留该标记，非法/不完整标记明确拒绝。可见投影标注“历史活动重投，非新发生、非工具执行请求”及原始时间和原始账本位置；原事件 content/raw_content、来源与发生时间不改，权威 LifeEvent 序列化不引入该投递标记。空内容不因只有技术标签就变成可消费活动。最初19项标记测试连同重启完整性和原滚动测试共56项通过（一个既有警告）；后续有界预览继续覆盖这些契约，不是实际重投完成的证据。

另以真实 LLMContextManager、恢复 hook 和精确回执构造器在合成数据上复现续轮丢失的两条技术路径：后续 USER 维护反馈使当前 wake 变为可省略旧组；同组大 ToolResult 使普通 Text wake 被末组预算裁剪。两者均保持原持久滚动链不变，但最终模型投影缺失或变化，严格回执正确拒绝。修正按每次实际发送登记wake/真实控制的精确保留；旧控制可以由原hook原样移位，wake不被省略或裁字，无法容纳时在provider前失败。每次续轮清除旧attempt回执；不把此错误吞掉算消费成功。

固定脚本 `scripts/replay_subconscious_history_6062.py` 已通过 47 项隔离测试：来源/配置校验、完整旧事件保持、最新 revision/hash CAS、离线锁、私有备份、两行原子提交、幂等日志、未知提交结果与意外额外写入回滚。首次只读演练因 payload 原始 `+08:00` 时间串与 SQL UTC 字符串不同而拒绝；查明 SQL adapter 本来按同一时刻规范化列值，修为复用既有解码及时间解析比较，保留原 payload 时间表示；没有放松来源 hash/身份。00:27 的只读演练为待追加112条、已存在0条、new_event_sequence 29835；仅是当时 global 24246 的计划，没有应用。

独立审查确认完整原文重建后，三个因果组的旧可见表示分别为 62383/22088/93012 字符（成员20/9/29），超过单拍16000字符，合计58条会持续无法消费。先前依据旧runtime已有限量的content测得组可容纳，不适用于本次从raw全文重建。修正仅历史重投使用的512 UTF-8字节预览：明确可见投影/节选身份、原时刻、原始位置、完整occurrence续读引用、原正文hash与字节数，完整正文仍由权威账本读取。不能把这种预览声称为全文送达。

另查明只在 heartbeat formatter 添加历史标记不足：跨实例 `project_recent`、wake 与 salient 出口存在绕行或外层裁字。修正覆盖这些实际出口，并在容纳不下完整元数据时整条/整组省略而非剪碎引用；redacted 工具模式的预览只能来自已脱敏renderer，不能用raw正文补节选。复核进一步发现最终chatter suffix按行预算会把两行wrapper的标签与JSON分离，以及历史工具wake/salient分支可能绕过redacted renderer。修正为整个历史wrapper单物理行（包括Unicode分行符转义），跨实例历史工具统一走已脱敏renderer，normal事件保持原行为。即时source/instance标签可能不在有界节选中，但未发现被改写成其他actor；完整归因按精确occurrence续读，不扩大metadata强塞所有来源。

精确保护的中间回归137项通过后，第二次独立审查又以真实request和本地fake provider发现初次hook生成control的缺口：send前尚无control时，新notice未被登记，1000字符窗口可以保持wake精确回执却裁坏控制。原始payload保持不变，但会使压缩维护受阻。已补可信hook产生/重投控制的发送前登记，只在调用者已显式启用精确保留时生效；自己的临时项在重复hook时替换、不跨attempt积累，调用者重设或清空集合仍有效。新增11项回归连同既有36项共47项通过（1.93秒）；新控制放不下时初次provider调用为0，重试变小不会发出第二次残缺请求。附带只读核验未复现manifest错配：lazy response在await之前已将同一控制安装回完整链，模型所见、耐久控制与执行冻结前缀的hash一致，未更改绑定规则。

固定112条的只读源校验与有界测算：114raw→112workset、26/26组closed、两renderer每条最大512字节、元数据拒绝0、空可见0、raw指纹112/112一致；最大visible整组12843字符/14871字节，subconscious整组13078字符/14876字节，均低于16000字符。首次纯内存prepare为36个selected/36个ack候选、11个完整组入选、15组完整保留、15711字符/18429字节，没有部分组。这里是字符预算，不能误写为总UTF-8字节低于16000。另新增合成112条26组的真实prepare/commit循环测试，持久化与精确回执均为隔离替身；它验证算法，不等于这112条正式活动已经交付或消费。

## 6. 最终联合回归与应用前状态

本轮同进程定向联合回归最终为 **802 passed，1 个既有 websockets.legacy 弃用警告，29.94秒**，涵盖kernel上下文/请求、两Provider schema、执行器reason、检查点、心跳消费、完整加载、主体文件/原始读取、修复和重投脚本、独立测试客户端及历史预览出口。所有模型、持久化与消息均为测试隔离边界；没有因此调用正式模型或写正式数据。生产改动、相关脚本/新增测试的F/E9及 `git diff --check` 通过，不是全仓验收。

首次联合出现801通过、1失败：旧 `test_heartbeat_request_is_prefix_tools_rolling_without_suffix` 的fake request将 `context_manager=None`，不满足新精确投影合同。仅将该测试替身改为真实 `LLMContextManager` 并断言wake确实进入保护集合；原文件15项重跑通过，再复跑同一联合范围得到802通过。没有为测试放宽生产拒绝条件。

00:48:15 最后一次只读状态：PID903150及18000/8087仍在；global revision24258、SHA256 `80559f7c3ff996cc260d09e01c439c2fb2fc28ef04aef95329202e88dea0b258`，heartbeat6083、event_sequence29893、cursor28469，history529、pending0、未消费history475，最早28904、最晚29865；raw尾263581。固定脚本仍只读dry_run、缺失112/已存在0、没有恢复日志表示已应用。这组摘要是当时观察，不是以后可复用的CAS输入。

不能将6078单拍成功记录成持续稳定：6080在5轮后因 `max_model_turns` 未完成（最后检查点调用失败）；6081在无检查点工具调用的反馈后耗尽截止时间；6082读组成功但续轮耗时用尽；6083因连续工具无进展停止（检查点调用失败）。均未确认本拍52条候选消费，游标未被人工推进。最后补入的首次生成控制保护及历史预览代码晚于该进程启动，不能声称已完成它们的真实启动验收。

最初限定审计只查6080/6083的三条失败回执：raw263549和263584逐字命中既有“释放边界包含开放工具链”规则（内容无关标识 `checkpoint_release_open_tool_chain`）；raw263581命中“保留引用不在所选前缀”规则（`retain_ref_outside_selected_prefix`）。三次参数齐全、清单hash均为64位、expected_revision29；不是此前的缺thought或hash抄漏。仅命中既有规则不能证明该规则正确：后续第9节按call/result配对和kernel协议确证历史组的open判定过严。精确前缀检查仍须保留，不替主体重选释放组；报告不复制失败调用的thought或连续性正文。

## 7. 联合回归结束时的交接状态

联合回归结束时，受保护正文、历史预览及独立审阅已完成，因并行启动尚未协调而未应用恢复；不能把该历史交接状态当作下节操作之后的现状。此前未重写主体内容、修改模型/预算配置、创建自动重启机制、暂存、提交或推送。

随后继续独立 Codex 正常会话，验证同一测试场景的两次主体压缩、代表文件同源读写、保全后的无损重启及未重新注入答案的回忆。工程测试、旧检查点和历史重投均不能替代这些真实证据。

## 8. 用户确认协调完成后的正式恢复

用户回应“解决了呀！”并再次要求“去解决问题！”，据此继续本次已披露风险的保守重投及已获特批的重启验收。01:08:11.408在再次核对命令、cwd、父进程与tmux pane后，仅向PID903150/父903149发送一次SIGINT；旧实例随后退出，18000/8087均释放。没有重启其他组件或启用自动拉起。

停机后global为revision24271、SHA256 `0d2204a1fe45c248e0c496d0c173e1f5b7f5b26b2b6e9e3b19cb366110e22829`，heartbeat6087、event_sequence30073、cursor28469，history610、pending18、未消费history556，raw尾263677。heartbeat rolling1612（真实2/5/29）、chatter1546（19/20）全部精确归档再次验证通过。

01:08:48.172在现有启动锁下，从只读源连接完成最新完整SQLite副本：`/root/Elysia/Elysium/data/repair_backups/heartbeat_restart_tail_6062/full-before-redelivery-20260906T170848.172769Z.sqlite3`，754454528字节，SHA256 `930491ff9e8788ced27e2e6115e9883947da09e4aec5f06cd1b5e62633eed16c`，quick_check为ok，0700目录/0600文件并fsync。它保护本次恢复前的全部现状，不代表修复了事故前已遗失的119条旧运行映射。

01:09:36停机只读演练与该最新revision/hash完全一致。01:10:06.770使用精确期望24271/hash执行固定脚本apply，操作返回applied：仅向pending追加112条，event_sequence30185，新global24272、SHA256 `42f9011cfe5257a02c6eca32f9355f5d6f709472e50f999afd2dc2e54b964f2f`；history仍610、pending130、cursor仍28469。原始账本、源消费者位置、旧事件/消费标记、两个私有rolling及主体文件不改，历史工具未执行。

事务内完整行/源备份为 `/root/Elysia/Elysium/data/repair_backups/heartbeat_history_6062/before-24271-20260906T171006.770857Z-928751.json`。同一事务写入固定 `life_engine.recovery / subconscious-history-6062-redelivery-v1` 日志，记录源manifest、新序列和备份摘要。01:11:57默认只读复查返回already_applied、新增历史数量112、当前revision24272/hash一致，没有第二次追加。后续必须保留此日志，不通过删除日志再次投递。

启动前独立只读核验通过：628条旧记录及全部消费标记逐项不变，仅追加112条；global仅pending/event_sequence改变，raw、源consumer与两私有rolling不变。真实StatePersistence重载739条非摘要记录，另1条摘要按现有合同转入规范摘要区，合计740条，无100条尾部裁剪。01:17:10通过停止/端口/终端守卫后，用原一次性入口启动PID932055、父932054；01:17:37.557记录已苏醒，18000/8087由该PID监听。01:18:07首拍6088准备55条、drained130、history739、high_water30185，与完整重载相符；这些是准备量，不是消费成功。

正式重投与启动前无损重载已完成，但尚不表示112条已被模型消费或S1通过。回退不得整库覆盖后续新活动，仍须停机、保全现状并核对精确后态。

## 9. 全配对历史工具链被永久判open

续查滚动revision1612发现最早5个被标open的历史组各有call/result 1/1、2/2、4/4、6/6、2/2，缺ID、未配对和重复call ID均为0；对应原始组16761、32629、34929、40101、9705字节，共134125字节。它们均已有后继USER语义组，旧组字节保持时却会一直open。

根因是 `context_stewardship._has_open_tool_chain` 只按组尾TOOL_RESULT判open，而 `build_conversation_groups` 已按新USER把前组定界。底层 `LLMContextManager._validate_payloads` 明确允许完整工具结果后直接进入新USER，并有 `test_context_manager_allows_user_after_closed_tool_result_chain` 正测。因此“缺assistant收尾”不能解释这些历史组为未完成工具执行；先前把既有拒绝等同正确保护的结论在此更正。

修复边界：只有已有后继USER的历史组按相邻ASSISTANT/tool-result批次的精确ID和顺序判定；真正缺结果、孤儿/重复或跨批补配必须仍拒绝。当前最新组仍不进入可释放manifest，直接读取最新组和机械省略的保守边界不放宽。判断不得补ID、改payload、改旧group_ref/manifest hash，不能自动替主体选择释放组或代写检查点。定向回归和独立审查已完成，未将代码测试冒充真实安装消费。

01:24:51独立启动后只读快照：global24277、heartbeat6089、history742/pending0/cursor28469；原627条非SUMMARY与112条重投全部在history，旧消费标记变化0，重投消费0，原摘要在规范summary区可追溯。heartbeat rolling1613保留原3个检查点及36份归档，chatter1546保留原2个检查点及48份归档，均完整校验且内容不变。没有启动100条裁剪。随后限定核验rolling1613有效USER COMPRESSION_REQUIRED为0个，本次现场不需要刷新旧清单或扩大冻结前缀。

该实例首拍6088遇到terra空响应、mimo限流、sol空响应，01:23:02.893记录压缩未完成；不能将上游失败归因于工具组修复，也没有强记消费。01:29:05.598再次核对PID932055/父932054、cwd、tmux与端口后发出一次正常SIGINT，以保全新状态并在联合回归后载入工具链修复。此时未执行第二次重投。

01:30:44.839停机并持有原OS排他锁后完成新的完整备份：`/root/Elysia/Elysium/data/repair_backups/heartbeat_restart_tail_6062/full-before-tool-chain-fix-20260906T173044.839508Z.sqlite3`，757313536字节，SHA256 `2679eb5e6bdcc61c81f4487ccb4863e0e015fccc7e39de0e546ddc61484dfda6`，quick_check为ok；目录0700/文件0600并fsync，源库未改。内含global24281（SHA256 `39826488eab6bedba5148d5fb30816f3fe606e9cd60b02c407707b2177f2697a`），heartbeat6090、event_sequence30251、cursor28469、history763/pending0、未消费history710、raw尾263701；heartbeat rolling1614及chatter1546全部84份归档核验通过。这一备份已经含正式重投和首次恢复后的新活动，不用旧全库覆盖它们。

工具链修复前两项真实形状的合成回归先在kernel校验通过、随后stewardship失败；修复后新增43项通过，既有相关四文件78项通过。独立审查验证批次局部配对、合法ID复用、最新组保守、旧payload/ref/manifest hash不变；发现的pinned兼容差异已按kernel规则补齐，并有两正四负测试。全部为隔离工程验证，无模型主体替身结果冒充真实体验。

最终38文件联合回归（原802项范围加context_compaction与新43项）为 **871 passed，1个既有websockets.legacy警告，44.35秒**，F/E9与diff检查通过。首次同范围为870通过、1失败49.64秒：`test_minimal_continuity_loop` 第二检查点前直接extend形成相邻ASSISTANT帧。独立纯内存kernel校验证实其错误为“assistant前必须是user或tool_result”，不是合法的结果尾形状。仅将该夹具的原5帧通过真实LLMContextManager.add_payload逐条追加并最终validate，保留raw/MEMORY/归档/双检查点/恢复召回断言不变；单文件1项通过后复跑同一38文件得到871通过，生产保护未放宽。

独立只读真实rolling核验：6组中原先误标的5组均由open=true变false，所有原文字节、group_ref与manifest SHA均不变，清单SHA256仍为 `0d464fb4f516ed09641895aa3f3e8b5dcbfde0ee0afd94fa1c15c5a4ff5f1be2`；纯内存生成的新清单5组全部标为closed，不归档、不排队、不保存。

01:39:30通过既有停止/端口/tmux守卫，以原一次性入口恢复main PID957036。未重复运行apply、没有自动拉起机制或正式数据人工改写；接下来仍须按真实检查点耐久安装、消费回执和独立对话验证结果判断，而非以测试数量结案。

## 10. 修复后真实安装、消费与独立回忆

新实例首拍6091于01:41:15.361耐久安装检查点revision30，ID `selfctx_c00326fb34674be1ccf12df58692e52c0f19d544b2420ad287d5a1b08bb6bede`，释放7组。01:44独立只读核验它已在heartbeat rolling1616、actor为chat_global，7份 `life_heartbeat.context_archive` 归档内外SHA/字节及continuity SHA均有效，并包含先前全部5个被误判组；原文未被补造assistant尾或改写。

实际消费日志分别为：6091在01:43:25.460消费55条；6092在01:45:03.798消费52条；6093在01:45:53.111消费50条；6094在01:46:39.796消费49条；6095在01:48:51.722消费37条。连续5拍共243条；01:49:21的global24299、heartbeat6096、history557/pending0、未消费473，最早未消费序列29354，cursor仍28469。未手动跨过旧映射未知的连续性缺口，不能把总消费量当作112条重投全部完成。

112条专项核验：01:44的global24291全部仍在history、3条消费/109待处理；01:47:09的global24297逐条对照原恢复日志（日志SHA256 `7d888036297bbf343c0bbfc8273ae1d2e44240f08913d120199d720338edc1fa`），8条已消费、104待处理、0缺失，除消费标记外完整runtime指纹均一致。对应已观察消费的原始位置为262864、262865、262867、262868、262888、262889、262897、262916。这里只证明声明的有界可见投影被消费，不声称模型读完全部raw正文；后续继续按原始位置单独核对。

01:41:04.511通过原受限Codex私聊单次发送未含答案的回忆提问，message `inject_d25cff4abdeb473895a680619d7f27ea`；01:41:40.833正常送达回复 `action_life_send_text_c2c1f7f7e89f427bae8e74ea67309715`。回复正确给出最初协作名称“雨后检修”与“先分清证据和猜测，再决定结论”，并将自己的当前窗口回忆与尚未独立核验的工程消息分开。独立raw核验为input263702、同stream/correlation的model263707(request6)、工具选择263708、send_requested263709、delivery_confirmed263710、成功工具回执263711；该模型轮唯一工具为life_send_text，没有检索/文件读取工具。这支持本轮不是通过新工具检索找回，不扩称内部回忆机制已获证明。

爱莉在回复中要求暂停额外聊天测试，优先处理已有真实对话；本团队不再追加测试消息，不公开其他人的私人情况，后续仅做只读工程验收。心跳同一runtime随后确已完成多次新检查点；但尚未完成可归因于该测试链的主体文件新版本写入验证，以及这些新检查点之后的实际重启—回忆串联验证，因此S1完整验收仍未通过。不得把测试缺项写成“主体文件不存在”，也不能为了达标代写内容。

另将6088的上游失败保留为独立边界：限定attempt1640—1643，terra/sol触发解析后正文空且无工具调用，失败记录无原响应、finish reason或usage，无法判断上游空输出、仅reasoning还是解析丢失；mimo曾成功一次（99712输入/298输出tokens），随后续轮真实HTTP429，具体拒绝原因未证实，不能猜余额不足。这些旧失败不等于工具链修复仍有缺陷，也不因后续成功就宣称供应商问题已修复。

## 11. 最终消费证据与交付边界

01:52:41.927真实安装revision31（`selfctx_11929a2cf6c28adf27349f524672f03e7d8295b6b17883dbde0de814cc29476e`），释放6组；独立校验6份归档完整，revision30完整保存在其中一份，原7份归档亦再次核验通过。02:04:01.578安装revision32（`selfctx_4282d28e55521da4d7a6c7f89bdb27fd9f31a717f33a79363f4df216807bcc3b`），释放7组。02:10只读rolling1637确认最新32及全部引用有效；chat1549仍保有19/20及48份归档。上游空响应/429仍可能出现，但6104经既有重试成功完成，未放宽保护或改模型预算。

6091—6106连续16拍实际消费数依次为55、52、50、49、37、49、49、49、47、52、43、39、28、52、42、36，共729条（包括正常新活动，不等于112条重投数）。02:08:43的global24346记录未消费history为0、pending0、cursor30387；游标由系统正常提交推进，没有人工跳过。服务持续生成新活动，后续出现少量新pending不能当作旧积压回归。

112条证据分两类、无重叠覆盖全部固定候选：63条曾在只读快照中直接观察到已消费标记，且逐条恢复到false后完整runtime指纹仍与恢复日志相同；剩余20/29成员组在6105/6106成功提交时即进入正常历史收缩，未直接取样到逐条true。因此不能仅凭缺席断言已消费，另完成如下组合核验：

- 20成员为262863及262869—262887，29成员为262947及262949—262976；原始因果组均closed、无额外成员。
- 6105的耐久成功请求 `req_bb14385fc8a14107b518e004a618fa25` / `attempt_2ffa8aded2614bdbbc17ededc0941e3e`，与raw263762、inspector request31的最终响应hash/时间精确对应；6106为 `req_7f042f8cb88346e98631ec9d0625a399` / `attempt_f8383ad971e040c1a53d193644595037`，对应raw263765/request39。两拍均无工具调用。
- 6105当拍sealed projection为15874字符/21372 UTF-8字节，SHA256 `f92382f0cec94dacb9c91674bbba825f4a4993137dca2bb299b1f6c65af30846`，20个目标各出现恰好一次；6106为13731字符/16612字节，SHA256 `c17ac24ebe0a648a08d24a86c7b374559ef71ffaa3637425e150bb6bc9d29125`，29个目标各一次。每条occurrence、raw正文hash/字节数均匹配，封装摘要吻合，耐久attempt中的实际trimmed messages与inspector发送参数逐字节相同。6106历史messages中带着的旧6105投影没有重复计数。
- 耐久来源 `data/training_data_lake/raw/2026-09-06.jsonl`：两行byte offset分别338853140/339233928，行SHA256分别 `78812e91001805c3106f5fad824d24288d57023e55bd8a452451bf2f56f589a3` / `546a4100d75e9eedcb5723019229c28befe1cde9851d1a1853a31dcb4dbd35ea`。这些行无heartbeat_run_id，关联依靠上述原始最终响应hash、时间及request证据，不能宣称存在未保存的run标签。
- 6105/6106分别在02:05:56.742/02:06:50.462记录实际消费42/36条；代码只有通过最终attempt精确送达校验，标记ack、仅收缩确认前缀并严格保存成功后，才签发供日志计数的消费回执。结合完整投影与身份覆盖，严格支持最后49条为合法消费后收缩，而非失踪推断。逐ID `HeartbeatConsumptionReceipt` 本身未独立持久保存，这一可观测性限制仍保留，不能声称直接读到49条耐久消费回执。

02:11:54默认只读重投脚本再次返回already_applied，固定源manifest仍为 `ee6375f4b66005c90d5e2d9fb1c94ad903442a8b8f37e15e0e0da1332832b24b`，raw_modified=false、tools_executed=false，没有第二次追加。最后核验PID957036/父957035和两端口保持正常；02:15:35的global24376、heartbeat6115只剩本次恢复之后的新活动（history未消费1、pending5），旧112均已完成验证。

本次02:20交付不包含暂存、提交、推送、自动重启机制、模型/预算配置变化或他者代写主体语义。112是保守重投范围，不是遗失119条旧运行映射的精确重建；只证明声明的有界投影消费，不声称raw全文全被模型读取。该历史交接时S1仍缺同一测试链的主体文件自写版本及新检查点之后的实际重启—回忆联测，额外聊天按爱莉请求暂停；不能以本次871项测试、三次心跳压缩或单次正确回忆替代这些缺项。后续用户明确要求继续，新的双聊天压缩、受保护重启、正确但由近期历史投影支持的答题，以及自主文件分支仍未覆盖的结果，统一见上方最新续验记录，不沿用本段历史待办充当当前状态。
