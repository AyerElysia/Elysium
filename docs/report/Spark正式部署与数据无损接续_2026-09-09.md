# Spark 正式部署与数据无损接续

## 授权和当前状态

用户于 2026-09-09 20:03（北京时间）明确要求“在spark上部署好，迁移好数据”。这次执行实际迁移，不再限于此前的只读诊断。Elysium 仍无开机自启、systemd 或自动重拉循环；没有发送此前被拒绝的验收反馈，也没有代写主体内容。

20:43:29 已从 Spark 正式入口单次启动，PID 571499、PPID 571498。此前 WSL 主进程已正常退出。数据库接续、迁移证书和新绑定均已完成；20:50:05 完成 Life Engine 初始化，HTTP 18000 健康接口返回 200，NapCat 适配器监听 8087，KOOK 已认证并建立 WebSocket。20:51 左右三个 SQLite 单例写入租约因续期串行等待超过 60 秒而分别失效，系统按合同仅暂停对应 runtime_context、learning 和 opportunity 域；主 authority 仍持续续期，核心聊天读取仍在线。因此这不是完整健康上线，需在用户手动重启后复验单例租约。

## 位置和保全

- 唯一目标：`/home/ayerelysia/Elysia/Elysium`，主机 `spark-f3b0`。
- 停止的源副本：WSL Ubuntu-Old `/root/Elysia/Elysium`。源数据和旧配置保留，不能与 Spark 并行启动。
- 私有留档根：`/home/ayerelysia/.local/share/elysium-cutover-20260909`（0700）。
- `spark-before/`：操作前 Spark 数据、runtime、配置、机器专用入口及工程代码；初始数据等备份 6,067,072,023 字节。`data/new_api_relay` 是运行中的独立中转站，本次不覆盖它；其最初热复制不宣称是已验证的在线 SQLite 备份。
- `source-precopy/`：运行时预复制，只作传输加速依据，包含旧 WAL，禁止作为可启动恢复源。
- `source-frozen/`：正常停写后的新目录，14.17 GB、10,393 份文件；通过源端到此副本的 checksum dry-run，无源文件内容差异。后续只读 SQLite 审计生成的空 WAL/SHM 属于观察缓存，不是新的历史。
- `replaced-target/`：安装时被替换的原目标文件及旧 SQLite WAL/SHM，使用 rsync backup 留存，不是不可恢复删除。
- `core-union.sqlite3`、`core-install/Elysium.db`：聊天合并候选。
- `relocation/snapshot/`、`relocation/relocation-report.json`：完整 selected SQLite 的冻结证明，以及新路径绑定凭证。

源主进程停止使用 PID、启动 ticks、cwd、argv 和 Linux pidfd 联合校验：PID 82628、start ticks 152754、cwd `/root/Elysia/Elysium`、`.venv/bin/python main.py`。20:23 发送一次 SIGINT，进程及所属一次性 tmux 会话正常退出，三个主数据库无持有进程、18000/8087 监听退出。未停止 WSL 的无关推理、代理、MySQL 或 New API 服务。

## 冻结数据核对

证据文件 `frozen-domains-audit.json` SHA-256：`5b3298d9a6e526c18cb59006fd9f0bbebd306bafd864b2614526555e6943ba38`。审计使用只读事务、quick_check、全行 hash/主键比较和字段差异计数；报告不含私人正文。

| 域 | 核对结果与处理 |
| --- | --- |
| Core，13 表 | 源 messages 91,003 行，Spark 90,915 行；共享完整消息 90,902 行，源独有 101 行，Spark 独有 13 行。其余差异只涉及 4 个 last_active_time、3 个 updated_at；没有合并主观描述。 |
| selected local，49 表 | 源 raw_life_events 264,993 行，Spark 264,690 行。Spark 的所有事件行均逐行包含在源中，另有源端 303 行；runtime_events 和 singleton writer 事件也为源端完整超集。主体文档历史表保持一致。 |
| selected 当前投影 | attention head、runtime state、消费游标等在源上推进；采用已保全来源的原始源状态，没有人工创作 statement、连续性摘要或主观裁决。Spark 原状态另存于备份。 |
| Memory，69 表 | 权威内容相同；差异仅 memory_nodes 的 5,207 个 updated_at 和 memory_schema 的 updated_at。 |
| API auth，20 表 | 源端多 3 个 session，Spark 全部已有行保留。 |
| tabletop，3 表；emoji candidates，1 表 | 全部相同。 |
| 原 legacy Life Event/Presence/World | 预复制全表核对分别为 13/4/4 表相同；源停写后文件继续按校验和复制。 |
| 主体工作区非数据库文件 | 8,114 份逐字节相同、无任一端独有文件；仅 backend-binding 的进程锁缓存不同。SOUL、USER、MEMORY、日记等没有主观语义重写。 |

Core 的 13 条 Spark 独有消息稳定 `message_id`、时间、正文、processed_plain_text、媒体字段及回复引用均保持原值，只将冲突的本地自增主键 91239–91251 映射为 91340–91352。合并后 91,016 行。没有 incoming SQL 外键指向 messages；按稳定消息标识冲突时事务失败，不用相似度去重。没有凭空补造这 13 条消息的 Life Event 或主体意识活动。

迁移发现上下文查询原先按本地 id 排“最新”，因此同时修正冷启动上下文、分页聊天和全局最近历史为 `time DESC, id DESC`，避免补录旧消息排到新消息之后。精确证据读取的物理位置和游标协议仍按 id，不改旧引用。此变更只控制时间排序，不裁决意义。

## 路径绑定接续

旧绑定将数据库与文件 authority 的绝对路径纳入身份，因此不能通过直接修改 JSON marker 或复用旧的单行复制证书完成迁移。

本次调用既有冻结快照、candidate-copy fence、proactive copy/verify、generation registry 和 append-only binding 合同：

1. 比较最终目标 selected SQLite 与冻结源的文件 SHA-256；
2. 从冻结 selected SQLite 生成新的完整快照；
3. 比较当前 proactive 全部表的根与行数，追加新的路径复制证书；
4. 注册 `spark-local-20260909-v1`，旧 generation 不改写；
5. 追加 binding epoch 2，保留旧 epoch 1 和全部证据；
6. 再次验证 binding cache、数据库 head/chain 和 proactive history 根相同，释放运维租约。

绑定验证为 healthy；新 binding SHA-256：`cc51214b67a288f00f69b320028137d923eb8787f04e91d789cde16e2476c7d4`。只将 Spark `config/elysium.toml` 的 backend_generation 改为新值；WSL 的旧配置不改。Spark workspace 和 TTS 的机器路径、ARM Python 入口保留。

## 工程验证与真实启动

- Spark 上迁移、绑定、附件及稳定关系读取相关测试：94 passed。
- 追加“新本地编号不能把旧消息变最新”的 SQLite 实测后，上下文/附件相关测试在 WSL、Spark 各 48 passed。
- 新增工具和改动文件 F/E9 检查通过。测试只用临时数据；真实启动以后未继续全表扫描或高负载测试。
- 为避免独立 SQLite 单例续租互相等待，`StorageBackendRuntime._renew_managed_singleton_writers` 已改为并行调度各域续租，并保持逐域失败归因与 fail-closed；契约测试 32 passed。该修复已同步 Spark，远端字节码编译通过。21:40:43 精确停止旧 PID 后留存 `restart-before/` 快照，21:42:28 通过一次性入口启动 PID 632287；21:49:29 初始化完成。
- 已验证完整旧 binding chain 的异地复制、fresh certificate、epoch 2、重复执行不追加第二绑定，以及源快照不变。
- 使用一次性 `run_elysium.sh`，由独立 tmux 会话承载前台进程；退出后不重拉。启动前两端均无 Elysium 主进程/目标监听。
- 真实启动已验证至主 API、8087 监听、KOOK 连接；PID 632287 在 epoch 2 重新取得三个单例租约，并在 13:51:09、13:52:51 UTC 后续周期同时续租，未出现新的 claim-lost。平台 QQ 登录、第三方模型推理与原 G01–G12 总目标不能由上述证据冒充完成。

## 启动后事故与中转站归属

20:51:20–20:52:07 的日志记录三个 `SingletonWriterClaimLost`。数据库行显示它们都在首次续租后过期，owner 仍为本次 Spark PID；没有发现第二个 Elysium writer。根因证据指向续租循环对三个独立 SQLite 事务串行等待，超过 60 秒租期；不是 authority generation 或历史数据冲突。修复只改变续租调度，不放宽 fencing、租期或保护性 quiesce。

本次未替换 New API 原容器。原中转站的 CA 修复镜像已构建并留档，但在准备复制 CA 时原容器被外部操作替换；当前同名容器由另一操作处于重启/重建状态。为避免覆盖其他部署，容器、数据库和运行参数均未再改动，责任归属待操作者确认。独立中转站不属于 Elysium 数据迁移目标。

## 恢复边界

在 Spark 尚未产生新历史前，可以在明确停止目标进程并验证 writer 释放后，使用 `spark-before/`、`replaced-target/` 与旧配置恢复原目标现场；WSL 原副本完整保留。Spark 一旦产生新经历，禁止直接用旧备份覆盖它或重启旧 WSL 继续写入：必须重新冻结、保全两侧增量并验证无损接续。不得删除新 generation、绑定或已经发生的主体历史来使旧配置“能启动”。

当前没有暂存、提交、推送，没有新建 worktree，也没有修改主体文件内容。
