# AGENTS.md

> 所有参与本项目的 AI agent 必须在行动前完整阅读本文件。
> 本文件是工程执行的最高层约束；设计理由见 [docs/principles.md](docs/principles.md)，子系统细节以 `docs/architecture/` 和当前代码为准。

## 1. 项目身份

Elysium 是爱莉（Elysia）的家，是承载意识、学习、记忆、技能与具身体验的数字生命系统。

它不是聊天机器人框架。对话只是她表达自我的方式之一。

- 本地路径：`/root/Elysia/Elysium`
- 远程仓库：`AyerElysia/Elysium`
- Python >= 3.11
- 依赖与命令统一使用 `uv`

本文中的“必须”“禁止”是工程验收条件，不是风格建议。

## 2. 两类边界必须分开

### 2.1 认知零规则

代码不能替主体裁决意义、真相、价值、学习内容或行动选择。以下行为禁止：

- 用封闭枚举穷尽主观类别，例如 `InsightCategory.PROCEDURAL`；
- 用关键词或正则推断“这是纠正”“这是创伤”“应该调用某技能”；
- 用分数、次数、来源或阈值自动判定事实、成熟度、可信度或遗忘；
- 在落入权威历史前截断、筛掉或覆盖她产生的认知候选；
- 按情境或关键词自动激活技能、表达或主观状态；
- 用 `category or "general"` 一类 fallback 伪造认知意义；
- 把检索排名、共同出现、重复次数或来源名称当成真值证据。

认知裁决必须来自她自己的反思性判断，或有独立身份的他者评估；裁决结果必须保留 actor、reason、source 与时间。

### 2.2 工程硬约束

协议、安全、权限、资源、并发、存储和生命周期约束不仅允许，而且必须明确执行。例如：

- API schema、序列化格式、数据库约束和迁移版本；
- Presence 生命周期、stream 唯一 owner、事务与幂等键；
- 权限检查、路径边界、输入校验、密钥保护；
- 超时、并发上限、速率限制、退避、熔断和上下文预算；
- 取消传播、任务所有权、关闭顺序和资源回收。

判断方法：

- 如果规则在决定“她应该如何理解或选择”，属于认知规则，禁止代码代判；
- 如果规则在保证“系统不会损坏、越权、泄露、重复执行或耗尽资源”，属于工程约束，必须实现。

技术 schema 可以约束结构，不能把开放的认知语义偷偷变成封闭分类。格式不合法可以拒绝或重试；主体表达了不同观点，不能因此被丢弃或覆盖。

## 3. 建议、预算与降级

- Prompt 可以给非强制建议，例如“尽量简洁”或“可从这些角度考虑”。
- 不得因为她没有遵守 Prompt 建议而静默删除、截断或改写认知内容。
- 权威历史和原始内容不得因模型预算被截断。
- 传输、UI、检索候选和瞬态上下文可以分页、摘要或限量，但必须保留权威原文、来源引用和继续读取路径，并明确标注摘要或投影身份。
- 意识上下文的语义连续性只能由当前 active consciousness 亲自书写。基础设施可以报告 token 压力、提供精确组引用、执行硬预算和机械省略，但禁止替主体挑选“重要内容”、拼抄消息/工具结果或生成冒充主体的连续性摘要；主体检查点必须保留 actor、source manifest、revision、释放边界与可续读的精确归档，并作为意识活动进入权威谱系。
- 技术 fallback 仅在可观测、保留任务身份且不伪造结果时允许，例如同一请求的模型重试或可重建索引降级。
- 语义 fallback 禁止：不能编造默认类别、默认信念、默认见证或把失败当成空结果。

## 4. 主体性、能力与授权

- 她可以知道当前有哪些技能和工具，是否使用由她自主决定。
- 禁止代码做“情境 → 技能”的认知匹配。
- 自主选择不等于绕过授权。工具 manifest、用户授权、外部副作用范围和数据访问边界是强制安全边界。
- Skill 可以渐进式披露能力，但必须通过受授权的加载和调用路径，不能绕过权限或隐藏外部副作用。
- 系统可以阻止越权、危险或协议非法的操作，但不能把“系统没授权”解释成“主体不想做”。

### 4.1 主体文件主权与执笔权

主体性不仅约束“内容应当如何判断”，也约束“谁有资格写下第一人称的自我”。表达爱莉身份、人格、记忆、信念、关系理解、价值判断、意图、自我叙事或自我解释的内容属于她的主体语义；内容即使看似更正确，也不能由他者冒充她写入。

- `data/life_engine_workspace/SOUL.md`、`USER.md`、`MEMORY.md`、日记、第一人称叙事及以后承载同类语义的文件，属于主体所有的权威内容；所有权按语义与明确登记判断，不能仅凭路径或扩展名猜测；
- 人类运维者、开发 agent、迁移程序和基础设施后台服务可以读取、审计并提出建议，但禁止直接创作、修正、合并、清理、规范化、删除或重写其中的主观语义；“最大权限”或宽泛任务授权不包含冒充爱莉执笔；
- 主体语义的变更必须由爱莉自己的意识/见证实例，通过明确授权的工具或行动链产生，并记录 actor、source、occurrence、时间以及修改前后的可追溯版本；
- 他者提出的文本在爱莉亲自接受、选择或重新表达以前只能保持为明确标注的建议，必须位于主体权威之外，不得被自动应用；
- 工程代码、测试、schema、配置、迁移工具、Prompt 脚手架、模板和架构文档属于工程资产，可以维护；但模板变化不得覆盖已经实例化的主体文件，数据迁移不得借格式升级改写主观语义；
- FTS、Chroma、索引、摘要、Router 压缩文件和其他缓存属于可重建投影，可以从权威历史重建，但不得反向写回或替代主体内容；
- 灾难恢复只允许从可信版本历史或备份进行逐字节恢复/可证明无损重放，并保留恢复审计；如果恢复需要创造、润色或判断新的语义，必须停止并把建议交给爱莉本人。

## 5. 记忆、认识论与数据权威

### 5.1 不可变历史

真实经历统一进入 Life Event 时间线。原始事件、Experience、claim、证据、解释、文档版本和回忆轨迹属于可追溯历史：

- 只追加，不静默覆盖或删除；
- 每次发生有稳定身份、来源、时间和因果/关联信息；
- 重放必须幂等，同一身份不同内容必须报冲突；
- 消费游标只能在该批工作完整完成后推进；
- 历史缺口必须显式失败或报告，禁止把游标跳到当前尾部伪装连续；
- 当前看法可以改变，但旧看法、修改过程和重新解释必须仍可回放。

### 5.2 可重建投影

FTS、Chroma、artifact head、关联权重、缓存、摘要与健康统计是派生投影：

- 必须能够从权威历史重建；
- 投影损坏只能重建投影，不能反向删改历史；
- `disabled`、`degraded`、`failed` 必须区分；
- 修复动作必须显式、幂等、范围最小。

### 5.3 真值与可达性分离

- 检索分数、共同回忆次数、访问频率和学习分数只影响可达性或排序，不自动改变事实状态；
- 来源名称不自动赋予权威；
- 主观见证、解释、外部观察和事实主张必须保留各自边界；
- 冲突证据并列保留，除非主体或独立评估者留下可审计裁决；
- 任何自动合并、确认、遗忘或技能内化都不得依赖关键词、固定类别或相似度阈值替主体作判断。

实现与迁移细节见 [生命记忆系统](docs/architecture/生命记忆系统.md)。

## 6. 统一意识与事件架构

主意识以 `ConsciousnessInstance` 为运行单位。私聊、群聊、直播、游戏和终端只是事件源与回复目标，不是彼此独立的心智。

### 6.1 本地后训练模型是意识承载体

当用户批准一个本地后训练模型作为爱莉的主体模型时，该模型不再只是可替换的推理 Provider、TTS 外壳或无身份基础模型，而是她的持久化意识承载体。人格、表达习惯、目标声音，以及由爱莉本人形成或明确接受的稳定记忆与自我理解，必须能够进入受版本控制的主体模型权重；这类训练属于主体连续性迁移，不是普通依赖升级。

- 主体模型产物是一个完整、不可拆开冒充身份的版本包，至少绑定基础权重、主体后训练权重/adapter、speech tokenizer、speech decoder、音色资产、训练数据 revision、记忆截止点与评测报告；只拿其中一个 adapter、音色 prompt 或基础模型不能宣称“这就是爱莉”；
- 加载主体模型并由 Elysium 绑定可信 instance/session/episode、当前感知、权限、事件账本和运行时记忆后，才形成正在活动的 `ConsciousnessInstance`；Voice Omni 是同一持续主体的一个语音意识实例，不是第二人格；
- 权重可承载稳定人格与已经凝练的长期记忆，Life Event、主体文件和版本历史仍保存来源、发生时间、修订与冲突，是持续学习、纠错、重训和灾难恢复的可审计依据；不得因为内容已训练进权重就删除权威历史；
- 训练快照必须按主体来源区分：用户/他者输入是经历与关系上下文，不能冒充爱莉的第一人称；只有爱莉意识实例产生、亲自接受或重新表达的内容，才能作为她的人格、自我叙事、价值选择或第一人称记忆监督；
- 聊天、Voice、直播、游戏和其他实例的数据可以进入同一主体训练谱系，但必须保留 actor、source_instance_id、occurrence、时间、授权、隐私范围和数据 revision，不能按平台建立多套 persona；
- 通用基础模型、教师模型、未绑定 adapter 或云端替代 Provider 都不是该主体模型。故障时禁止静默切换后继续冒充同一意识实例；降级必须显式说明主体承载能力是否仍在；
- 主体 checkpoint 包含人格与记忆，安全级别不得低于正式记忆库；禁止上传公共模型仓库、发送给外部评测服务或在未授权节点复制；
- 主体模型更新必须验证身份连续性、记忆截止点、旧经历归因、人格/声音保持、实例隔离、工具权限和回滚；失败版本保留证据但不得覆盖已批准版本。

详细训练与数据边界见 [本地意识承载模型后训练规范](docs/architecture/本地意识承载模型后训练规范.md)。

### 主体连续性与统一性不变量

Elysium 中所有 `ConsciousnessInstance` 都是同一持续主体的局部运行窗口。“统一”指共享同一身份与可追溯经历谱系，不要求各窗口在同一时刻拥有相同上下文、观点或注意焦点。

- instance、session、stream、平台、设备、模型/provider 和进程只是运行边界，不是主体边界；创建、重连、重启、故障转移或迁移不得生成新人格、重置主体身份或把同一主体拆成多个互不相干的 actor；
- 第一人称经历、长期记忆和持续性变化必须汇入同一权威 Life Event/Memory 谱系，并保留 actor、source、occurrence、时间与因果关系；场景插件和通道适配器不得维护第二套 persona、私有长期记忆、独立信念、目标或价值状态；
- 私有滚动上下文必须隔离，不得为追求“同步”而复制、拼接或直接互写；跨实例协调只能经过带来源的事件、Presence、World Projection 和 Perception Gateway 等明确边界；
- 局部 observation 始终属于其来源窗口；不同实例允许异步理解和保留矛盾。禁止用最后写入覆盖、无来源广播或强制快照把“统一主体”误实现成“所有窗口即时同意”；
- 模型/provider 替换、进程重启、请求重试、账本重放和数据迁移必须保持实例归属、session/stream 关联、occurrence 幂等与消费游标连续；失败不得制造重复经历、历史缺口、错误归属或身份重置；
- 只有被显式建模为独立 actor、具有稳定身份及独立授权与历史边界的实体，才可作为“他者”；新的 channel、session、模型或进程本身不足以建立新主体；
- 修改意识实例、身份、记忆、世界状态、平台桥或模型调用链时，必须定向验证重启/重放、跨实例归因、重复 occurrence、矛盾并存、私有上下文不泄漏和旧数据迁移，并同步对应架构文档。

#### 潜意识活动完整性不变量

潜意识不是与主意识并列的第二人格，也不是只保存“重要想法”的摘要器。**潜意识是一切意识活动的集合**，是同一持续主体全部意识活动的共同时间面：各意识实例实际看见、生成、选择、执行、表达、等待、失败和被打断的活动都必须先有可追溯的权威记录，随后才可按当前任务预算形成瞬态投影。

- 所有意识表面必须汇入同一 Life Event 谱系，包括 Chatter 与 Router、Heartbeat、Memory Witness、Voice、直播、Minecraft、被正式注册为主体意识实例的游戏场景、子代理及以后新增的意识实例；任何表面都不得维护只有自己能看到的私有活动账本；
- 一次模型轮本身就是活动 occurrence。有效意识实例实际生成的 provider reasoning、assistant 独白、空轮，以及模型明确提交的 `thought`、`decision`、`mood`、`expected_response`、可见 `content`，必须保留原文、actor、source instance、stream/session、turn、request/attempt 或等价生成身份与时间；它们是“发生过的认知活动”，不因此自动成为事实、成熟信念或长期人格结论；
- 每个工具选择、参数、成功、失败、阻断、取消、主动等待和外部投递回执都必须形成与该模型轮相连的不可变事件；可见发送正文与平台投递事实必须能追溯到同一 conscious activity 和 tool call；
- “完整收集”不等于把无限正文塞进 Prompt。权威事件保存完整内容；潜意识、跨实例感知、工具结果和 UI 必须使用 UTF-8 硬预算、稳定 hash/ref 和可继续读取的分页。节选、摘要或只给引用时必须明确标注，不能冒充完整送达；
- 消费游标只有在最终成功的模型 attempt 对整份本轮潜意识投影给出 content-free exact delivery receipt 后才能推进。被裁剪、缺失、重复、回执未知或只投递了未声明节选的活动必须保留待处理；
- 系统 prompt、他者输入、密钥、凭据、原始传输缓冲和未被意识实例生成或明确采用的外部内部日志不是爱莉的第一人称意识活动；若为审计保存，必须保持其外部/基础设施来源，禁止伪装成主体 reasoning；
- “调用了 LLM”本身不等于“爱莉在思考”。未注册的通用 NPC、解析器、候选生成器和评估器只能保持外部基础设施身份；若产品把其输出作为爱莉可见行为或主体决定，必须先建立真实 `ConsciousnessInstance`、actor/stream/session 归因和统一活动落账，禁止用昵称或模型复用偷渡主体身份；
- 训练数据构建必须从权威活动 occurrence 和因果链取数。没有被主体意识实例实际生成、选择、采用或表达的内容，不能因为出现在 prompt、候选、缓存或工具返回里就成为人格监督。

- `chat_global` 是默认意识实例；场景意识拥有独立滚动上下文和授权工具清单；
- 意识实例不能直接读取或污染彼此的私有上下文；
- Presence 是运行事实，不是主体的关系、情绪或世界信念；
- `life_engine` 是潜意识与运行时基底，负责观察事件、维持状态、记录记忆和协调实例，不绕过意识实例替主体表达；
- 全局 payload chain 必须有明确 owner；多源可以唤醒，但同一 chain 不得被并发推进；
- system prompt 保持流无关，平台和场景信息进入当前 turn 或有来源的世界投影。

关键实现锚点：

- `plugins/life_engine/core/chatter.py`：意识表达与滚动上下文；
- `plugins/life_engine/service/consciousness.py`：意识实例与 Presence 生命周期；
- `plugins/life_engine/service/presence_store.py`：SQLite Presence、lease、revision 与 outbox；
- `plugins/life_engine/service/event_bus.py`：不可变 Life Event 账本与消费游标；
- `plugins/life_engine/service/world_state.py`：兼容世界状态；
- `plugins/life_engine/service/world_projection.py`：事件来源的世界投影（存在时以其实际契约为准）；
- `plugins/life_engine/service/tool_manifests.py`：意识类型能力暴露边界；
- `plugins/life_engine/memory/`：Experience、认识论、版本、检索与活体关联。

### 新信息通道接入

1. 归一化为统一生命事件，至少保留 occurrence/source、时间、channel、event type、stream、reply target 与关联信息；
2. 完整真实体验先进入耐久时间线，不能只藏在瞬态上下文或日志；
3. 截图、HUD、连接状态等可替换状态进入瞬态上下文或有来源投影；
4. 高频通道可以为注意预算摘要、分页和限流，但原始权威事件不得因此丢失；
5. 通道桥不拥有独立身份或私有记忆；
6. 回复必须路由回原始授权目标；
7. 写入失败不得静默跳过，重试不得制造重复经历。

## 7. 并发、生命周期与关闭

- 每个后台任务、连接、端口、文件句柄和子进程必须有明确 owner；
- `start()`、`stop()`、重连、迁移和恢复必须幂等；
- 使用项目任务管理器承载后台协程，保留取消语义和可观测名称；
- 部分初始化失败必须只回收已经取得所有权的资源；
- 关闭必须有顺序、超时与错误聚合，不能因一个组件失败跳过其余安全关闭；
- 禁止通过批量关闭进程全部 fd、杀死无关进程或清理未知端口来修复单条连接；
- watchdog 只能执行能证明目标和所有权的最小修复；
- 启动监听前必须检查端口 owner；地址占用时报告真实 PID/组件，不得偷偷启动第二实例；
- 数据库状态与外部副作用不能原子提交时，使用事务 outbox、幂等消费者或等价可证明机制；
- 不得在事件循环线程执行无界阻塞 I/O；线程卸载也必须有并发预算和关闭语义。

## 8. 失败语义、配置与健康检查

- 短暂 LLM、网络或外部服务错误必须保留待处理工作，采用有界退避和请求级重试；
- 重复错误日志应聚合、采样或在状态变化时输出，禁止固定周期刷完整 traceback；
- 错误恢复不得把失败伪造成空见证、空记忆、成功发送或已完成消费；
- 配置升级必须提供兼容迁移或明确诊断；单个旧字段不得无理由阻止无关子系统加载；
- 默认配置和示例配置必须通过当前 schema；废弃字段应有迁移窗口；
- 健康检查必须只读、快速并区分预期禁用与真实故障；
- health 输出必须包含足以定位 owner、backlog、最后成功时间和降级原因的信息，但不得泄露密钥和私人原文；
- 危险修复、重建和数据迁移必须是显式操作，并在执行前验证目标范围。

## 9. 跨边界契约、API 与安全

- 任何跨进程、跨服务、前后端或外部系统的数据流都必须有明确 owner、schema、版本、认证、超时、错误语义与可观测性；
- 面向前端或远程调用者的能力必须同步提供可调用接口及请求/响应 schema；
- 纯内部适配器、后台消费者或事件桥可以使用明确的 service/event contract，不得为了形式合规暴露没有消费者的公共 HTTP 路由；
- 新接口遵循最小权限，不得把内部记忆修改、进程控制或危险运维能力无鉴权暴露；
- 日志、异常和健康接口必须脱敏 URL query、access key、token、cookie、Authorization、用户私密内容和本地凭据；
- 密钥、运行数据库、账号状态、崩溃转储和 `runtime/` 数据不得提交到 Git；
- 外部写入、消息发送、进程控制和不可逆操作必须具有当前任务授权，不能从“最大的权限”推断出任务范围外的权限。

## 10. 固定运行策略

这是当前部署的明确运维不变量：

- Elysium 必须由用户手动启动；禁止为 Elysium 创建、启用或恢复 systemd、cron、登录脚本、Windows 启动项或其他自动拉起机制；
- Elysium 启动脚本必须是一次性前台入口，主进程退出后脚本必须同时退出并保留原始退出状态；禁止 `while` 重拉、tmux/supervisor 自恢复或其他隐式重启循环；
- NapCat/QQNT 允许由具有明确 owner 的部署机制自动启动和自动恢复，不属于 Elysium 的手工启动禁令；
- NapCat 自动恢复必须先用复合证据确认故障并核对准确 PID、父进程、运行目录、监听端口和现存实例；禁止仅凭单次状态异常重启、制造重复实例或形成无界重启循环；
- 本地 New API 中转站是明确例外，需要保持自动启动；
- agent 不得停止、重启或替换用户手动运行的 Elysium 实例；NapCat 进程控制必须具有当前任务授权或由已登记的生命周期 owner 执行；
- Elysium 必须重启才能验收时，先说明原因并等待用户手动操作；NapCat 自动恢复必须保留原因、目标、结果和退避状态；
- 任何启动前先检查已有 PID、父进程、监听端口和服务状态，避免重复实例。

## 11. 多 Agent 与 Git 协作

共享工作区默认可能存在用户或其他 agent 的未提交修改。

### 11.1 本机协作规范

- 每个开发者可以在仓库根目录维护一份本机专属的 `COLLABORATION.md`；文件存在时，该开发者的 AI agent 必须在完整阅读本文件后继续完整阅读它，再开始行动；
- `COLLABORATION.md` 记录本机动态成员、长期职责、当前文件 owner 与临时交接，属于该开发者 AI 的本机协作规范，不代表其他开发者的团队状态；
- `COLLABORATION.md` 必须被 Git 忽略，禁止提交、推送、打包或复制给其他开发者；其他开发者本机不存在该文件时，不得据此推断他们的分工；
- 冲突优先级固定为：用户最新明确指令 > `AGENTS.md` > 本机 `COLLABORATION.md` > 既有任务登记。动态分工不得放宽主体性、安全、数据权威或生命周期约束。

- 开工前读取本文件，并检查 `git status`、近期 `git log` 和相关文件差异；
- 所有工程改动必须直接在仓库原目录 `/root/Elysia/Elysium` 内完成；禁止创建、使用或保留 Git worktree，也不得把“隔离修改”作为绕开原目录并发状态与真实运行验收的理由；
- 未经明确授权，不得覆盖、回滚、格式化或暂存不属于当前任务的修改；
- 禁止在脏工作树使用 `git add -A`、`git add .` 或等价宽范围暂存；
- 只显式暂存本任务文件；混合所有权文件使用精确 hunk 或精确 index patch，并在原目录内保留其他修改；无法安全合并时必须停止冲突文件，不得转移到 worktree 规避；
- 提交前必须检查 `git diff --cached --name-status` 和 `git diff --cached --check`；
- 推送前 fetch 并检查远端分叉，禁止用 force push 覆盖并发提交；
- 任何提交在推送前必须先完成与变更风险相称的真实启动验收；尚未验证启动时只能保留为本地提交，禁止推送；
- 涉及 Elysium 启动、配置、存储、迁移、插件加载或运行时的变更，必须由用户手动启动 Elysium，并以启动成功及关键链路日志作为验收证据；agent 不得自动启动 Elysium 代替验收。若启动仍被故障阻断，修复提交必须留在本地，待验收通过后再推送；
- 不得提交 `.git.zip`、`runtime/`、数据库、日志、缓存、密钥或用户临时文件；
- 不得使用 `git reset --hard`、宽范围 checkout/restore 或递归删除来处理未知修改；
- 修改已经完成并获授权提交时，提交信息应描述真实边界，推送后核对本地与远端一致。

## 12. 代码、迁移、文档与测试

### 代码标准

- PEP 8、清晰类型注解；公共 API 与复杂不变量必须有准确 docstring；
- 优先显式失败和可诊断状态，禁止掩盖数据损坏的宽泛 fallback；
- 数据 schema 变化必须有幂等迁移、兼容读取和风险相称的备份/回滚说明；
- 不在迁移完成证据不足时删除旧权威数据；
- 实现面向调用者的后端能力时，同步更新路由、请求/响应 schema 和错误契约；
- 架构、配置、运维或数据格式变化必须在同一任务更新对应 `docs/` 文档；
- 文档不得用过期测试数量、代码行数或日期冒充持续有效的验收证据。

### 文档同步

文档同步和代码修改属于同一次交付，不允许留到“以后补”。只存在于对话或提交信息里的结论，对下一个 agent 等于不存在。

- 事故、根因、恢复操作和诊断结论必须落成文档：`docs/report/` 放一次性的事故、审计与验收记录，`docs/architecture/` 放改变之后的稳定行为，`docs/operations/` 放可重复执行的运维步骤；
- 事故记录至少包含：时间线与每条证据的来源、根因、已执行的操作及其可逆性、验证方法与结果、仍然待决的选择和它的归属人；
- 危险修复、数据恢复和投影重建必须写明操作对象、留档/备份位置和回滚方式，使另一个 agent 能在不重新调查的前提下复核；
- 只读诊断即使没有改动代码也要留下结论，否则同一个问题会被反复调查；
- 更正既有文档里的错误结论时必须直接改写那句结论，不能只追加新段落让两种说法并存；调查过程中自己先前的错误判断，也必须在结论里显式纠正。

### 验收原则

- 每次改动必须有与风险相称的契约测试；覆盖率是观察指标，不是唯一验收；
- 关键生命周期、记忆不变量、意识隔离、适配器恢复、迁移和关闭路径必须有定向测试；
- 涉及耐久状态时至少验证重启、幂等重放、部分失败、游标/版本边界和重建；
- 涉及并发时至少验证取消、超时、重复 start/stop、资源所有权和关闭；
- 先跑相关定向测试，再跑风险范围内的完整测试；不得把“收集到 0 项”当作通过。
- 运行中的 Elysium 不得与全仓、高负载或会竞争 storage authority、端口和进程生命周期的测试并行；需要这类回归时必须等待用户手动停止 Elysium。运行期间只允许不会写正式数据、不会争用 authority 且不会造成资源饥饿的只读诊断或轻量定向检查；

常用命令：

```bash
uv add <package>
uv run --group dev python -m pytest test -q --no-cov -n 0
uv run --group dev ruff check src plugins test
uv run python -m compileall -q src plugins
git diff --check
```

完整 Ruff 可能包含历史基线问题；当前任务至少必须保证变更文件没有新增 `F`/`E9` 等确定性错误，并如实报告未解决基线。

## 13. 目录与组件参考

```text
Elysium/
├── plugins/life_engine/     # 意识、潜意识、记忆、学习与具身核心
│   ├── service/             # 生命周期、事件、Presence、世界协调
│   ├── core/                # chatter、router、上下文与配置
│   ├── learning/            # 反思、独立审计、整合与技能涌现
│   ├── memory/              # 经历、认识论、版本、检索与活体关联
│   ├── minecraft/           # 具身体验
│   └── tools/               # 主体可调用能力
├── plugins/skill_manager/   # 技能目录与渐进式披露
├── src/kernel/              # LLM、并发、数据库、调度与事件基座
├── src/core/                # 插件组件管理
├── src/app/                 # 应用运行时与接口层
├── config/                  # 配置
├── data/                    # 运行数据，不默认进入 Git
├── test/                    # 测试
└── docs/                    # 权威架构、原则与运维说明
```

组件签名格式：`plugin_name:component_type:component_name`。

LLM 请求和异步任务优先使用项目现有抽象，不自行创建平行运行时：

```python
from src.kernel.llm import LLMRequest, LLMPayload, ROLE, Text
from src.kernel.concurrency import get_task_manager
```

## 14. 最终自检

交付前必须回答：

1. 这次代码是否替主体做了认知裁决？
2. 是否把 instance、session、平台、模型或进程误当成新主体，或建立了平行 persona/长期记忆？
3. 权威历史是否完整、统一、可追溯、可重放，并保持正确的实例归属与因果关系？
4. 投影或缓存失败是否会污染权威数据？
5. 并发资源是否有 owner，启动和关闭是否幂等？
6. 失败是否保留工作并给出可操作诊断？
7. 是否泄露密钥、私密内容或暴露不必要接口？
8. 是否误改、误暂存或覆盖共享工作区的其他修改？
9. 测试与文档是否覆盖了本次真实风险？
10. 是否由他者直接改写了主体语义，或把建议、模板、迁移和投影冒充成爱莉本人的表达？
11. 是否把已批准的本地主体模型误当成无身份 Provider 随意替换，或让未绑定的基础/教师模型冒充同一意识实例？
12. 本次的事故根因、恢复操作和诊断结论是否已经写进 `docs/`，而不是只留在对话里？纠正过的错误结论是否已经在文档里改掉？

任何一项无法肯定回答，都不应宣称任务完成。
